# 增强对照表（源英文 → 加强英文 → 中文本地化）

> spec 共 21 个接口，1015 个对外字段/参数。逐字段对照「原文 → 加强 → 本地化」三态，判断改得更好还是更坏。

## post_dataforseo_backlinks_anchors_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with a detailed overview of anchors used when linking to the specified website with relevant backlink data for each of them. | Returns the anchor texts used in backlinks pointing to a target, each with aggregated link and referring-domain metrics. Use it to analyse anchor-text distribution and spot over-optimised or branded anchors. | 返回指向目标的外链所用的锚文本列表，每条附带外链与引荐域名的聚合指标，用于分析锚文本分布、识别过度优化或品牌词锚文本。 |
| `req.target` | domain, subdomain or webpage to get anchors for required field a domain or a subdomain should be specified without https:// and www. a page should be specified with absolute URL (including http:// or https://) | Domain, subdomain or webpage whose backlink data you want. Specify a domain/subdomain without the `http(s)://` scheme and without `www.`; specify a page as an absolute URL including the scheme. | 要查询外链数据的域名、子域名或网页。域名/子域名填写时去掉 `http(s)://` 和 `www.`；网页则填写含协议的完整绝对 URL。 |
| `req.limit` | the maximum number of returned anchors optional field default value: 100 maximum value: 1000 | Page size: how many items to return in this call. Combine with `offset` to page through the full result set. | 单页返回条数，配合 `offset` 翻页遍历完整结果集。 |
| `req.offset` | offset in the results array of returned anchors optional field default value: 0 if you specify the 10 value, the first ten anchors in the results array will be omitted and the data will be provided for the successive anchors | Number of leading items to skip before returning results, for offset-based pagination together with `limit`. | 返回前跳过的结果条数，与 `limit` 配合实现偏移分页。 |
| `req.internal_list_limit` | maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: referring_links_tld referring_links_types referring_links_attributes referring_links_platform_types referring_links_semantic_locations default value: 10 maximum value: 1000 | Caps how many elements are returned inside the nested aggregation arrays of each item (e.g. `referring_links_tld`, `referring_links_types`); lower it to keep responses small. | 限制每个条目内嵌聚合数组（如 `referring_links_tld`、`referring_links_types`）的元素数量，调小可精简响应体。 |
| `req.backlinks_status_type` | set what backlinks to return and count optional field you can use this field to choose what backlinks will be returned and used for aggregated metrics for your target; possible values: all – all backlinks will be returned and counted; live – backlinks found during the last check will be returned and counted; lost – lost backlinks will be returned and counted; default value: live | Selects which backlinks feed both the returned list and the aggregated metrics: `all` counts every known link, `live` only links confirmed in the latest check, `lost` only links that have disappeared. | 决定纳入列表与聚合指标的外链范围：`all` 计入全部已知外链，`live` 仅最近一次检测仍存活的，`lost` 仅已消失的。 |
| `req.filters` | array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, =, , in, not_in, like, not_like, ilike, not_ilike, match, not_match you can use the % operator with like and not_like to match any string of zero or more characters example: ["referring_links_types.anchors",">","1"] [["broken_pages",">","2"], "and", ["backlinks",">","10"]] [["first_seen",">","2017-10-23 11:31:45 +00:00"], "and", [["anchor","like","%seo%"],"or",["referring_domains",">","10"]]] The full list of possible filters is available here. | Conditions applied to the returned items, combined with `and`/`or` logical operators (up to 8). Reference any item field by name to narrow large result sets. | 对返回条目施加的筛选条件，用 `and`/`or` 组合（最多 8 条），可引用任意条目字段名以收窄大结果集。 |
| `req.order_by` | results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["backlinks,desc"] note that you can set no more than three sorting rules in a single request you should use a comma to separate several sorting rules example: ["backlinks,desc","rank,asc"] | Sorting rules for the returned items; each rule pairs a field with `asc` or `desc`, up to three rules applied in order. | 返回条目的排序规则，每条规则指定字段加 `asc`/`desc`，最多三条按顺序生效。 |
| `req.backlinks_filters` | filter the backlinks of your target optional field you can use this field to filter the initial backlinks that will be included in the dataset for aggregated metrics for your target you can filter the backlinks by all fields available in the response of this endpoint using this parameter, you can include only dofollow backlinks in the response and create a flexible backlinks dataset to calculate the metrics for example: "backlinks_filters": [["dofollow", "=", true]] | Pre-filters the raw backlinks that go into the aggregated metrics for the target, so metrics can be computed over a custom subset (e.g. dofollow-only). | 在计算目标聚合指标前先筛选原始外链，从而基于自定义子集（如仅 dofollow）计算指标。 |
| `req.include_subdomains` | indicates if the subdomains of the target will be included in the search optional field if set to false, the subdomains will be ignored default value: true | When true, subdomains of the target are included in the search; set false to restrict analysis to the target host only. | 为 true 时把目标的子域名一并纳入检索；设为 false 则只分析目标主机本身。 |
| `req.include_indirect_links` | indicates if indirect links to the target will be included in the results optional field if set to true, the results will include data on indirect links pointing to a page that either redirects to the target, or points to a canonical page if set to false, indirect links will be ignored default value: true | When true, results also count indirect links — links to a page that redirects to the target or points to the target's canonical page. | 为 true 时一并统计间接链接，即指向「会重定向到目标」或「指向目标 canonical 页」的页面的链接。 |
| `req.exclude_internal_backlinks` | indicates whether the backlinks from subdomains of the target are excluded optional field if set to false, the backlinks from subdomains of the target will be ommited and you won’t receive the same domain in the response; default value: true | When true, links coming from the target's own subdomains are dropped so the same domain is not reported as its own referrer; set false to keep such internal links. | 为 true 时剔除来自目标自身子域名的链接，避免目标域名成为自己的引荐方；设为 false 则保留这类内部链接。 |
| `req.rank_scale` | defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on a 0–1000 scale default value: one_thousand learn more about how this parameter works and how ranking metrics are calculated in this Help Center article | Chooses the numeric scale for all rank-style metrics (`rank`, `domain_from_rank`, `page_from_rank`): `one_hundred` for a 0–100 scale or `one_thousand` for a 0–1000 scale. | 选择各类 rank 指标（`rank`、`domain_from_rank`、`page_from_rank`）的数值刻度：`one_hundred` 为 0–100，`one_thousand` 为 0–1000。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.target` | target in the post array | The target this result block refers to, echoing the requested domain/subdomain/page. | 该结果块所对应的目标，回显请求的域名/子域名/页面。 |
| `resp.200.tasks.result.total_count` | total number of relevant items in the database | Total number of items matching the request, ignoring pagination. | 匹配请求的条目总数（不受分页影响）。 |
| `resp.200.tasks.result.items_count` | number of items in the results array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | items array | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.type` | type of element = ‘backlinks_anchor’ | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.items.anchor` | anchor of the backlink | The anchor text used by the backlink(s); for the backlinks endpoint, the clickable text of this link. | 外链所用的锚文本；在外链明细接口中即该链接的可点击文字。 |
| `resp.200.tasks.result.items.rank` | rank of the anchor links rank volume that referring websites pass to the target through links with a particular anchor rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity, on the chosen `rank_scale`; higher means stronger link authority. | 该实体基于外链的权重评分，按所选 `rank_scale` 计算，越高表示链接权重越强。 |
| `resp.200.tasks.result.items.backlinks` | indicates the number of backlinks | Number of backlinks pointing to the entity. | 指向该实体的外链数量。 |
| `resp.200.tasks.result.items.first_seen` | date and time when our crawler found the backlink with this anchor for the first time in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date the entity (link/anchor/page) was first discovered in the index. | 该实体（链接/锚文本/页面）首次被索引发现的日期。 |
| `resp.200.tasks.result.items.lost_date` | date and time when the last backlink with this anchor was lost indicates the date and time when our crawler visited the page and it responded with 4xx or 5xx status code or the last backlink was removed in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2017-01-24 13:20:59 +00:00 | Date the entity was detected as lost; null if it is still live. | 该实体被检测为丢失的日期；仍存活则为 null。 |
| `resp.200.tasks.result.items.backlinks_spam_score` | average spam score of all backlinks with this anchor learn more about how the metric is calculated on this help center page | Aggregated spam score of the entity's backlink profile; higher values flag riskier link sources. | 该实体外链画像的聚合垃圾分，越高表示链接来源风险越大。 |
| `resp.200.tasks.result.items.broken_backlinks` | number of broken backlinks number of broken backlinks pointing to the target | Number of backlinks pointing to broken (non-resolving) pages of the entity. | 指向该实体失效（无法访问）页面的外链数量。 |
| `resp.200.tasks.result.items.broken_pages` | number of broken pages number of pages that respond with 4xx or 5xx status codes where backlinks are pointing to | Number of the entity's pages that are broken but still receive backlinks. | 该实体仍在接收外链但已失效的页面数量。 |
| `resp.200.tasks.result.items.referring_domains` | indicates the number of referring domains | Number of distinct domains linking to the entity. | 链向该实体的不同域名数量。 |
| `resp.200.tasks.result.items.referring_domains_nofollow` | number of domains pointing at least one nofollow link to the target | Number of referring domains whose links are all nofollow. | 链接全为 nofollow 的引荐域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains` | indicates the number of referring main domains | Number of distinct main (root) domains linking to the entity. | 链向该实体的不同主（根）域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains_nofollow` | number of main domains pointing at least one nofollow link to the target | Number of referring main domains whose links are all nofollow. | 链接全为 nofollow 的引荐主域名数量。 |
| `resp.200.tasks.result.items.referring_ips` | number of referring IP addresses number of IP addresses pointing to this page | Number of distinct IP addresses the backlinks originate from. | 外链来源的不同 IP 地址数量。 |
| `resp.200.tasks.result.items.referring_subnets` | number of referring subnetworks | Number of distinct subnets the backlinks originate from. | 外链来源的不同子网数量。 |
| `resp.200.tasks.result.items.referring_pages` | indicates the number of pages pointing to target with this anchor | Number of distinct pages linking to the entity. | 链向该实体的不同页面数量。 |
| `resp.200.tasks.result.items.referring_links_tld` | top-level domains of the referring links contains top level domains and referring link count per each | Breakdown of referring links by the top-level domain of their source. | 引荐链接按来源顶级域名（TLD）的分布。 |
| `resp.200.tasks.result.items.referring_links_types` | types of referring links indicates the types of the referring links and link count per each type possible values: anchor, image, link, meta, canonical, alternate, redirect | Breakdown of referring links by link type (e.g. anchor, image, redirect). | 引荐链接按链接类型（如锚文本、图片、重定向）的分布。 |
| `resp.200.tasks.result.items.referring_links_attributes` | link attributes of the referring links indicates link attributes of the referring links and link count per each attribute | Breakdown of referring links by their HTML attributes (e.g. nofollow, sponsored). | 引荐链接按其 HTML 属性（如 nofollow、sponsored）的分布。 |
| `resp.200.tasks.result.items.referring_links_platform_types` | types of referring platforms indicates referring platform types and and link count per each platform possible values: cms, blogs, ecommerce, message-boards, wikis, news, organization | Breakdown of referring links by the platform type of their source site. | 引荐链接按来源站点平台类型的分布。 |
| `resp.200.tasks.result.items.referring_links_semantic_locations` | semantic locations of the referring links indicates semantic elements in HTML where the referring links are located and link count per each semantic location you can get the full list of semantic elements here examples: article, section, summary | Breakdown of referring links by the semantic location on the source page (e.g. content, footer). | 引荐链接按其在来源页面上的语义位置（如正文、页脚）的分布。 |
| `resp.200.tasks.result.items.referring_links_countries` | ISO country codes of the referring links indicates ISO country codes of the domains where the referring links are located and the link count per each country | Breakdown of referring links by the country of their source. | 引荐链接按来源国家/地区的分布。 |
| `resp.200.tasks.result.items.referring_pages_nofollow` | number of referring pages pointing at least one nofollow link to the target with this anchor | Number of referring pages whose links are all nofollow. | 链接全为 nofollow 的引荐页面数量。 |

## post_dataforseo_backlinks_backlinks_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with a list of backlinks and relevant data for the specified domain, subdomain, or webpage. | Returns the individual backlinks pointing to a target with full per-link detail (source page, anchor, attributes, ranks, status). The core endpoint for inspecting a target's link profile link by link. | 返回指向目标的逐条外链明细（来源页、锚文本、属性、rank、状态等），是逐条审视目标链接画像的核心接口。 |
| `req.target` | domain, subdomain or webpage to get backlinks for required field a domain or a subdomain should be specified without https:// and www. a page should be specified with absolute URL (including http:// or https://) | Domain, subdomain or webpage whose backlink data you want. Specify a domain/subdomain without the `http(s)://` scheme and without `www.`; specify a page as an absolute URL including the scheme. | 要查询外链数据的域名、子域名或网页。域名/子域名填写时去掉 `http(s)://` 和 `www.`；网页则填写含协议的完整绝对 URL。 |
| `req.mode` | results grouping type optional field possible grouping types: as_is – returns all backlinks one_per_domain – returns one backlink per domain one_per_anchor – returns one backlink per anchor default value: as_is | Grouping of the returned backlinks: `as_is` returns every backlink, `one_per_domain` keeps one per referring domain, `one_per_anchor` keeps one per anchor text. | 返回外链的分组方式：`as_is` 返回全部，`one_per_domain` 每个引荐域名保留一条，`one_per_anchor` 每种锚文本保留一条。 |
| `req.custom_mode` | detailed results grouping type optional field use this object to get a specific number of backlinks per field if you use custom_mode, then mode will be ignored example: "custom_mode": {"field": "domain", "value": 100} | Fine-grained grouping that returns a fixed number of backlinks per chosen field; when set it overrides `mode`. | 更精细的分组，按指定字段返回固定条数的外链；设置后将覆盖 `mode`。 |
| `req.filters` | array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: =, , in, not_in, like, not_like, ilike, not_ilike, regex, not_regex, match, not_match you can use the % operator with like and not_like to match any string of zero or more characters example: ["rank",">","80"] [["page_from_rank",">","55"], "and", ["dofollow","=",true]] [["first_seen",">","2017-10-23 11:31:45 +00:00"], "and", [["anchor","like","%seo%"],"or",["text_pre","like","%seo%"]]] The full list of possible filters is available here. | Conditions applied to the returned items, combined with `and`/`or` logical operators (up to 8). Reference any item field by name to narrow large result sets. | 对返回条目施加的筛选条件，用 `and`/`or` 组合（最多 8 条），可引用任意条目字段名以收窄大结果集。 |
| `req.order_by` | results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["rank,desc"] note that you can set no more than three sorting rules in a single request you should use a comma to separate several sorting rules example: ["domain_from_rank,desc","page_from_rank,asc"] | Sorting rules for the returned items; each rule pairs a field with `asc` or `desc`, up to three rules applied in order. | 返回条目的排序规则，每条规则指定字段加 `asc`/`desc`，最多三条按顺序生效。 |
| `req.offset` | offset in the results array of the returned backlinks optional field default value: 0 if you specify the 10 value, the first ten backlinks in the results array will be omitted and the data will be provided for the successive backlinks; Note: the maximum value is 20,000, use the search_after_token if you would like to offset more results | Number of leading items to skip before returning results, for offset-based pagination together with `limit`. | 返回前跳过的结果条数，与 `limit` 配合实现偏移分页。 |
| `req.search_after_token` | token for subsequent requests optional field provided in the identical filed of the response to each request; use this parameter to avoid timeouts while trying to obtain over 20,000 results in a single request; by specifying the unique search_after_token value from the response array, you will get the subsequent results of the initial task; search_after_token values are unique for each subsequent task ; Note: if the search_after_token is specified in the request, all other parameters should be identical to the previous request | Continuation token for deep pagination beyond the 20,000-result offset cap. Pass the token from the previous response, keeping all other parameters identical, to fetch the next page. | 用于突破 20,000 条偏移上限的深翻页续传令牌。传入上一次响应返回的令牌（其余参数须保持一致）即可取下一页。 |
| `req.limit` | the maximum number of returned backlinks optional field default value: 100 maximum value: 1000 | Page size: how many items to return in this call. Combine with `offset` to page through the full result set. | 单页返回条数，配合 `offset` 翻页遍历完整结果集。 |
| `req.backlinks_status_type` | set what backlinks to return and count optional field you can use this field to choose what backlinks will be returned and used for aggregated metrics for your target; possible values: all – all backlinks will be returned and counted; live – backlinks found during the last check will be returned and counted; lost – lost backlinks will be returned and counted; default value: live | Selects which backlinks feed both the returned list and the aggregated metrics: `all` counts every known link, `live` only links confirmed in the latest check, `lost` only links that have disappeared. | 决定纳入列表与聚合指标的外链范围：`all` 计入全部已知外链，`live` 仅最近一次检测仍存活的，`lost` 仅已消失的。 |
| `req.include_subdomains` | indicates if the subdomains of the target will be included in the search optional field if set to false, the subdomains will be ignored default value: true | When true, subdomains of the target are included in the search; set false to restrict analysis to the target host only. | 为 true 时把目标的子域名一并纳入检索；设为 false 则只分析目标主机本身。 |
| `req.exclude_internal_backlinks` | indicates if internal backlinks from subdomains to the target will be excluded from the results optional field if set to true, the results will not include data on internal backlinks from subdomains of the same domain as target if set to false, internal links will be included in the results default value: true | When true, links coming from the target's own subdomains are dropped so the same domain is not reported as its own referrer; set false to keep such internal links. | 为 true 时剔除来自目标自身子域名的链接，避免目标域名成为自己的引荐方；设为 false 则保留这类内部链接。 |
| `req.rank_scale` | defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on a 0–1000 scale default value: one_thousand learn more about how this parameter works and how ranking metrics are calculated in this Help Center article | Chooses the numeric scale for all rank-style metrics (`rank`, `domain_from_rank`, `page_from_rank`): `one_hundred` for a 0–100 scale or `one_thousand` for a 0–1000 scale. | 选择各类 rank 指标（`rank`、`domain_from_rank`、`page_from_rank`）的数值刻度：`one_hundred` 为 0–100，`one_thousand` 为 0–1000。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.target` | target domain in a POST array | The target this result block refers to, echoing the requested domain/subdomain/page. | 该结果块所对应的目标，回显请求的域名/子域名/页面。 |
| `resp.200.tasks.result.mode` | mode specified in a POST array | Grouping mode that was applied to these results, echoing the request. | 应用于这些结果的分组模式，回显请求。 |
| `resp.200.tasks.result.custom_mode` | custom mode specified in a POST array | Custom grouping that was applied, echoing the request. | 应用的自定义分组，回显请求。 |
| `resp.200.tasks.result.total_count` | total amount of results relevant the request | Total number of items matching the request, ignoring pagination. | 匹配请求的条目总数（不受分页影响）。 |
| `resp.200.tasks.result.items_count` | the number of results returned in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | contains relevant backlinks and referring domains data | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.type` | type of element = ‘backlink’ | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.items.domain_from` | domain referring to the target domain or webpage | Domain of the page that hosts the backlink (the source). | 承载该外链的来源页面所在域名。 |
| `resp.200.tasks.result.items.url_from` | URL of the page where the backlink is found | Full URL of the source page that contains the backlink. | 包含该外链的来源页面完整 URL。 |
| `resp.200.tasks.result.items.url_from_https` | indicates whether the referring URL is secured with HTTPS if true, the referring URL is secured with HTTPS | Whether the source URL uses HTTPS. | 来源 URL 是否使用 HTTPS。 |
| `resp.200.tasks.result.items.domain_to` | domain the backlink is pointing to | Domain the backlink points to (the target side). | 该外链所指向的目标侧域名。 |
| `resp.200.tasks.result.items.url_to` | URL the backlink is pointing to | Full URL the backlink points to. | 该外链指向的完整 URL。 |
| `resp.200.tasks.result.items.url_to_https` | indicates if the URL the backlink is pointing to is secured with HTTPS if true, the URL is secured with HTTPS | Whether the destination URL uses HTTPS. | 目标 URL 是否使用 HTTPS。 |
| `resp.200.tasks.result.items.tld_from` | top-level domain of the referring URL | Top-level domain of the source page. | 来源页面的顶级域名（TLD）。 |
| `resp.200.tasks.result.items.is_new` | indicates whether the backlink is new if true, the backlink was found on the page last time our crawler visited it | Whether this backlink was newly discovered in the latest check. | 该外链是否为最近一次检测新发现。 |
| `resp.200.tasks.result.items.is_lost` | indicates whether the backlink was removed if true, the backlink or the entire page was removed | Whether this backlink has been detected as lost. | 该外链是否已被检测为丢失。 |
| `resp.200.tasks.result.items.backlink_spam_score` | spam score of the backlink learn more about how the metric is calculated on this help center page | Spam score of this individual backlink; higher values flag a riskier source. | 该条外链的垃圾分，越高表示来源风险越大。 |
| `resp.200.tasks.result.items.rank` | backlink rank rank that the given backlink passes to the target rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity, on the chosen `rank_scale`; higher means stronger link authority. | 该实体基于外链的权重评分，按所选 `rank_scale` 计算，越高表示链接权重越强。 |
| `resp.200.tasks.result.items.page_from_rank` | page rank of the referring page page_from_rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Authority score of the source page, on the chosen `rank_scale`. | 来源页面的权重评分，按 `rank_scale` 计算。 |
| `resp.200.tasks.result.items.domain_from_rank` | domain rank of the referring domain domain_from_rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Authority score of the source domain, on the chosen `rank_scale`. | 来源域名的权重评分，按 `rank_scale` 计算。 |
| `resp.200.tasks.result.items.domain_from_platform_type` | platform types of the referring domain possible values: cms, blogs, ecommerce, message-boards, wikis, news, organization | Platform type(s) detected for the source domain (e.g. cms, blogs). | 来源域名检测到的平台类型（如 cms、blogs）。 |
| `resp.200.tasks.result.items.domain_from_is_ip` | indicates if the domain is IP if true, the domain functions as an IP address and does not have a domain name | Whether the source is addressed by a bare IP rather than a domain name. | 来源是否以裸 IP（而非域名）寻址。 |
| `resp.200.tasks.result.items.domain_from_ip` | IP address of the referring domain | IP address of the source domain. | 来源域名的 IP 地址。 |
| `resp.200.tasks.result.items.domain_from_country` | ISO country code of the referring domain | Country associated with the source domain. | 来源域名关联的国家/地区。 |
| `resp.200.tasks.result.items.page_from_external_links` | number of external links found on the referring page | Number of external links on the source page. | 来源页面上的外部链接数量。 |
| `resp.200.tasks.result.items.page_from_internal_links` | number of internal links found on the referring page | Number of internal links on the source page. | 来源页面上的内部链接数量。 |
| `resp.200.tasks.result.items.page_from_size` | size of the referring page, in bytes example: 63357 | Size of the source page in bytes. | 来源页面的字节大小。 |
| `resp.200.tasks.result.items.page_from_encoding` | character encoding of the referring page example: utf-8 | Character encoding of the source page. | 来源页面的字符编码。 |
| `resp.200.tasks.result.items.page_from_language` | language of the referring page in ISO 639-1 format example: en | Detected language of the source page. | 来源页面检测到的语言。 |
| `resp.200.tasks.result.items.page_from_title` | title of the referring page | Title of the source page. | 来源页面的标题。 |
| `resp.200.tasks.result.items.page_from_status_code` | HTTP status code returned by the referring page example: 200 | HTTP status code returned by the source page when last fetched. | 最近一次抓取来源页面返回的 HTTP 状态码。 |
| `resp.200.tasks.result.items.first_seen` | date and time when our crawler found the backlink for the first time in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date the entity (link/anchor/page) was first discovered in the index. | 该实体（链接/锚文本/页面）首次被索引发现的日期。 |
| `resp.200.tasks.result.items.prev_seen` | previous to the most recent date when our crawler visited the backlink in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date of the previous time the entity was seen before the most recent check. | 最近一次检测之前，该实体上一次被发现的日期。 |
| `resp.200.tasks.result.items.last_seen` | most recent date when our crawler visited the backlink in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Most recent date the entity was confirmed present. | 该实体最近一次被确认存在的日期。 |
| `resp.200.tasks.result.items.item_type` | link type possible values: anchor, image, meta, canonical, alternate, redirect | Specific type of this backlink record (e.g. anchor, redirect, image). | 该外链记录的具体类型（如锚文本、重定向、图片）。 |
| `resp.200.tasks.result.items.attributes` | link attributes of the referring links example: nofollow | HTML rel attributes of the link (e.g. nofollow, ugc, sponsored). | 链接的 HTML rel 属性（如 nofollow、ugc、sponsored）。 |
| `resp.200.tasks.result.items.dofollow` | indicates whether the backlink is dofollow if false, the backlink is nofollow | Whether the link passes authority (true = dofollow, false = nofollow). | 该链接是否传递权重（true 为 dofollow，false 为 nofollow）。 |
| `resp.200.tasks.result.items.original` | indicates whether the backlink was present on the referring page when our crawler first visited it | Whether this is the original link as opposed to a duplicate occurrence. | 该链接是否为原始链接（而非重复出现）。 |
| `resp.200.tasks.result.items.alt` | alternative text of the image this field will be null if backlink type is not image | Alt text of the link when it is an image link. | 图片链接时该链接的 alt 文本。 |
| `resp.200.tasks.result.items.image_url` | URL of the image the URL leading to the image on the original resource or DataForSEO storage (in case the original source is not available) | URL of the image when the backlink is an image link. | 图片外链时所用图片的 URL。 |
| `resp.200.tasks.result.items.anchor` | anchor text of the backlink | The anchor text used by the backlink(s); for the backlinks endpoint, the clickable text of this link. | 外链所用的锚文本；在外链明细接口中即该链接的可点击文字。 |
| `resp.200.tasks.result.items.text_pre` | snippet before the anchor text | Text appearing immediately before the anchor on the source page. | 来源页面上锚文本前紧邻的文字。 |
| `resp.200.tasks.result.items.text_post` | snippet after the anchor text | Text appearing immediately after the anchor on the source page. | 来源页面上锚文本后紧邻的文字。 |
| `resp.200.tasks.result.items.semantic_location` | indicates semantic element in HTML where the backlink is found you can get the full list of semantic elements here examples: article, section, summary | Semantic location of the link on the source page (e.g. content, header, footer). | 链接在来源页面上的语义位置（如正文、页眉、页脚）。 |
| `resp.200.tasks.result.items.links_count` | number of identical backlinks found on the referring page | Number of links from the source page to the target. | 来源页面指向目标的链接数量。 |
| `resp.200.tasks.result.items.group_count` | indicates total number of backlinks from this domain for example, if mode is set to one_per_domain, this field will indicate the total number of backlinks coming from this domain | Number of grouped duplicate links represented by this record. | 该记录所代表的合并重复链接数量。 |
| `resp.200.tasks.result.items.is_broken` | indicates whether the backlink is broken if true, the backlink is pointing to a page responding with a 4xx or 5xx status code | Whether the link is broken (its destination does not resolve). | 该链接是否失效（目标无法访问）。 |
| `resp.200.tasks.result.items.url_to_status_code` | status code of the referenced page if the value is null, our crawler hasn’t yet visited the webpage the link is pointing to example: 200 | HTTP status code returned by the destination URL. | 目标 URL 返回的 HTTP 状态码。 |
| `resp.200.tasks.result.items.url_to_spam_score` | spam score of the referenced page if the value is null, our crawler hasn’t yet visited the webpage the link is pointing to; learn more about how the metric is calculated on this help center page | Spam score of the destination URL. | 目标 URL 的垃圾分。 |
| `resp.200.tasks.result.items.url_to_redirect_target` | target url of the redirect target page the redirect is pointing to | Final URL the destination redirects to, if any. | 目标若发生重定向，则最终跳转到的 URL。 |
| `resp.200.tasks.result.items.is_indirect_link` | indicates whether the backlink is an indirect link if true, the backlink is an indirect link pointing to a page that either redirects to url_to, or points to a canonical page | Whether the link reaches the target indirectly via a redirect or canonical chain. | 该链接是否经由重定向或 canonical 链间接到达目标。 |
| `resp.200.tasks.result.items.indirect_link_path` | indirect link path indicates a URL or a sequence of URLs that lead to url_to | The redirect/canonical hops the indirect link passes through to reach the target. | 间接链接到达目标所经过的重定向/canonical 跳转路径。 |
| `resp.200.tasks.result.items.indirect_link_path.type` | indirect link type possible values: redirect, canonical | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.items.indirect_link_path.status_code` | HTTP status code of the URL | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.result.items.indirect_link_path.url` | indirect link URL | URL of the page this item describes. | 该条目所描述页面的 URL。 |
| `resp.200.tasks.result.search_after_token` | token for subsequent requests by specifying the unique search_after_token when setting a new task, you will get the subsequent results of the initial task; search_after_token values are unique for each subsequent task | Token to pass as `search_after_token` in the next request to continue deep pagination. | 用于下一次请求作为 `search_after_token` 续传深翻页的令牌。 |

## post_dataforseo_backlinks_bulk_backlinks_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with the number of backlinks pointing to domains, subdomains, and pages specified in the `targets` array. The returned numbers correspond to all **live** backlinks, that is, total number of referring links with all attributes (e.g., nofollow, noreferrer, ugc, sponsored etc) that were found during the latest check. | Returns the total backlink count for up to 1000 targets in a single request — a fast way to compare link volume across many domains or pages. | 单次请求返回多达 1000 个目标的外链总数，用于快速横比多个域名或页面的外链体量。 |
| `req.targets` | domains, subdomains or webpages to get the number of backlinks for required field you can set up to 1000 domains, subdomains or webpages the domain or subdomain should be specified without https:// and www. the page should be specified with absolute URL (including http:// or https://) example: "targets": [ "forbes.com", "cnn.com", "bbc.com", "yelp.com", "https://www.apple.com/iphone/", "https://ahrefs.com/blog/", "ibm.com", "https://variety.com/", "https://stackoverflow.com/", "www.trustpilot.com" ] | Set of domains, subdomains or webpages to query in one bulk request, each formatted the same way as `target`. | 单次批量请求要查询的一组域名、子域名或网页，每项格式与 `target` 相同。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.items_count` | the number of results returned in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | contains relevant backlink data | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.target` | domain, subdomain or webpage from a POST array | The target this row reports on, echoing one of the requested `targets`. | 该行所统计的目标，回显请求 `targets` 中的某一项。 |
| `resp.200.tasks.result.items.backlinks` | number of backlinks pointing to the target | Number of backlinks pointing to the entity. | 指向该实体的外链数量。 |

## post_dataforseo_backlinks_bulk_new_lost_backlinks_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with the number of new and lost backlinks for the domains, subdomains, and pages specified in the `targets` array. | Returns the number of newly gained and lost backlinks since a given date for up to 1000 targets, for tracking link velocity in bulk. | 返回多达 1000 个目标自指定日期以来新增与丢失的外链数量，用于批量跟踪外链增减趋势。 |
| `req.targets` | domains, subdomains or webpages to get new & lost backlinks for required field you can set up to 1000 domains, subdomains or webpages the domain or subdomain should be specified without https:// and www. the page should be specified with absolute URL (including http:// or https://) example: "targets": [ "forbes.com", "cnn.com", "bbc.com", "yelp.com", "https://www.apple.com/iphone/", "https://ahrefs.com/blog/", "ibm.com", "https://variety.com/", "https://stackoverflow.com/", "www.trustpilot.com" ] | Set of domains, subdomains or webpages to query in one bulk request, each formatted the same way as `target`. | 单次批量请求要查询的一组域名、子域名或网页，每项格式与 `target` 相同。 |
| `req.date_from` | starting date of the time range optional field this field indicates the date which will be used as a threshold for new and lost backlinks; the backlinks that appeared in our index after the specified date will be considered as new; the backlinks that weren’t found after the specified date, but were present before, will be considered as lost; default value: today’s date -(minus) one month; e.g. if today is 2021-10-13, default date_from will be 2021-09-13. minimum value equals today’s date -(minus) one year; e.g. if today is 2021-10-13, minimum date_from will be 2020-10-13. date format: "yyyy-mm-dd" example: "2021-01-01" | Start of the time range (format `yyyy-mm-dd`); if omitted, the endpoint's minimum supported date is used. | 时间范围起始日期（格式 `yyyy-mm-dd`）；不填则使用该接口支持的最小日期。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.items_count` | the number of results returned in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | contains relevant backlinks and referring domains data | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.target` | domain, subdomain or webpage from a POST array | The target this row reports on, echoing one of the requested `targets`. | 该行所统计的目标，回显请求 `targets` 中的某一项。 |
| `resp.200.tasks.result.items.new_backlinks` | number of new backlinks number of new backlinks pointing to the target | Number of backlinks newly gained since `date_from`. | 自 `date_from` 起新增的外链数量。 |
| `resp.200.tasks.result.items.lost_backlinks` | number of lost backlinks number of lost backlinks of the target | Number of backlinks lost since `date_from`. | 自 `date_from` 起丢失的外链数量。 |

## post_dataforseo_backlinks_bulk_new_lost_referring_domains_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with the number of referring domains pointing to the domains, subdomains and pages specified in the `targets` array. | Returns the count of newly gained and lost referring domains (and main domains) since a given date for up to 1000 targets. | 返回多达 1000 个目标自指定日期以来新增与丢失的引荐域名（及主域名）数量。 |
| `req.targets` | domains, subdomains or webpages to get new & lost referring domains for required field you can set up to 1000 domains, subdomains or webpages the domain or subdomain should be specified without https:// and www. the page should be specified with absolute URL (including http:// or https://) example: "targets": [ "forbes.com", "cnn.com", "bbc.com", "yelp.com", "https://www.apple.com/iphone/", "https://ahrefs.com/blog/", "ibm.com", "https://variety.com/", "https://stackoverflow.com/", "www.trustpilot.com" ] | Set of domains, subdomains or webpages to query in one bulk request, each formatted the same way as `target`. | 单次批量请求要查询的一组域名、子域名或网页，每项格式与 `target` 相同。 |
| `req.date_from` | starting date of the time range optional field this field indicates the date which will be used as a threshold for new and lost referring domains; the referring domains that appeared in our index after the specified date will be considered as new; the referring domains that weren’t found after the specified date, but were present before, will be considered as lost; default value: today’s date -(minus) one month; e.g. if today is 2021-10-13, default date_from will be 2021-09-13. minimum value equals today’s date -(minus) one year; e.g. if today is 2021-10-13, minimum date_from will be 2020-10-13. date format: "yyyy-mm-dd" example: "2021-01-01" | Start of the time range (format `yyyy-mm-dd`); if omitted, the endpoint's minimum supported date is used. | 时间范围起始日期（格式 `yyyy-mm-dd`）；不填则使用该接口支持的最小日期。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.items_count` | the number of results returned in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | contains relevant backlinks and referring domains data | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.target` | domain, subdomain or webpage from a POST array | The target this row reports on, echoing one of the requested `targets`. | 该行所统计的目标，回显请求 `targets` 中的某一项。 |
| `resp.200.tasks.result.items.new_referring_domains` | number of new referring domains number of new referring domains pointing to the target | Number of referring domains newly gained since `date_from`. | 自 `date_from` 起新增的引荐域名数量。 |
| `resp.200.tasks.result.items.lost_referring_domains` | number of lost referring domains number of lost referring domains of the target | Number of referring domains lost since `date_from`. | 自 `date_from` 起丢失的引荐域名数量。 |
| `resp.200.tasks.result.items.new_referring_main_domains` | number of new referring main domains pointing to the target | Number of referring main domains newly gained since `date_from`. | 自 `date_from` 起新增的引荐主域名数量。 |
| `resp.200.tasks.result.items.lost_referring_main_domains` | number of lost referring main domains pointing to the target | Number of referring main domains lost since `date_from`. | 自 `date_from` 起丢失的引荐主域名数量。 |

## post_dataforseo_backlinks_bulk_pages_summary_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with a comprehensive overview of backlinks and related data for a bulk of up to 1000 pages, domains, or subdomains. If you indicate a single page as a target, you will get comprehensive summary data on all backlinks for that page. | Returns a compact backlink summary (rank, link and referring counts) per target for up to 1000 pages/domains at once. | 单次请求返回多达 1000 个页面/域名的外链摘要（rank、外链与引荐计数）。 |
| `req.targets` | domains, subdomains or webpages to get summary data for required field a domain or a subdomain should be specified without https:// and www. a page should be specified with absolute URL (including http:// or https://) you can specify up to 1000 pages, domains, or subdomains in each request. note that the URLs you set in a single request cannot belong to more than 100 different domains. | Set of domains, subdomains or webpages to query in one bulk request, each formatted the same way as `target`. | 单次批量请求要查询的一组域名、子域名或网页，每项格式与 `target` 相同。 |
| `req.include_subdomains` | indicates if the subdomains of the target will be included in the search optional field if set to false, the subdomains will be ignored default value: true | When true, subdomains of the target are included in the search; set false to restrict analysis to the target host only. | 为 true 时把目标的子域名一并纳入检索；设为 false 则只分析目标主机本身。 |
| `req.rank_scale` | defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on a 0–1000 scale default value: one_thousand learn more about how this parameter works and how ranking metrics are calculated in this Help Center article | Chooses the numeric scale for all rank-style metrics (`rank`, `domain_from_rank`, `page_from_rank`): `one_hundred` for a 0–100 scale or `one_thousand` for a 0–1000 scale. | 选择各类 rank 指标（`rank`、`domain_from_rank`、`page_from_rank`）的数值刻度：`one_hundred` 为 0–100，`one_thousand` 为 0–1000。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.total_count` | total number of relevant items in the database | Total number of items matching the request, ignoring pagination. | 匹配请求的条目总数（不受分页影响）。 |
| `resp.200.tasks.result.items_count` | number of items in the results array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | items array | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.type` | type of element = ‘backlinks_page_summary’ | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.items.url` | page URL | URL of the page this item describes. | 该条目所描述页面的 URL。 |
| `resp.200.tasks.result.items.rank` | page rank rank of the page on the target website rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity, on the chosen `rank_scale`; higher means stronger link authority. | 该实体基于外链的权重评分，按所选 `rank_scale` 计算，越高表示链接权重越强。 |
| `resp.200.tasks.result.items.main_domain_rank` | rank of the main domain rank of the main domain is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity's main domain, on the chosen `rank_scale`. | 该实体所属主域名基于外链的权重评分，按 `rank_scale` 计算。 |
| `resp.200.tasks.result.items.backlinks` | number of backlinks | Number of backlinks pointing to the entity. | 指向该实体的外链数量。 |
| `resp.200.tasks.result.items.first_seen` | date and time when our crawler found a backlink to this page for the first time in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date the entity (link/anchor/page) was first discovered in the index. | 该实体（链接/锚文本/页面）首次被索引发现的日期。 |
| `resp.200.tasks.result.items.lost_date` | date and time when the last backlink to this page was lost indicates the date and time when our crawler visited the page and it responded with 4xx or 5xx status code or the last backlink was removed in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2017-01-24 13:20:59 +00:00 | Date the entity was detected as lost; null if it is still live. | 该实体被检测为丢失的日期；仍存活则为 null。 |
| `resp.200.tasks.result.items.backlinks_spam_score` | average spam score of the backlinks pointing to the page learn more about how the metric is calculated on this help center page | Aggregated spam score of the entity's backlink profile; higher values flag riskier link sources. | 该实体外链画像的聚合垃圾分，越高表示链接来源风险越大。 |
| `resp.200.tasks.result.items.broken_backlinks` | number of broken backlinks number of broken backlinks pointing to the page | Number of backlinks pointing to broken (non-resolving) pages of the entity. | 指向该实体失效（无法访问）页面的外链数量。 |
| `resp.200.tasks.result.items.broken_pages` | number of broken pages number of pages that respond with 4xx or 5xx status codes where backlinks are pointing to | Number of the entity's pages that are broken but still receive backlinks. | 该实体仍在接收外链但已失效的页面数量。 |
| `resp.200.tasks.result.items.referring_domains` | indicates the number domains referring to the page | Number of distinct domains linking to the entity. | 链向该实体的不同域名数量。 |
| `resp.200.tasks.result.items.referring_domains_nofollow` | number of domains pointing at least one nofollow link to the target | Number of referring domains whose links are all nofollow. | 链接全为 nofollow 的引荐域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains` | indicates the number of referring main domains | Number of distinct main (root) domains linking to the entity. | 链向该实体的不同主（根）域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains_nofollow` | number of main domains pointing at least one nofollow link to the target | Number of referring main domains whose links are all nofollow. | 链接全为 nofollow 的引荐主域名数量。 |
| `resp.200.tasks.result.items.referring_ips` | number of referring IP addresses number of IP addresses pointing to this page | Number of distinct IP addresses the backlinks originate from. | 外链来源的不同 IP 地址数量。 |
| `resp.200.tasks.result.items.referring_subnets` | number of referring subnetworks | Number of distinct subnets the backlinks originate from. | 外链来源的不同子网数量。 |
| `resp.200.tasks.result.items.referring_pages` | indicates the number of pages pointing to the relevant url | Number of distinct pages linking to the entity. | 链向该实体的不同页面数量。 |
| `resp.200.tasks.result.items.referring_pages_nofollow` | number of referring pages pointing at least one nofollow link to the target | Number of referring pages whose links are all nofollow. | 链接全为 nofollow 的引荐页面数量。 |
| `resp.200.tasks.result.items.referring_links_tld` | top-level domains of the referring links contains top level domains and referring link count per each | Breakdown of referring links by the top-level domain of their source. | 引荐链接按来源顶级域名（TLD）的分布。 |
| `resp.200.tasks.result.items.referring_links_types` | types of referring links indicates the types of the referring links and link count per each type possible values: anchor, image, link, meta, canonical, alternate, redirect | Breakdown of referring links by link type (e.g. anchor, image, redirect). | 引荐链接按链接类型（如锚文本、图片、重定向）的分布。 |
| `resp.200.tasks.result.items.referring_links_attributes` | link attributes of the referring links indicates link attributes of the referring links and link count per each attribute | Breakdown of referring links by their HTML attributes (e.g. nofollow, sponsored). | 引荐链接按其 HTML 属性（如 nofollow、sponsored）的分布。 |
| `resp.200.tasks.result.items.referring_links_platform_types` | types of referring platforms indicates referring platform types and and link count per each platform possible values: cms, blogs, ecommerce, message-boards, wikis, news, organization | Breakdown of referring links by the platform type of their source site. | 引荐链接按来源站点平台类型的分布。 |
| `resp.200.tasks.result.items.referring_links_semantic_locations` | semantic locations of the referring links indicates semantic elements in HTML where the referring links are located and link count per each semantic location you can get the full list of semantic elements here examples: article, section, footer | Breakdown of referring links by the semantic location on the source page (e.g. content, footer). | 引荐链接按其在来源页面上的语义位置（如正文、页脚）的分布。 |
| `resp.200.tasks.result.items.referring_links_countries` | ISO country codes of the referring links indicates ISO country codes of the domains where the referring links are located and the link count per each country | Breakdown of referring links by the country of their source. | 引荐链接按来源国家/地区的分布。 |

## post_dataforseo_backlinks_bulk_ranks_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with rank scores of the domains, subdomains, and pages specified in the `targets` array. The score is based on the number of referring domains pointing to the specified domains, subdomains, or pages. The `rank` values represent real-time data for the date of the request and range from 0 (no backlinks detected) to 1,000 (highest rank). A similar scoring system is used in Google’s Page Rank algorithm. You can learn more about rank scores in [this help center article](https://dataforseo.com/help-center/what_is_rank_in_backlinks_api) | Returns the backlink-based rank for up to 1000 targets in a single request, for bulk authority comparison. | 单次请求返回多达 1000 个目标基于外链的 rank，用于批量比较权重。 |
| `req.targets` | domains, subdomains or webpages to get rank for required field you can set up to 1000 domains, subdomains or webpages the domain or subdomain should be specified without https:// and www. the page should be specified with absolute URL (including http:// or https://) example: "targets": [ "forbes.com", "cnn.com", "bbc.com", "yelp.com", "https://www.apple.com/iphone/", "https://ahrefs.com/blog/", "ibm.com", "https://variety.com/", "https://stackoverflow.com/", "www.trustpilot.com" ] | Set of domains, subdomains or webpages to query in one bulk request, each formatted the same way as `target`. | 单次批量请求要查询的一组域名、子域名或网页，每项格式与 `target` 相同。 |
| `req.rank_scale` | defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on a 0–1000 scale default value: one_thousand learn more about how this parameter works and how ranking metrics are calculated in this Help Center article | Chooses the numeric scale for all rank-style metrics (`rank`, `domain_from_rank`, `page_from_rank`): `one_hundred` for a 0–100 scale or `one_thousand` for a 0–1000 scale. | 选择各类 rank 指标（`rank`、`domain_from_rank`、`page_from_rank`）的数值刻度：`one_hundred` 为 0–100，`one_thousand` 为 0–1000。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.items_count` | the number of results returned in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | contains relevant backlinks and referring domains data | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.target` | domain, subdomain or webpage from a POST array | The target this row reports on, echoing one of the requested `targets`. | 该行所统计的目标，回显请求 `targets` 中的某一项。 |
| `resp.200.tasks.result.items.rank` | rank of the target values represent real-time data for the date of the request rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity, on the chosen `rank_scale`; higher means stronger link authority. | 该实体基于外链的权重评分，按所选 `rank_scale` 计算，越高表示链接权重越强。 |

## post_dataforseo_backlinks_bulk_referring_domains_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with the number of referring domains pointing to domains, subdomains, and pages specified in the `targets` array. The returned numbers are based on all **live** referring domains, that is, total number of domains pointing to the target with any type of backlinks (e.g., nofollow, noreferrer, ugc, sponsored etc) that were found during the latest check. | Returns referring-domain and main-domain counts (total and nofollow) for up to 1000 targets at once. | 单次请求返回多达 1000 个目标的引荐域名与主域名数量（含 nofollow 计数）。 |
| `req.targets` | domains, subdomains or webpages to get the number of referring domains for required field you can set up to 1000 domains, subdomains or webpages the domain or subdomain should be specified without https:// and www. the page should be specified with absolute URL (including http:// or https://) example: "targets": [ "forbes.com", "cnn.com", "bbc.com", "yelp.com", "https://www.apple.com/iphone/", "https://ahrefs.com/blog/", "ibm.com", "https://variety.com/", "https://stackoverflow.com/", "www.trustpilot.com" ] | Set of domains, subdomains or webpages to query in one bulk request, each formatted the same way as `target`. | 单次批量请求要查询的一组域名、子域名或网页，每项格式与 `target` 相同。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.items_count` | the number of results returned in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | contains relevant backlinks and referring domains data | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.target` | domain, subdomain or webpage from a POST array | The target this row reports on, echoing one of the requested `targets`. | 该行所统计的目标，回显请求 `targets` 中的某一项。 |
| `resp.200.tasks.result.items.referring_domains` | number of referring domains pointing to the target note that we calculate main domains (root domains, like example.com) and their subdomains (e.g. blog.example.com) separately for this metric | Number of distinct domains linking to the entity. | 链向该实体的不同域名数量。 |
| `resp.200.tasks.result.items.referring_domains_nofollow` | number of domains pointing at least one nofollow link to the target | Number of referring domains whose links are all nofollow. | 链接全为 nofollow 的引荐域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains` | number of referring main domains pointing to the target the number of primary (root) domains referring to your target | Number of distinct main (root) domains linking to the entity. | 链向该实体的不同主（根）域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains_nofollow` | number of main domains pointing at least one nofollow link to the target | Number of referring main domains whose links are all nofollow. | 链接全为 nofollow 的引荐主域名数量。 |

## post_dataforseo_backlinks_bulk_spam_score_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with spam scores of the domains, subdomains, and pages you specified in the `targets` array. Spam Score is DataForSEO’s proprietary metric that indicates how “spammy” your target is on a scale from 0 to 100. You can learn more about Spam Score, how it is calculated, and signals it takes into account in [this help center article](https://dataforseo.com/help-center/what-is-spam-score-and-how-is-it-calculated) | Returns the backlink spam score for up to 1000 targets, for bulk link-quality screening. | 单次请求返回多达 1000 个目标的外链垃圾分，用于批量筛查链接质量。 |
| `req.targets` | domains, subdomains or webpages to get rank for required field you can set up to 1000 domains, subdomains or webpages the domain or subdomain should be specified without https:// and www. the page should be specified with absolute URL (including http:// or https://) example: "targets": [ "forbes.com", "cnn.com", "bbc.com", "yelp.com", "https://www.apple.com/iphone/", "https://ahrefs.com/blog/", "ibm.com", "https://variety.com/", "https://stackoverflow.com/", "www.trustpilot.com" ] | Set of domains, subdomains or webpages to query in one bulk request, each formatted the same way as `target`. | 单次批量请求要查询的一组域名、子域名或网页，每项格式与 `target` 相同。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.items_count` | the number of results returned in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | contains relevant backlinks and referring domains data | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.type` | type = ‘backlinks_bulk_spam_score’ | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.items.target` | domain, subdomain or webpage from a POST array | The target this row reports on, echoing one of the requested `targets`. | 该行所统计的目标，回显请求 `targets` 中的某一项。 |
| `resp.200.tasks.result.items.spam_score` | average spam score the target learn more about how the metric is calculated | Spam score of the target's backlink profile; higher values flag riskier link sources. | 目标外链画像的垃圾分，越高表示链接来源风险越大。 |

## post_dataforseo_backlinks_competitors_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with a list of competitors that share some part of the backlink profile with a target website, along with a number of backlink intersections and the rank of every competing website. | Returns domains that share referring domains with the target — i.e. likely backlink competitors — ranked by how many referrers they have in common. | 返回与目标共享引荐域名的域名（即潜在外链竞争对手），按共有引荐方数量排序。 |
| `req.target` | domain, subdomain or webpage to get competitor domains for required field a domain or a subdomain should be specified without https:// and www. a page should be specified with absolute URL (including http:// or https://) | Domain, subdomain or webpage whose backlink data you want. Specify a domain/subdomain without the `http(s)://` scheme and without `www.`; specify a page as an absolute URL including the scheme. | 要查询外链数据的域名、子域名或网页。域名/子域名填写时去掉 `http(s)://` 和 `www.`；网页则填写含协议的完整绝对 URL。 |
| `req.limit` | the maximum number of returned domains optional field default value: 100 maximum value: 1000 | Page size: how many items to return in this call. Combine with `offset` to page through the full result set. | 单页返回条数，配合 `offset` 翻页遍历完整结果集。 |
| `req.offset` | offset in the results array of returned domains optional field default value: 0 if you specify the 10 value, the first ten domains in the results array will be omitted and the data will be provided for the successive pages | Number of leading competitor domains to skip before returning results, for paging with `limit`. | 返回前跳过的竞争对手域名条数，与 `limit` 配合翻页。 |
| `req.filters` | array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, =, , in, not_in, like, not_like, ilike, not_ilike, match, not_match you can use the % operator with like and not_like to match any string of zero or more characters example: ["rank",">","100"] [["target","like","%forbes%"], "and", [["rank",">","100"],"or",["intersections",">","5"]]] The full list of possible filters is available here. | Conditions applied to the returned items, combined with `and`/`or` logical operators (up to 8). Reference any item field by name to narrow large result sets. | 对返回条目施加的筛选条件，用 `and`/`or` 组合（最多 8 条），可引用任意条目字段名以收窄大结果集。 |
| `req.order_by` | results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["rank,desc"] note that you can set no more than three sorting rules in a single request you should use a comma to separate several sorting rules example: ["intersections,desc","rank,asc"] | Sorting rules for the returned items; each rule pairs a field with `asc` or `desc`, up to three rules applied in order. | 返回条目的排序规则，每条规则指定字段加 `asc`/`desc`，最多三条按顺序生效。 |
| `req.main_domain` | indicates if only main domain of the target will be included in the search optional field if set to true, only the main domain will be included in search; default value: true | When true, only the main domain of the target is considered when searching for competitors. | 为 true 时仅以目标的主域名参与竞争对手检索。 |
| `req.exclude_large_domains` | indicates whether large domain will appear in results optional field if set to true, the results from the large domain (google.com, amazon.com, etc.) will be omitted; default value: true | When true, very large generic domains (e.g. google.com, amazon.com) are omitted from competitor results. | 为 true 时从竞争对手结果中剔除超大型通用域名（如 google.com、amazon.com）。 |
| `req.exclude_internal_backlinks` | indicates if internal backlinks from subdomains to the target will be excluded from the results optional field if set to true, the results will not include data on internal backlinks from subdomains of the same domain as target if set to false, internal links will be included in the results default value: true | When true, links coming from the target's own subdomains are dropped so the same domain is not reported as its own referrer; set false to keep such internal links. | 为 true 时剔除来自目标自身子域名的链接，避免目标域名成为自己的引荐方；设为 false 则保留这类内部链接。 |
| `req.rank_scale` | defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on a 0–1000 scale default value: one_thousand learn more about how this parameter works and how ranking metrics are calculated in this Help Center article | Chooses the numeric scale for all rank-style metrics (`rank`, `domain_from_rank`, `page_from_rank`): `one_hundred` for a 0–100 scale or `one_thousand` for a 0–1000 scale. | 选择各类 rank 指标（`rank`、`domain_from_rank`、`page_from_rank`）的数值刻度：`one_hundred` 为 0–100，`one_thousand` 为 0–1000。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.total_count` | total number of relevant items in the database | Total number of items matching the request, ignoring pagination. | 匹配请求的条目总数（不受分页影响）。 |
| `resp.200.tasks.result.items_count` | number of items in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | items array | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.type` | type of element = ‘backlinks_competitors’ | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.items.target` | competitor domain | The target this row reports on, echoing one of the requested `targets`. | 该行所统计的目标，回显请求 `targets` 中的某一项。 |
| `resp.200.tasks.result.items.rank` | domain rank domain rank across all domains in the database rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity, on the chosen `rank_scale`; higher means stronger link authority. | 该实体基于外链的权重评分，按所选 `rank_scale` 计算，越高表示链接权重越强。 |
| `resp.200.tasks.result.items.intersections` | indicates the number of backlink intersections with the target specified in the POST array | Number of referring domains this competitor shares with the target. | 该竞争对手与目标共有的引荐域名数量。 |

## post_dataforseo_backlinks_domain_intersection_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with the list of domains pointing to the specified websites. This endpoint is especially useful for creating a Link Gap feature that shows what domains link to your competitors but do not link out to your website. | Returns referring domains that link to several of the specified targets, with per-target metrics — useful for finding shared link sources across competitors. | 返回同时链向多个指定目标的引荐域名，并给出各目标的指标，便于发现竞争对手间共有的链接来源。 |
| `req.targets` | domains, subdomains or webpages to get links for required field you can set up to 20 domains, subdomains or webpages a domain or a subdomain should be specified without https:// and www. a page should be specified with absolute URL (including http:// or https://) example: "targets": { "1": "http://planet.postgresql.org/", "2": "http://gborg.postgresql.org/" } | Set of domains, subdomains or webpages to query in one bulk request, each formatted the same way as `target`. | 单次批量请求要查询的一组域名、子域名或网页，每项格式与 `target` 相同。 |
| `req.exclude_targets` | domains, subdomains or webpages you want to exclude optional field you can specify up to 10 domains, subdomains or webpages if you use this array, results will contain the referring domains that link to targets but don’t link to exclude_targets example: "exclude_targets": [ "bbc.com", "https://www.apple.com/iphone/*", "https://dataforseo.com/apis/*"] | Targets to exclude: results keep only referrers that link to `targets` but not to any entry here, useful for finding link gaps unique to your set. | 要排除的目标：结果只保留链向 `targets` 但不链向本列表任一项的引荐方，便于发现专属于目标集的链接缺口。 |
| `req.filters` | array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, =, , in, not_in, like, not_like, ilike, not_ilike, match, not_match you can use the % operator with like and not_like to match any string of zero or more characters example: ["1.internal_links_count",">","1"] [["2.referring_pages",">","2"], "and", ["1.backlinks",">","10"]] [["1.first_seen",">","2017-10-23 11:31:45 +00:00"], "and", [["2.target","like","%dataforseo.com%"],"or",["1.referring_domains",">","10"]]] The full list of possible filters is available here. | Conditions applied to the returned items, combined with `and`/`or` logical operators (up to 8). Reference any item field by name to narrow large result sets. | 对返回条目施加的筛选条件，用 `and`/`or` 组合（最多 8 条），可引用任意条目字段名以收窄大结果集。 |
| `req.order_by` | results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["backlinks,desc"] note that you can set no more than three sorting rules in a single request you should use a comma to separate several sorting rules example: ["backlinks,desc","rank,asc"] | Sorting rules for the returned items; each rule pairs a field with `asc` or `desc`, up to three rules applied in order. | 返回条目的排序规则，每条规则指定字段加 `asc`/`desc`，最多三条按顺序生效。 |
| `req.offset` | offset in the array of returned results optional field default value: 0 if you specify the 10 value, the first ten backlinks in the results array will be omitted and the data will be provided for the successive backlinks | Number of leading items to skip before returning results, for offset-based pagination together with `limit`. | 返回前跳过的结果条数，与 `limit` 配合实现偏移分页。 |
| `req.limit` | the maximum number of returned results optional field default value: 100 maximum value: 1000 | Page size: how many items to return in this call. Combine with `offset` to page through the full result set. | 单页返回条数，配合 `offset` 翻页遍历完整结果集。 |
| `req.internal_list_limit` | maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: referring_links_tld referring_links_types referring_links_attributes referring_links_platform_types referring_links_semantic_locations default value: 10 maximum value: 1000 | Caps how many elements are returned inside the nested aggregation arrays of each item (e.g. `referring_links_tld`, `referring_links_types`); lower it to keep responses small. | 限制每个条目内嵌聚合数组（如 `referring_links_tld`、`referring_links_types`）的元素数量，调小可精简响应体。 |
| `req.backlinks_status_type` | set what backlinks to return and count optional field you can use this field to choose what backlinks will be returned and used for aggregated metrics for your targets; possible values: all – all backlinks will be returned and counted; live – backlinks found during the last check will be returned and counted; lost – lost backlinks will be returned and counted; default value: live | Selects which backlinks feed both the returned list and the aggregated metrics: `all` counts every known link, `live` only links confirmed in the latest check, `lost` only links that have disappeared. | 决定纳入列表与聚合指标的外链范围：`all` 计入全部已知外链，`live` 仅最近一次检测仍存活的，`lost` 仅已消失的。 |
| `req.backlinks_filters` | filter the backlinks of your target optional field you can use this field to filter the initial backlinks that will be included in the dataset for aggregated metrics for your target you can filter the backlinks by all fields available in the response of this endpoint using this parameter, you can include only dofollow backlinks in the response and create a flexible backlinks dataset to calculate the metrics for example: "backlinks_filters": [["dofollow", "=", true]] | Pre-filters the raw backlinks that go into the aggregated metrics for the target, so metrics can be computed over a custom subset (e.g. dofollow-only). | 在计算目标聚合指标前先筛选原始外链，从而基于自定义子集（如仅 dofollow）计算指标。 |
| `req.include_subdomains` | indicates if the subdomains of the target will be included in the search optional field if set to false, the subdomains will be ignored default value: true | When true, subdomains of the target are included in the search; set false to restrict analysis to the target host only. | 为 true 时把目标的子域名一并纳入检索；设为 false 则只分析目标主机本身。 |
| `req.include_indirect_links` | indicates if indirect links to the targets will be included in the results optional field if set to true, the results will include data on indirect links pointing to a page that either redirects to a target, or points to a canonical page if set to false, indirect links will be ignored default value: true | When true, results also count indirect links — links to a page that redirects to the target or points to the target's canonical page. | 为 true 时一并统计间接链接，即指向「会重定向到目标」或「指向目标 canonical 页」的页面的链接。 |
| `req.exclude_internal_backlinks` | indicates whether the backlinks from subdomains of the target are excluded optional field if set to false, the backlinks from subdomains of the target will be omitted and you won’t receive the same domain in the response; default value: true | When true, links coming from the target's own subdomains are dropped so the same domain is not reported as its own referrer; set false to keep such internal links. | 为 true 时剔除来自目标自身子域名的链接，避免目标域名成为自己的引荐方；设为 false 则保留这类内部链接。 |
| `req.intersection_mode` | indicates whether to intersect backlinks optional field use this field to intersect or merge results for the specified domains possible values: all, partial all – results are based on all backlinks; partial – results are based on the intersecting backlinks only; default value: all | How results across the specified targets are combined: `all` bases results on all backlinks, `partial` only on backlinks shared across targets. | 多个目标结果的合并方式：`all` 基于全部外链，`partial` 仅基于目标间共有的外链。 |
| `req.rank_scale` | defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on a 0–1000 scale default value: one_thousand learn more about how this parameter works and how ranking metrics are calculated in this Help Center article | Chooses the numeric scale for all rank-style metrics (`rank`, `domain_from_rank`, `page_from_rank`): `one_hundred` for a 0–100 scale or `one_thousand` for a 0–1000 scale. | 选择各类 rank 指标（`rank`、`domain_from_rank`、`page_from_rank`）的数值刻度：`one_hundred` 为 0–100，`one_thousand` 为 0–1000。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.targets` | target domains, subdomains or webpages in a POST array | The set of targets this intersection result was computed across. | 该交集结果所基于的一组目标。 |
| `resp.200.tasks.result.total_count` | total amount of results relevant to your request | Total number of items matching the request, ignoring pagination. | 匹配请求的条目总数（不受分页影响）。 |
| `resp.200.tasks.result.items_count` | the number of results returned in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | contains domain that link to all targets from the POST array | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.domain_intersection` | contains data on domains that link to the corresponding targets specified in the POST array data is provided in separate objects corresponding to domains, subdomains or pages specified in the targets object | Per-target backlink metrics for this shared referring domain, keyed by target index. | 该共有引荐域名对各目标的外链指标，按目标序号索引。 |
| `resp.200.tasks.result.items.1` | contains data on a domain that links to the corresponding target from the POST array field name varies in the range from 1 to 20 according to the number of domains, subdomains, or pages in the targets object | Per-target block keyed by the target's index in the request (e.g. `1`, `2`). | 按目标在请求中的序号（如 `1`、`2`）索引的分目标数据块。 |
| `resp.200.tasks.result.items.1.type` | type of element = ‘backlinks_domain_intersection’ | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.items.1.target` | domain that links to the corresponding target from the POST array | The target this result block refers to, echoing the requested domain/subdomain/page. | 该结果块所对应的目标，回显请求的域名/子域名/页面。 |
| `resp.200.tasks.result.items.1.rank` | rank referred to the target from the POST array indicates the rank that the referring domain (target above) refers to your target from the POST array; rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity, on the chosen `rank_scale`; higher means stronger link authority. | 该实体基于外链的权重评分，按所选 `rank_scale` 计算，越高表示链接权重越强。 |
| `resp.200.tasks.result.items.1.backlinks` | indicates the number of backlinks | Number of backlinks pointing to the entity. | 指向该实体的外链数量。 |
| `resp.200.tasks.result.items.1.first_seen` | date and time when our crawler found the backlink from this target for the first time in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date the entity (link/anchor/page) was first discovered in the index. | 该实体（链接/锚文本/页面）首次被索引发现的日期。 |
| `resp.200.tasks.result.items.1.lost_date` | date and time when the last backlink from this target was lost indicates the date and time when our crawler visited the page and it responded with 4xx or 5xx status code or the last backlink was removed in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date the entity was detected as lost; null if it is still live. | 该实体被检测为丢失的日期；仍存活则为 null。 |
| `resp.200.tasks.result.items.1.backlinks_spam_score` | average spam score of the backlinks pointing to the target learn more about how the metric is calculated on this help center page | Aggregated spam score of the entity's backlink profile; higher values flag riskier link sources. | 该实体外链画像的聚合垃圾分，越高表示链接来源风险越大。 |
| `resp.200.tasks.result.items.1.broken_backlinks` | number of broken backlinks | Number of backlinks pointing to broken (non-resolving) pages of the entity. | 指向该实体失效（无法访问）页面的外链数量。 |
| `resp.200.tasks.result.items.1.broken_pages` | number of broken pages | Number of the entity's pages that are broken but still receive backlinks. | 该实体仍在接收外链但已失效的页面数量。 |
| `resp.200.tasks.result.items.1.referring_domains` | number of referring domains | Number of distinct domains linking to the entity. | 链向该实体的不同域名数量。 |
| `resp.200.tasks.result.items.1.referring_domains_nofollow` | number of domains pointing at least one nofollow link to the corresponding target | Number of referring domains whose links are all nofollow. | 链接全为 nofollow 的引荐域名数量。 |
| `resp.200.tasks.result.items.1.referring_main_domains` | number of referring main domains | Number of distinct main (root) domains linking to the entity. | 链向该实体的不同主（根）域名数量。 |
| `resp.200.tasks.result.items.1.referring_main_domains_nofollow` | number of main domains pointing at least one nofollow link to the target | Number of referring main domains whose links are all nofollow. | 链接全为 nofollow 的引荐主域名数量。 |
| `resp.200.tasks.result.items.1.referring_ips` | number of referring IP addresses | Number of distinct IP addresses the backlinks originate from. | 外链来源的不同 IP 地址数量。 |
| `resp.200.tasks.result.items.1.referring_subnets` | number of referring subnetworks | Number of distinct subnets the backlinks originate from. | 外链来源的不同子网数量。 |
| `resp.200.tasks.result.items.1.referring_pages` | indicates the number of pages pointing to the target | Number of distinct pages linking to the entity. | 链向该实体的不同页面数量。 |
| `resp.200.tasks.result.items.1.referring_links_tld` | top level domains of the referring links contains top-level domains and referring link count per each | Breakdown of referring links by the top-level domain of their source. | 引荐链接按来源顶级域名（TLD）的分布。 |
| `resp.200.tasks.result.items.1.referring_links_types` | types of the referring links indicates the types of referring links and link count per each type possible values: anchor, image, link, meta, canonical, alternate, redirect | Breakdown of referring links by link type (e.g. anchor, image, redirect). | 引荐链接按链接类型（如锚文本、图片、重定向）的分布。 |
| `resp.200.tasks.result.items.1.referring_links_attributes` | link attributes of the referring links indicates link attributes of the referring links and the link count per each attribute | Breakdown of referring links by their HTML attributes (e.g. nofollow, sponsored). | 引荐链接按其 HTML 属性（如 nofollow、sponsored）的分布。 |
| `resp.200.tasks.result.items.1.referring_links_platform_types` | types of referring platforms indicates referring platform types and link count per each platform possible values: cms, blogs, ecommerce, message-boards, wikis, news, organization | Breakdown of referring links by the platform type of their source site. | 引荐链接按来源站点平台类型的分布。 |
| `resp.200.tasks.result.items.1.referring_links_semantic_locations` | semantic locations of the referring links indicates semantic elements in HTML where the referring links are located and the link count per each semantic location you can get the full list of semantic elements here | Breakdown of referring links by the semantic location on the source page (e.g. content, footer). | 引荐链接按其在来源页面上的语义位置（如正文、页脚）的分布。 |
| `resp.200.tasks.result.items.1.referring_links_countries` | ISO country codes of the referring links indicates ISO country codes of the domains where the referring links are located and the link count per each country | Breakdown of referring links by the country of their source. | 引荐链接按来源国家/地区的分布。 |
| `resp.200.tasks.result.items.1.referring_pages_nofollow` | number of referring pages pointing at least one nofollow link to the target | Number of referring pages whose links are all nofollow. | 链接全为 nofollow 的引荐页面数量。 |
| `resp.200.tasks.result.items.summary` | contains the domain intersections summary | Roll-up of intersection statistics for this item. | 该条目交集统计的汇总。 |
| `resp.200.tasks.result.items.intersections_count` | total number of intersections | Number of targets this item intersects with. | 该条目所交集的目标数量。 |

## post_dataforseo_backlinks_domain_pages_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with a detailed overview of domain pages with backlink data for each page. | Returns the pages of a domain that have backlinks, each with page metadata and a per-page backlink summary — useful for finding the most-linked pages of a site. | 返回某域名下有外链的页面，每条附带页面元信息和该页的外链摘要，便于找出站点中被链接最多的页面。 |
| `req.target` | domain or subdomain required field a domain or a subdomain should be specified without https:// and www. example: forbes.com | Domain or subdomain whose indexed pages you want, specified without `http(s)://` and without `www.`. | 要查询已收录页面的域名或子域名，去掉 `http(s)://` 和 `www.` 填写。 |
| `req.limit` | the maximum number of returned pages optional field default value: 100 maximum value: 1000 | Page size: how many items to return in this call. Combine with `offset` to page through the full result set. | 单页返回条数，配合 `offset` 翻页遍历完整结果集。 |
| `req.offset` | offset in the results array of returned pages optional field default value: 0 if you specify the 10 value, the first ten pages in the results array will be omitted and the data will be provided for the successive pages | Number of leading items to skip before returning results, for offset-based pagination together with `limit`. | 返回前跳过的结果条数，与 `limit` 配合实现偏移分页。 |
| `req.internal_list_limit` | maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: referring_links_tld referring_links_types referring_links_attributes referring_links_platform_types referring_links_semantic_locations default value: 10 maximum value: 1000 | Caps how many elements are returned inside the nested aggregation arrays of each item (e.g. `referring_links_tld`, `referring_links_types`); lower it to keep responses small. | 限制每个条目内嵌聚合数组（如 `referring_links_tld`、`referring_links_types`）的元素数量，调小可精简响应体。 |
| `req.backlinks_status_type` | set what backlinks to return and count optional field you can use this field to choose what backlinks will be returned and used for aggregated metrics; possible values: all – all backlinks will be returned and counted; live – backlinks found during the last check will be returned and counted; lost – lost backlinks will be returned and counted; default value: live | Selects which backlinks feed both the returned list and the aggregated metrics: `all` counts every known link, `live` only links confirmed in the latest check, `lost` only links that have disappeared. | 决定纳入列表与聚合指标的外链范围：`all` 计入全部已知外链，`live` 仅最近一次检测仍存活的，`lost` 仅已消失的。 |
| `req.filters` | array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, =, , in, not_in, like, not_like, ilike, not_ilike, match, not_match you can use the % operator with like and not_like to match any string of zero or more characters example: ["meta.internal_links_count",">","1"] [["meta.external_links_count",">","2"], "and", ["backlinks",">","10"]] [["first_visited",">","2017-10-23 11:31:45 +00:00"], "and", [["title","like","%seo%"],"or",["referring_domains",">","10"]]] The full list of possible filters is available here. | Conditions applied to the returned items, combined with `and`/`or` logical operators (up to 8). Reference any item field by name to narrow large result sets. | 对返回条目施加的筛选条件，用 `and`/`or` 组合（最多 8 条），可引用任意条目字段名以收窄大结果集。 |
| `req.order_by` | results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["page_summary.backlinks,desc"] note that you can set no more than three sorting rules in a single request you should use a comma to separate several sorting rules example: ["page_summary.backlinks,desc","page_summary.rank,asc"] | Sorting rules for the returned items; each rule pairs a field with `asc` or `desc`, up to three rules applied in order. | 返回条目的排序规则，每条规则指定字段加 `asc`/`desc`，最多三条按顺序生效。 |
| `req.backlinks_filters` | filter the backlinks of your target optional field you can use this field to filter the initial backlinks that will be included in the dataset for aggregated metrics for your target you can filter the backlinks by all fields available in the response of this endpoint using this parameter, you can include only dofollow backlinks in the response and create a flexible backlinks dataset to calculate the metrics for example: "backlinks_filters": ["dofollow", "=", true] | Pre-filters the raw backlinks that go into the aggregated metrics for the target, so metrics can be computed over a custom subset (e.g. dofollow-only). | 在计算目标聚合指标前先筛选原始外链，从而基于自定义子集（如仅 dofollow）计算指标。 |
| `req.include_subdomains` | indicates if the subdomains of the target will be included in the search optional field if set to false, the subdomains will be ignored default value: true | When true, subdomains of the target are included in the search; set false to restrict analysis to the target host only. | 为 true 时把目标的子域名一并纳入检索；设为 false 则只分析目标主机本身。 |
| `req.exclude_internal_backlinks` | indicates if internal backlinks from subdomains to the target will be excluded from the results optional field if set to true, the results will not include data on internal backlinks from subdomains of the same domain as target if set to false, internal links will be included in the results default value: true | When true, links coming from the target's own subdomains are dropped so the same domain is not reported as its own referrer; set false to keep such internal links. | 为 true 时剔除来自目标自身子域名的链接，避免目标域名成为自己的引荐方；设为 false 则保留这类内部链接。 |
| `req.rank_scale` | defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on a 0–1000 scale default value: one_thousand learn more about how this parameter works and how ranking metrics are calculated in this Help Center article | Chooses the numeric scale for all rank-style metrics (`rank`, `domain_from_rank`, `page_from_rank`): `one_hundred` for a 0–100 scale or `one_thousand` for a 0–1000 scale. | 选择各类 rank 指标（`rank`、`domain_from_rank`、`page_from_rank`）的数值刻度：`one_hundred` 为 0–100，`one_thousand` 为 0–1000。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.target` | target in a POST array | The target this result block refers to, echoing the requested domain/subdomain/page. | 该结果块所对应的目标，回显请求的域名/子域名/页面。 |
| `resp.200.tasks.result.total_count` | total number of relevant items in the database | Total number of items matching the request, ignoring pagination. | 匹配请求的条目总数（不受分页影响）。 |
| `resp.200.tasks.result.items_count` | number of items in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | items array | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.type` | type of element = ‘backlinks_domain_page’ | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.items.main_domain` | main website domain main website domain does not include subdomains | Main (root) domain of the page. | 该页面的主（根）域名。 |
| `resp.200.tasks.result.items.domain` | domain domain where the page was found | The referring domain that links to the target. | 链向目标的引荐域名。 |
| `resp.200.tasks.result.items.tld` | top-level domain top-level domain in the DNS root zone | Top-level domain of the page. | 该页面的顶级域名（TLD）。 |
| `resp.200.tasks.result.items.page` | page URL relevant page URL | Path or URL of the page within the domain. | 该页面在域名内的路径或 URL。 |
| `resp.200.tasks.result.items.ip` | Internet Protocol address | IP address serving the page. | 承载该页面的 IP 地址。 |
| `resp.200.tasks.result.items.first_visited` | date and time of the first page visit date and time when our crawler visited this page for the first time in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2017-01-24 13:20:59 +00:00 | Date the page was first crawled. | 该页面首次被抓取的日期。 |
| `resp.200.tasks.result.items.prev_visited` | previous to the most recent date when our crawler visited the page in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2017-01-24 13:20:59 +00:00 | Date the page was crawled before the most recent visit. | 最近一次抓取之前该页面上一次被抓取的日期。 |
| `resp.200.tasks.result.items.fetch_time` | most recent date and time when our crawler visited the page in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2017-01-24 13:20:59 +00:00 | Timestamp of the most recent crawl of the page. | 该页面最近一次抓取的时间。 |
| `resp.200.tasks.result.items.status code` | HTTP status code of the page | HTTP status code returned by the page on the last crawl. | 最近一次抓取页面返回的 HTTP 状态码。 |
| `resp.200.tasks.result.items.location` | location header indicates the URL to redirect a page to if exists | Redirect Location header value when the page redirects. | 页面发生重定向时的 Location 头取值。 |
| `resp.200.tasks.result.items.size` | indicates the page size, in bytes | Size of the page in bytes. | 该页面的字节大小。 |
| `resp.200.tasks.result.items.encoded_size` | page size after encoding indicates the size of the encoded page, in bytes | Compressed (transfer) size of the page in bytes. | 该页面压缩（传输）后的字节大小。 |
| `resp.200.tasks.result.items.content_encoding` | type of encoding | Content-Encoding (compression) used to serve the page. | 页面传输所用的 Content-Encoding（压缩方式）。 |
| `resp.200.tasks.result.items.media_type` | types of media used to display a page | Media (MIME) type of the page. | 该页面的媒体（MIME）类型。 |
| `resp.200.tasks.result.items.server` | server version | Web server software reported for the host. | 主机上报的 Web 服务器软件。 |
| `resp.200.tasks.result.items.meta` | page meta data | On-page metadata extracted from the crawled page. | 从抓取页面提取的页面元信息。 |
| `resp.200.tasks.result.items.meta.title` | page title | Page title extracted from the crawled page. | 从抓取页面提取的标题。 |
| `resp.200.tasks.result.items.meta.canonical` | canonical page | Canonical URL declared by the page. | 该页面声明的 canonical URL。 |
| `resp.200.tasks.result.items.meta.internal_links_count` | number of internal links on the page | Number of internal links on the page. | 页面上的内部链接数量。 |
| `resp.200.tasks.result.items.meta.external_links_count` | number of external links on the page | Number of external links on the page. | 页面上的外部链接数量。 |
| `resp.200.tasks.result.items.meta.images_count` | number of images on the page | Number of images on the page. | 页面上的图片数量。 |
| `resp.200.tasks.result.items.meta.words_count` | number of words on the page | Number of words in the page content. | 页面正文的词数。 |
| `resp.200.tasks.result.items.meta.page_spam_score` | spam score of the page learn more about how the metric is calculated on this help center page | Spam score computed for this individual page. | 针对该单个页面计算的垃圾分。 |
| `resp.200.tasks.result.items.meta.social_media_tags` | array of social media tags found on the page contains social media tags and their content supported tags include but are not limited to Open Graph and Twitter card | Social media meta tags (e.g. Open Graph, Twitter Card) found on the page. | 页面上发现的社交媒体元标签（如 Open Graph、Twitter Card）。 |
| `resp.200.tasks.result.items.meta.h1` | h1 tag content of h1 tags | H1 headings extracted from the page. | 从页面提取的 H1 标题。 |
| `resp.200.tasks.result.items.meta.h2` | h2 tag content of h2 tags | H2 headings extracted from the page. | 从页面提取的 H2 标题。 |
| `resp.200.tasks.result.items.meta.h3` | h3 tag content of h3 tags | H3 headings extracted from the page. | 从页面提取的 H3 标题。 |
| `resp.200.tasks.result.items.meta.images_alt` | content of alt tags | Alt texts of images on the page. | 页面上图片的 alt 文本。 |
| `resp.200.tasks.result.items.meta.powered_by` | CMS details | Technologies the page reports being powered by. | 页面上报的支撑技术。 |
| `resp.200.tasks.result.items.meta.language` | page content language example: en | Detected language of the page. | 页面检测到的语言。 |
| `resp.200.tasks.result.items.meta.charset` | character encoding examples: utf-8 | Character set declared by the page. | 页面声明的字符集。 |
| `resp.200.tasks.result.items.meta.platform_type` | type of a platform | Platform type(s) detected for the page (e.g. cms, blogs). | 页面检测到的平台类型（如 cms、blogs）。 |
| `resp.200.tasks.result.items.meta.technologies` | website technologies | Detected technology stack of the page, grouped by category. | 页面检测到的技术栈，按类别分组。 |
| `resp.200.tasks.result.items.meta.technologies.cms` | content management system | Detected content management systems. | 检测到的内容管理系统（CMS）。 |
| `resp.200.tasks.result.items.meta.technologies.blogs` | blog management system | Detected blogging platforms. | 检测到的博客平台。 |
| `resp.200.tasks.result.items.meta.technologies.cdn` | content delivery network | Detected content delivery networks. | 检测到的内容分发网络（CDN）。 |
| `resp.200.tasks.result.items.page_summary` | contains backlink data for this page | Backlink summary metrics for this individual page. | 该单个页面的外链汇总指标。 |
| `resp.200.tasks.result.items.page_summary.first_seen` | date and time when our crawler found the backlink for the first time in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date the entity (link/anchor/page) was first discovered in the index. | 该实体（链接/锚文本/页面）首次被索引发现的日期。 |
| `resp.200.tasks.result.items.page_summary.lost_date` | date and time when the last backlink for this page was lost indicates the date and time when our crawler visited the page and it responded with 4xx or 5xx status code or the last backlink was removed in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2017-01-24 13:20:59 +00:00 | Date the entity was detected as lost; null if it is still live. | 该实体被检测为丢失的日期；仍存活则为 null。 |
| `resp.200.tasks.result.items.page_summary.rank` | page rank rank of the page rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity, on the chosen `rank_scale`; higher means stronger link authority. | 该实体基于外链的权重评分，按所选 `rank_scale` 计算，越高表示链接权重越强。 |
| `resp.200.tasks.result.items.page_summary.backlinks` | indicates the number of backlinks | Number of backlinks pointing to the entity. | 指向该实体的外链数量。 |
| `resp.200.tasks.result.items.page_summary.backlinks_spam_score` | average spam score of the backlinks pointing to the page learn more about how the metric is calculated on this help center page | Aggregated spam score of the entity's backlink profile; higher values flag riskier link sources. | 该实体外链画像的聚合垃圾分，越高表示链接来源风险越大。 |
| `resp.200.tasks.result.items.page_summary.broken_backlinks` | number of broken backlinks number of broken backlinks pointing to the page | Number of backlinks pointing to broken (non-resolving) pages of the entity. | 指向该实体失效（无法访问）页面的外链数量。 |
| `resp.200.tasks.result.items.page_summary.broken_pages` | number of broken pages number of pages that respond with 4xx or 5xx status codes where backlinks are pointing to | Number of the entity's pages that are broken but still receive backlinks. | 该实体仍在接收外链但已失效的页面数量。 |
| `resp.200.tasks.result.items.page_summary.referring_domains` | indicates the number of referring domains | Number of distinct domains linking to the entity. | 链向该实体的不同域名数量。 |
| `resp.200.tasks.result.items.page_summary.referring_domains_nofollow` | number of domains pointing at least one nofollow link to the page | Number of referring domains whose links are all nofollow. | 链接全为 nofollow 的引荐域名数量。 |
| `resp.200.tasks.result.items.page_summary.referring_main_domains` | indicates the number of referring main domains | Number of distinct main (root) domains linking to the entity. | 链向该实体的不同主（根）域名数量。 |
| `resp.200.tasks.result.items.page_summary.referring_main_domains_nofollow` | number of main domains pointing at least one nofollow link to the page | Number of referring main domains whose links are all nofollow. | 链接全为 nofollow 的引荐主域名数量。 |
| `resp.200.tasks.result.items.page_summary.referring ips` | number of referring IP addresses number of IP addresses pointing to this page | Number of distinct IP addresses the backlinks originate from. | 外链来源的不同 IP 地址数量。 |
| `resp.200.tasks.result.items.page_summary.referring subnets` | number of referring subnetworks | Number of distinct subnets the backlinks originate from. | 外链来源的不同子网数量。 |
| `resp.200.tasks.result.items.page_summary.referring_pages` | indicates the number of pages pointing to the page | Number of distinct pages linking to the entity. | 链向该实体的不同页面数量。 |
| `resp.200.tasks.result.items.page_summary.referring_links_tld` | top-level domains of the referring links contains top level domains and referring link count per each | Breakdown of referring links by the top-level domain of their source. | 引荐链接按来源顶级域名（TLD）的分布。 |
| `resp.200.tasks.result.items.page_summary.referring_links_types` | types of referring links indicates the types of the referring links and link count per each type possible values: anchor, image, link, meta, canonical, alternate, redirect | Breakdown of referring links by link type (e.g. anchor, image, redirect). | 引荐链接按链接类型（如锚文本、图片、重定向）的分布。 |
| `resp.200.tasks.result.items.page_summary.referring_links_attributes` | link attributes of the referring links indicates link attributes of the referring links and link count per each attribute | Breakdown of referring links by their HTML attributes (e.g. nofollow, sponsored). | 引荐链接按其 HTML 属性（如 nofollow、sponsored）的分布。 |
| `resp.200.tasks.result.items.page_summary.referring_links_platform_types` | types of referring platforms indicates referring platform types and and link count per each platform possible values: cms, blogs, ecommerce, message-boards, wikis, news, organization | Breakdown of referring links by the platform type of their source site. | 引荐链接按来源站点平台类型的分布。 |
| `resp.200.tasks.result.items.page_summary.referring_links_semantic_locations` | semantic locations of the referring links indicates semantic elements in HTML where the referring links are located and link count per each semantic location you can get the full list of semantic elements here examples: article, section, summary | Breakdown of referring links by the semantic location on the source page (e.g. content, footer). | 引荐链接按其在来源页面上的语义位置（如正文、页脚）的分布。 |
| `resp.200.tasks.result.items.page_summary.referring_links_countries` | ISO country codes of the referring links indicates ISO country codes of the domains where the referring links are located and the link count per each country | Breakdown of referring links by the country of their source. | 引荐链接按来源国家/地区的分布。 |
| `resp.200.tasks.result.items.page_summary.referring_pages_nofollow` | number of referring pages pointing at least one nofollow link to the page | Number of referring pages whose links are all nofollow. | 链接全为 nofollow 的引荐页面数量。 |

## post_dataforseo_backlinks_domain_pages_summary_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with detailed summary data on all backlinks and related metrics for each page of the target domain or subdomain you specify. If you indicate a single page as a target, you will get comprehensive summary data on all backlinks for that page. | Returns a per-page backlink summary (rank, link and referring counts) for the linked pages of a target domain. | 返回目标域名下被链接页面的逐页外链摘要（rank、外链与引荐计数）。 |
| `req.target` | domain, subdomain or webpage to get summary data for required field a domain or a subdomain should be specified without https:// and www. a page should be specified with absolute URL (including http:// or https://) | Domain, subdomain or webpage whose backlink data you want. Specify a domain/subdomain without the `http(s)://` scheme and without `www.`; specify a page as an absolute URL including the scheme. | 要查询外链数据的域名、子域名或网页。域名/子域名填写时去掉 `http(s)://` 和 `www.`；网页则填写含协议的完整绝对 URL。 |
| `req.limit` | the maximum number of returned anchors optional field default value: 100 maximum value: 1000 | Page size: how many items to return in this call. Combine with `offset` to page through the full result set. | 单页返回条数，配合 `offset` 翻页遍历完整结果集。 |
| `req.offset` | offset in the results array of returned anchors optional field default value: 0 if you specify the 10 value, the first ten anchors in the results array will be omitted and the data will be provided for the successive anchors | Number of leading items to skip before returning results, for offset-based pagination together with `limit`. | 返回前跳过的结果条数，与 `limit` 配合实现偏移分页。 |
| `req.internal_list_limit` | maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: referring_links_tld referring_links_types referring_links_attributes referring_links_platform_types referring_links_semantic_locations default value: 10 maximum value: 1000 | Caps how many elements are returned inside the nested aggregation arrays of each item (e.g. `referring_links_tld`, `referring_links_types`); lower it to keep responses small. | 限制每个条目内嵌聚合数组（如 `referring_links_tld`、`referring_links_types`）的元素数量，调小可精简响应体。 |
| `req.backlinks_status_type` | set what backlinks to return and count optional field you can use this field to choose what backlinks will be returned and used for aggregated metrics for your target; possible values: all – all backlinks will be returned and counted; live – backlinks found during the last check will be returned and counted; lost – lost backlinks will be returned and counted; default value: live | Selects which backlinks feed both the returned list and the aggregated metrics: `all` counts every known link, `live` only links confirmed in the latest check, `lost` only links that have disappeared. | 决定纳入列表与聚合指标的外链范围：`all` 计入全部已知外链，`live` 仅最近一次检测仍存活的，`lost` 仅已消失的。 |
| `req.filters` | array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, =, , in, not_in, like, not_like, ilike, not_ilike, match, not_match you can use the % operator with like and not_like to match any string of zero or more characters example: ["referring_links_types.anchors",">","1"] [["broken_pages",">","2"], "and", ["backlinks",">","10"]] [["first_seen",">","2017-10-23 11:31:45 +00:00"], "and", [["anchor","like","%seo%"],"or",["referring_domains",">","10"]]] The full list of possible filters is available here. | Conditions applied to the returned items, combined with `and`/`or` logical operators (up to 8). Reference any item field by name to narrow large result sets. | 对返回条目施加的筛选条件，用 `and`/`or` 组合（最多 8 条），可引用任意条目字段名以收窄大结果集。 |
| `req.order_by` | results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["backlinks,desc"] note that you can set no more than three sorting rules in a single request you should use a comma to separate several sorting rules example: ["backlinks,desc","rank,asc"] | Sorting rules for the returned items; each rule pairs a field with `asc` or `desc`, up to three rules applied in order. | 返回条目的排序规则，每条规则指定字段加 `asc`/`desc`，最多三条按顺序生效。 |
| `req.backlinks_filters` | filter the backlinks of your target optional field you can use this field to filter the initial backlinks that will be included in the dataset for aggregated metrics for your target you can filter the backlinks by all fields available in the response of this endpoint using this parameter, you can include only dofollow backlinks in the response and create a flexible backlinks dataset to calculate the metrics for example: "backlinks_filters": [["dofollow", "=", true]] | Pre-filters the raw backlinks that go into the aggregated metrics for the target, so metrics can be computed over a custom subset (e.g. dofollow-only). | 在计算目标聚合指标前先筛选原始外链，从而基于自定义子集（如仅 dofollow）计算指标。 |
| `req.include_subdomains` | indicates if the subdomains of the target domain will be included in the search optional field if set to false, the subdomains will be ignored default value: true | When true, subdomains of the target are included in the search; set false to restrict analysis to the target host only. | 为 true 时把目标的子域名一并纳入检索；设为 false 则只分析目标主机本身。 |
| `req.include_indirect_links` | indicates if indirect links to the target will be included in the results optional field if set to true, the results will include data on indirect links pointing to a page that either redirects to the target, or points to a canonical page if set to false, indirect links will be ignored default value: true | When true, results also count indirect links — links to a page that redirects to the target or points to the target's canonical page. | 为 true 时一并统计间接链接，即指向「会重定向到目标」或「指向目标 canonical 页」的页面的链接。 |
| `req.exclude_internal_backlinks` | indicates whether the backlinks from subdomains of the target are excluded optional field if set to false, backlinks from the subdomains of the target domain will be ommited and you won’t receive the same domain in the response; default value: true | When true, links coming from the target's own subdomains are dropped so the same domain is not reported as its own referrer; set false to keep such internal links. | 为 true 时剔除来自目标自身子域名的链接，避免目标域名成为自己的引荐方；设为 false 则保留这类内部链接。 |
| `req.rank_scale` | defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on a 0–1000 scale default value: one_thousand learn more about how this parameter works and how ranking metrics are calculated in this Help Center article | Chooses the numeric scale for all rank-style metrics (`rank`, `domain_from_rank`, `page_from_rank`): `one_hundred` for a 0–100 scale or `one_thousand` for a 0–1000 scale. | 选择各类 rank 指标（`rank`、`domain_from_rank`、`page_from_rank`）的数值刻度：`one_hundred` 为 0–100，`one_thousand` 为 0–1000。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.target` | target in the post array | The target this result block refers to, echoing the requested domain/subdomain/page. | 该结果块所对应的目标，回显请求的域名/子域名/页面。 |
| `resp.200.tasks.result.total_count` | total number of relevant items in the database | Total number of items matching the request, ignoring pagination. | 匹配请求的条目总数（不受分页影响）。 |
| `resp.200.tasks.result.items_count` | number of items in the results array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | items array | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.type` | type of element = ‘backlinks_page_summary’ | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.items.url` | page URL | URL of the page this item describes. | 该条目所描述页面的 URL。 |
| `resp.200.tasks.result.items.rank` | page rank rank of the page rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity, on the chosen `rank_scale`; higher means stronger link authority. | 该实体基于外链的权重评分，按所选 `rank_scale` 计算，越高表示链接权重越强。 |
| `resp.200.tasks.result.items.backlinks` | number of backlinks | Number of backlinks pointing to the entity. | 指向该实体的外链数量。 |
| `resp.200.tasks.result.items.first_seen` | date and time when our crawler found a backlink to this page for the first time in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date the entity (link/anchor/page) was first discovered in the index. | 该实体（链接/锚文本/页面）首次被索引发现的日期。 |
| `resp.200.tasks.result.items.lost_date` | date and time when the last backlink to this page was lost indicates the date and time when our crawler visited the page and it responded with 4xx or 5xx status code or the last backlink was removed in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2017-01-24 13:20:59 +00:00 | Date the entity was detected as lost; null if it is still live. | 该实体被检测为丢失的日期；仍存活则为 null。 |
| `resp.200.tasks.result.items.backlinks_spam_score` | average spam score of the backlinks pointing to the page learn more about how the metric is calculated on this help center page | Aggregated spam score of the entity's backlink profile; higher values flag riskier link sources. | 该实体外链画像的聚合垃圾分，越高表示链接来源风险越大。 |
| `resp.200.tasks.result.items.broken_backlinks` | number of broken backlinks number of broken backlinks pointing to the page | Number of backlinks pointing to broken (non-resolving) pages of the entity. | 指向该实体失效（无法访问）页面的外链数量。 |
| `resp.200.tasks.result.items.broken_pages` | number of broken pages number of pages that respond with 4xx or 5xx status codes where backlinks are pointing to | Number of the entity's pages that are broken but still receive backlinks. | 该实体仍在接收外链但已失效的页面数量。 |
| `resp.200.tasks.result.items.referring_domains` | indicates the number domains referring to the page | Number of distinct domains linking to the entity. | 链向该实体的不同域名数量。 |
| `resp.200.tasks.result.items.referring_domains_nofollow` | number of domains pointing at least one nofollow link to the page | Number of referring domains whose links are all nofollow. | 链接全为 nofollow 的引荐域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains` | indicates the number of referring main domains | Number of distinct main (root) domains linking to the entity. | 链向该实体的不同主（根）域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains_nofollow` | number of main domains pointing at least one nofollow link to the page | Number of referring main domains whose links are all nofollow. | 链接全为 nofollow 的引荐主域名数量。 |
| `resp.200.tasks.result.items.referring_ips` | number of referring IP addresses number of IP addresses pointing to this page | Number of distinct IP addresses the backlinks originate from. | 外链来源的不同 IP 地址数量。 |
| `resp.200.tasks.result.items.referring_subnets` | number of referring subnetworks | Number of distinct subnets the backlinks originate from. | 外链来源的不同子网数量。 |
| `resp.200.tasks.result.items.referring_pages` | indicates the number of pages pointing to the relevant url | Number of distinct pages linking to the entity. | 链向该实体的不同页面数量。 |
| `resp.200.tasks.result.items.referring_links_tld` | top-level domains of the referring links contains top level domains and referring link count per each | Breakdown of referring links by the top-level domain of their source. | 引荐链接按来源顶级域名（TLD）的分布。 |
| `resp.200.tasks.result.items.referring_links_types` | types of referring links indicates the types of the referring links and link count per each type possible values: anchor, image, link, meta, canonical, alternate, redirect | Breakdown of referring links by link type (e.g. anchor, image, redirect). | 引荐链接按链接类型（如锚文本、图片、重定向）的分布。 |
| `resp.200.tasks.result.items.referring_links_attributes` | link attributes of the referring links indicates link attributes of the referring links and link count per each attribute | Breakdown of referring links by their HTML attributes (e.g. nofollow, sponsored). | 引荐链接按其 HTML 属性（如 nofollow、sponsored）的分布。 |
| `resp.200.tasks.result.items.referring_links_platform_types` | types of referring platforms indicates referring platform types and and link count per each platform possible values: cms, blogs, ecommerce, message-boards, wikis, news, organization | Breakdown of referring links by the platform type of their source site. | 引荐链接按来源站点平台类型的分布。 |
| `resp.200.tasks.result.items.referring_links_semantic_locations` | semantic locations of the referring links indicates semantic elements in HTML where the referring links are located and link count per each semantic location you can get the full list of semantic elements here examples: article, section, footer | Breakdown of referring links by the semantic location on the source page (e.g. content, footer). | 引荐链接按其在来源页面上的语义位置（如正文、页脚）的分布。 |
| `resp.200.tasks.result.items.referring_links_countries` | ISO country codes of the referring links indicates ISO country codes of the domains where the referring links are located and the link count per each country | Breakdown of referring links by the country of their source. | 引荐链接按来源国家/地区的分布。 |
| `resp.200.tasks.result.items.referring_pages_nofollow` | number of referring pages pointing at least one nofollow link to the page | Number of referring pages whose links are all nofollow. | 链接全为 nofollow 的引荐页面数量。 |

## post_dataforseo_backlinks_history_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with historical backlinks data back to the beginning of 2019. You can receive the number of backlinks a given domain had in a specific time period, the number of new & lost backlinks, referring domains, and more. | Returns a time series of a domain's overall backlink metrics within a date range, for tracking how its link profile evolved. | 返回某域名在指定日期范围内整体外链指标的时间序列，用于跟踪其链接画像的演变。 |
| `req.target` | domain required field a domain should be specified without https:// and www. | Domain whose historical backlink summary you want. Specify it without `http(s)://` and without `www.` (this endpoint accepts a domain only). | 要查询历史外链汇总的域名。去掉 `http(s)://` 和 `www.` 填写（本接口仅接受域名）。 |
| `req.date_from` | starting date of the time range optional field minimum value 2019-01-01 if you don’t specify this field, the minimum value will be used by default date format: "yyyy-mm-dd" example: "2019-01-15" | Start of the time range (format `yyyy-mm-dd`); if omitted, the endpoint's minimum supported date is used. | 时间范围起始日期（格式 `yyyy-mm-dd`）；不填则使用该接口支持的最小日期。 |
| `req.date_to` | ending date of the time range optional field if you don’t specify this field, the today’s date will be used by default date format: "yyyy-mm-dd" example: "2019-01-15" | End of the time range (format `yyyy-mm-dd`); if omitted, today's date is used. | 时间范围结束日期（格式 `yyyy-mm-dd`）；不填则默认到今天。 |
| `req.rank_scale` | defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on a 0–1000 scale default value: one_thousand learn more about how this parameter works and how ranking metrics are calculated in this Help Center article | Chooses the numeric scale for all rank-style metrics (`rank`, `domain_from_rank`, `page_from_rank`): `one_hundred` for a 0–100 scale or `one_thousand` for a 0–1000 scale. | 选择各类 rank 指标（`rank`、`domain_from_rank`、`page_from_rank`）的数值刻度：`one_hundred` 为 0–100，`one_thousand` 为 0–1000。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.target` | target from the POST array | The target this result block refers to, echoing the requested domain/subdomain/page. | 该结果块所对应的目标，回显请求的域名/子域名/页面。 |
| `resp.200.tasks.result.date_from` | starting date of the time range in the UTC format: “yyyy-mm-dd” example: 2019-01-01 | Start date of the time range covered by this result. | 该结果覆盖时间范围的起始日期。 |
| `resp.200.tasks.result.date_to` | ending date of the time range in the UTC format: "yyyy-mm-dd" example: "2019-01-15" | End date of the time range covered by this result. | 该结果覆盖时间范围的结束日期。 |
| `resp.200.tasks.result.items_count` | the number of results returned in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | contains historical backlink data for the specified domain the data is provided month-by-month; the metrics are aggregated according to the backlinks the specified domain had on the first day of each given month | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.type` | type of element = ‘backlinks_history’ | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.items.date` | date and time when the data for the target was stored in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date this timeseries data point corresponds to. | 该时间序列数据点对应的日期。 |
| `resp.200.tasks.result.items.rank` | domain rank on the given date learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity, on the chosen `rank_scale`; higher means stronger link authority. | 该实体基于外链的权重评分，按所选 `rank_scale` 计算，越高表示链接权重越强。 |
| `resp.200.tasks.result.items.backlinks` | number of backlinks | Number of backlinks pointing to the entity. | 指向该实体的外链数量。 |
| `resp.200.tasks.result.items.new_backlinks` | number of new backlinks for the target data is provided based in a comparison with the previous period Note: this data is available from May 2021; if the date range specified in the POST request precedes May 2021, the field will equal 0 | Number of backlinks newly gained since `date_from`. | 自 `date_from` 起新增的外链数量。 |
| `resp.200.tasks.result.items.lost_backlinks` | number of lost backlinks for the target data is provided based in a comparison with the previous period Note: this data is available from May 2021; if the date range specified in the POST request precedes May 2021, the field will equal 0 | Number of backlinks lost since `date_from`. | 自 `date_from` 起丢失的外链数量。 |
| `resp.200.tasks.result.items.new_referring_domains` | number of new referring domains for the target data is provided based in a comparison with the previous period Note: this data is available from May 2021; if the date range specified in the POST request precedes May 2021, the field will equal 0 | Number of referring domains newly gained since `date_from`. | 自 `date_from` 起新增的引荐域名数量。 |
| `resp.200.tasks.result.items.lost_referring_domains` | number of lost referring domains for the target data is provided based in a comparison with the previous period Note: this data is available from May 2021; if the date range specified in the POST request precedes May 2021, the field will equal 0 | Number of referring domains lost since `date_from`. | 自 `date_from` 起丢失的引荐域名数量。 |
| `resp.200.tasks.result.items.crawled_pages` | number of crawled pages for the target | Number of the target's pages crawled by DataForSEO. | DataForSEO 已抓取的目标页面数量。 |
| `resp.200.tasks.result.items.info` | information about the target | Host and technology information about the target. | 目标的主机与技术信息。 |
| `resp.200.tasks.result.items.info.server` | server | Web server software reported for the host. | 主机上报的 Web 服务器软件。 |
| `resp.200.tasks.result.items.info.cms` | content management system | Detected content management systems. | 检测到的内容管理系统（CMS）。 |
| `resp.200.tasks.result.items.info.platform_type` | platform type | Platform type(s) detected for the page (e.g. cms, blogs). | 页面检测到的平台类型（如 cms、blogs）。 |
| `resp.200.tasks.result.items.info.ip_address` | IP address of the target | IP address of the target host. | 目标主机的 IP 地址。 |
| `resp.200.tasks.result.items.info.country` | country code that the target domain is determined to belong to | Country associated with the target host. | 目标主机关联的国家/地区。 |
| `resp.200.tasks.result.items.info.is_ip` | indicates if the target is IP if true, the domain, subdomain or webpage functions as an IP address and does not have a domain name | Whether the target is addressed by a bare IP rather than a domain name. | 目标是否以裸 IP（而非域名）寻址。 |
| `resp.200.tasks.result.items.info.target_spam_score` | spam score of the target if the target is a domain/subdomain, this fields indicates the average spam score of all pages of that domain/subdomain; learn more about how the metric is calculated on this help center page | Spam score computed for the target host. | 针对目标主机计算的垃圾分。 |
| `resp.200.tasks.result.items.internal_links_count` | number of internal links calculated as the sum of internal links on the pages of the specified target | Number of internal links on the page. | 页面上的内部链接数量。 |
| `resp.200.tasks.result.items.external_links_count` | number of external links on the page calculated as the sum of external links on the pages of the specified target | Number of external links on the page. | 页面上的外部链接数量。 |
| `resp.200.tasks.result.items.broken_backlinks` | number of broken backlinks number of broken backlinks pointing to the target | Number of backlinks pointing to broken (non-resolving) pages of the entity. | 指向该实体失效（无法访问）页面的外链数量。 |
| `resp.200.tasks.result.items.broken_pages` | number of broken pages number of pages that receive backlinks but respond with 4xx or 5xx status codes | Number of the entity's pages that are broken but still receive backlinks. | 该实体仍在接收外链但已失效的页面数量。 |
| `resp.200.tasks.result.items.referring_domains` | number of referring domains referring domains include subdomains that are counted as separate domains for this metric | Number of distinct domains linking to the entity. | 链向该实体的不同域名数量。 |
| `resp.200.tasks.result.items.referring_domains_nofollow` | number of domains pointing at least one nofollow link to the target | Number of referring domains whose links are all nofollow. | 链接全为 nofollow 的引荐域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains` | number of referring main domains | Number of distinct main (root) domains linking to the entity. | 链向该实体的不同主（根）域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains_nofollow` | number of main domains pointing at least one nofollow link to the target | Number of referring main domains whose links are all nofollow. | 链接全为 nofollow 的引荐主域名数量。 |
| `resp.200.tasks.result.items.referring_ips` | number of referring IP addresses number of IP addresses pointing to this page | Number of distinct IP addresses the backlinks originate from. | 外链来源的不同 IP 地址数量。 |
| `resp.200.tasks.result.items.referring_subnets` | number of referring subnetworks | Number of distinct subnets the backlinks originate from. | 外链来源的不同子网数量。 |
| `resp.200.tasks.result.items.referring_pages` | number of pages pointing to the target | Number of distinct pages linking to the entity. | 链向该实体的不同页面数量。 |
| `resp.200.tasks.result.items.referring_links_tld` | top-level domains of the referring links contains top-level domains and referring link count per each | Breakdown of referring links by the top-level domain of their source. | 引荐链接按来源顶级域名（TLD）的分布。 |
| `resp.200.tasks.result.items.referring_links_types` | types of referring links indicates the types of the referring links and link count per each type possible values: anchor, image, link, meta, canonical, alternate, redirect | Breakdown of referring links by link type (e.g. anchor, image, redirect). | 引荐链接按链接类型（如锚文本、图片、重定向）的分布。 |
| `resp.200.tasks.result.items.referring_links_attributes` | link attributes of the referring links indicates link attributes of the referring links and link count per each attribute | Breakdown of referring links by their HTML attributes (e.g. nofollow, sponsored). | 引荐链接按其 HTML 属性（如 nofollow、sponsored）的分布。 |
| `resp.200.tasks.result.items.referring_links_platform_types` | types of referring platforms indicates referring platform types and and link count per each platform possible values: cms, blogs, ecommerce, message-boards, wikis, news, organization | Breakdown of referring links by the platform type of their source site. | 引荐链接按来源站点平台类型的分布。 |
| `resp.200.tasks.result.items.referring_links_semantic_locations` | semantic locations of the referring links indicates semantic elements in HTML where the referring links are located and link count per each semantic location you can get the full list of semantic elements here examples: article, section, summary | Breakdown of referring links by the semantic location on the source page (e.g. content, footer). | 引荐链接按其在来源页面上的语义位置（如正文、页脚）的分布。 |
| `resp.200.tasks.result.items.referring_links_countries` | ISO country codes of the referring links indicates ISO country codes of the domains where the referring links are located and the link count per each country | Breakdown of referring links by the country of their source. | 引荐链接按来源国家/地区的分布。 |
| `resp.200.tasks.result.items.referring_pages_nofollow` | number of referring pages pointing at least one nofollow link to the target | Number of referring pages whose links are all nofollow. | 链接全为 nofollow 的引荐页面数量。 |

## get_dataforseo_backlinks_index

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with the total number of backlinks, domains, and pages our database contains for the moment when you make a request. You will also get stats for the last 12 months. | Returns the current size of the DataForSEO backlink index (total backlinks, pages, domains) plus its growth history. Takes no parameters. | 返回 DataForSEO 外链索引的当前规模（外链、页面、域名总数）及其增长历史，无需任何参数。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the parameters passed in the URL of the GET request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.total_backlinks` | total number of backlinks our database contains for the moment of checking | Total number of backlinks currently held in the index. | 索引当前收录的外链总数。 |
| `resp.200.tasks.result.total_pages` | total number of pages our database contains for the moment of checking | Total number of pages currently held in the index. | 索引当前收录的页面总数。 |
| `resp.200.tasks.result.total_domains` | total number of domains our database contains for the moment of checking | Total number of domains currently held in the index. | 索引当前收录的域名总数。 |
| `resp.200.tasks.result.index_history` | index volume data for the past 12 months | Historical snapshots of the index size over time. | 索引规模随时间变化的历史快照。 |
| `resp.200.tasks.result.index_history.date` | date for which index volume data is provided in the UTC format: “yyyy-mm-dd” example: 2021-10-01 | Date this timeseries data point corresponds to. | 该时间序列数据点对应的日期。 |
| `resp.200.tasks.result.index_history.total_backlinks` | total number of backlinks our database contained on the given date | Total number of backlinks currently held in the index. | 索引当前收录的外链总数。 |
| `resp.200.tasks.result.index_history.total_pages` | total number of pages our database contained on the given date | Total number of pages currently held in the index. | 索引当前收录的页面总数。 |
| `resp.200.tasks.result.index_history.total_domains` | total number of domains our database contained on the given date | Total number of domains currently held in the index. | 索引当前收录的域名总数。 |

## post_dataforseo_backlinks_page_intersection_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with the list of referring pages pointing to the specified targets. It is especially useful for finding the backlinks that point to your competitors but don’t point to your website. | Returns backlinks whose source pages link to several of the specified targets, with full per-link detail — for finding pages that link to multiple competitors at once. | 返回来源页同时链向多个指定目标的外链，并给出逐条明细，用于找出同时链向多个竞品的页面。 |
| `req.targets` | domains, subdomains or webpages to get links for required field you can set up to 20 domains, subdomains or webpages a domain or a subdomain should be specified without https:// and www. a page should be specified with absolute URL (including http:// or https://) example: "targets": { "1": "http://planet.postgresql.org/", "2": "http://gborg.postgresql.org/" } | Set of domains, subdomains or webpages to query in one bulk request, each formatted the same way as `target`. | 单次批量请求要查询的一组域名、子域名或网页，每项格式与 `target` 相同。 |
| `req.exclude_targets` | domains, subdomains or webpages you want to exclude optional field you can set up to 10 domains, subdomains or webpages if you use this array, results will contain the referring pages that link to targets but don’t link to exclude_targets example: "exclude_targets": [ "bbc.com", "https://www.apple.com/iphone/*", "https://dataforseo.com/apis/*"] | Targets to exclude: results keep only referrers that link to `targets` but not to any entry here, useful for finding link gaps unique to your set. | 要排除的目标：结果只保留链向 `targets` 但不链向本列表任一项的引荐方，便于发现专属于目标集的链接缺口。 |
| `req.backlinks_status_type` | set what backlinks to return and count optional field you can use this field to choose what backlinks will be returned and used for aggregated metrics for your targets; possible values: all – all backlinks will be returned and counted; live – backlinks found during the last check will be returned and counted; lost – lost backlinks will be returned and counted; default value: live | Selects which backlinks feed both the returned list and the aggregated metrics: `all` counts every known link, `live` only links confirmed in the latest check, `lost` only links that have disappeared. | 决定纳入列表与聚合指标的外链范围：`all` 计入全部已知外链，`live` 仅最近一次检测仍存活的，`lost` 仅已消失的。 |
| `req.filters` | array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, =, , in, not_in, like, not_like, ilike, not_ilike, match, not_match you can use the % operator with like and not_like to match any string of zero or more characters example: ["1.rank",">","80"] [["2.page_from_rank",">","55"], "and", ["1.original","=","true"]] [["1.first_seen",">","2017-10-23 11:31:45 +00:00"], "and", [["1.acnhor","like","%seo%"],"or",["1.text_pre","not_like","%seo%"]]] The full list of possible filters is available here. | Conditions applied to the returned items, combined with `and`/`or` logical operators (up to 8). Reference any item field by name to narrow large result sets. | 对返回条目施加的筛选条件，用 `and`/`or` 组合（最多 8 条），可引用任意条目字段名以收窄大结果集。 |
| `req.order_by` | results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["rank,desc"] note that you can set no more than three sorting rules in a single request you should use a comma to separate several sorting rules example: ["domain_from_rank,desc","page_from_rank,asc"] | Sorting rules for the returned items; each rule pairs a field with `asc` or `desc`, up to three rules applied in order. | 返回条目的排序规则，每条规则指定字段加 `asc`/`desc`，最多三条按顺序生效。 |
| `req.offset` | offset in the results array of the returned backlinks optional field default value: 0 if you specify the 10 value, the first ten backlinks in the results array will be omitted and the data will be provided for the successive backlinks | Number of leading items to skip before returning results, for offset-based pagination together with `limit`. | 返回前跳过的结果条数，与 `limit` 配合实现偏移分页。 |
| `req.limit` | the maximum number of returned backlinks optional field default value: 100 maximum value: 1000 | Page size: how many items to return in this call. Combine with `offset` to page through the full result set. | 单页返回条数，配合 `offset` 翻页遍历完整结果集。 |
| `req.internal_list_limit` | maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: attributes domain_from_platform_type default value: 10 maximum value: 1000 | Caps how many elements are returned inside the nested aggregation arrays of each item (e.g. `referring_links_tld`, `referring_links_types`); lower it to keep responses small. | 限制每个条目内嵌聚合数组（如 `referring_links_tld`、`referring_links_types`）的元素数量，调小可精简响应体。 |
| `req.include_subdomains` | indicates if the subdomains of the targets will be included in the search optional field if set to false, the subdomains will be ignored default value: true | When true, subdomains of the target are included in the search; set false to restrict analysis to the target host only. | 为 true 时把目标的子域名一并纳入检索；设为 false 则只分析目标主机本身。 |
| `req.include_indirect_links` | indicates if indirect links to the targets will be included in the results optional field if set to true, the results will include data on indirect links pointing to a page that either redirects to a target, or points to a canonical page if set to false, indirect links will be ignored default value: true | When true, results also count indirect links — links to a page that redirects to the target or points to the target's canonical page. | 为 true 时一并统计间接链接，即指向「会重定向到目标」或「指向目标 canonical 页」的页面的链接。 |
| `req.exclude_internal_backlinks` | indicates if internal backlinks from subdomains to the target will be excluded from the results optional field if set to true, the results will not include data on internal backlinks from subdomains of the same domain as target if set to false, internal links will be included in the result default value: true | When true, links coming from the target's own subdomains are dropped so the same domain is not reported as its own referrer; set false to keep such internal links. | 为 true 时剔除来自目标自身子域名的链接，避免目标域名成为自己的引荐方；设为 false 则保留这类内部链接。 |
| `req.intersection_mode` | indicates whether to intersect backlinks optional field use this field to intersect or merge results for the specified URLs possible values: all, partial all – results are based on all backlinks; partial – results are based on the intersecting backlinks only; default value: all | How results across the specified targets are combined: `all` bases results on all backlinks, `partial` only on backlinks shared across targets. | 多个目标结果的合并方式：`all` 基于全部外链，`partial` 仅基于目标间共有的外链。 |
| `req.rank_scale` | defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on a 0–1000 scale default value: one_thousand learn more about how this parameter works and how ranking metrics are calculated in this Help Center article | Chooses the numeric scale for all rank-style metrics (`rank`, `domain_from_rank`, `page_from_rank`): `one_hundred` for a 0–100 scale or `one_thousand` for a 0–1000 scale. | 选择各类 rank 指标（`rank`、`domain_from_rank`、`page_from_rank`）的数值刻度：`one_hundred` 为 0–100，`one_thousand` 为 0–1000。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.targets` | targets from a POST array | The set of targets this intersection result was computed across. | 该交集结果所基于的一组目标。 |
| `resp.200.tasks.result.total_count` | total amount of results relevant the request | Total number of items matching the request, ignoring pagination. | 匹配请求的条目总数（不受分页影响）。 |
| `resp.200.tasks.result.items_count` | the number of results returned in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | contains relevant backlinks and referring domains data | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.page_intersection` | contains data on pages that link to the corresponding targets specified in the POST array data is provided in separate objects corresponding to pages specified in the targets object | The backlink shared across targets, keyed by target index, with full per-link detail. | 跨目标共有的外链，按目标序号索引，附带逐条明细。 |
| `resp.200.tasks.result.page_intersection.1` | contains data on a referring page that links to the corresponding target from the POST array field name varies in the range from 1 to 20 according to the number of domains, subdomains or pages in the targets object | Per-target block keyed by the target's index in the request (e.g. `1`, `2`). | 按目标在请求中的序号（如 `1`、`2`）索引的分目标数据块。 |
| `resp.200.tasks.result.page_intersection.1.type` | type of element = ‘backlinks_page_intersection’ | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.page_intersection.1.domain_from` | domain referring to the target domain or webpage | Domain of the page that hosts the backlink (the source). | 承载该外链的来源页面所在域名。 |
| `resp.200.tasks.result.page_intersection.1.url_from` | URL of the page where the backlink is found | Full URL of the source page that contains the backlink. | 包含该外链的来源页面完整 URL。 |
| `resp.200.tasks.result.page_intersection.1.url_from_https` | indicates whether the referring URL is secured with HTTPS if true, the referring URL is secured with HTTPS | Whether the source URL uses HTTPS. | 来源 URL 是否使用 HTTPS。 |
| `resp.200.tasks.result.page_intersection.1.domain_to` | domain the backlink is pointing to | Domain the backlink points to (the target side). | 该外链所指向的目标侧域名。 |
| `resp.200.tasks.result.page_intersection.1.url_to` | URL the backlink is pointing to | Full URL the backlink points to. | 该外链指向的完整 URL。 |
| `resp.200.tasks.result.page_intersection.1.url_to_https` | indicates if the URL the backlink is pointing to is secured with HTTPS if true, the URL is secured with HTTPS | Whether the destination URL uses HTTPS. | 目标 URL 是否使用 HTTPS。 |
| `resp.200.tasks.result.page_intersection.1.tld_from` | top-level domain of the referring URL | Top-level domain of the source page. | 来源页面的顶级域名（TLD）。 |
| `resp.200.tasks.result.page_intersection.1.is_new` | indicates whether the backlink is new if true, the backlink was found on the page last time our crawler visited it | Whether this backlink was newly discovered in the latest check. | 该外链是否为最近一次检测新发现。 |
| `resp.200.tasks.result.page_intersection.1.is_lost` | indicates whether the backlink was removed if true, the backlink or the entire page was removed | Whether this backlink has been detected as lost. | 该外链是否已被检测为丢失。 |
| `resp.200.tasks.result.page_intersection.1.backlink_spam_score` | spam score of the backlink learn more about how the metric is calculated on this help center page | Spam score of this individual backlink; higher values flag a riskier source. | 该条外链的垃圾分，越高表示来源风险越大。 |
| `resp.200.tasks.result.page_intersection.1.rank` | backlink rank rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity, on the chosen `rank_scale`; higher means stronger link authority. | 该实体基于外链的权重评分，按所选 `rank_scale` 计算，越高表示链接权重越强。 |
| `resp.200.tasks.result.page_intersection.1.page_from_rank` | page rank of the referring page page_from_rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Authority score of the source page, on the chosen `rank_scale`. | 来源页面的权重评分，按 `rank_scale` 计算。 |
| `resp.200.tasks.result.page_intersection.1.domain_from_rank` | domain rank of the referring domain indicates the rank of the domain at the time our crawler last saw the backlink; domain_from_rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Authority score of the source domain, on the chosen `rank_scale`. | 来源域名的权重评分，按 `rank_scale` 计算。 |
| `resp.200.tasks.result.page_intersection.1.domain_from_platform_type` | platform types of the referring domain possible values: cms, blogs, ecommerce, message-boards, wikis, news, organization | Platform type(s) detected for the source domain (e.g. cms, blogs). | 来源域名检测到的平台类型（如 cms、blogs）。 |
| `resp.200.tasks.result.page_intersection.1.domain_from_is_ip` | indicates if the domain is IP if true, the domain functions as an IP address and does not have a domain name | Whether the source is addressed by a bare IP rather than a domain name. | 来源是否以裸 IP（而非域名）寻址。 |
| `resp.200.tasks.result.page_intersection.1.domain_from_ip` | IP address of the referring domain | IP address of the source domain. | 来源域名的 IP 地址。 |
| `resp.200.tasks.result.page_intersection.1.domain_from_country` | ISO country code of the referring domain | Country associated with the source domain. | 来源域名关联的国家/地区。 |
| `resp.200.tasks.result.page_intersection.1.page_from_external_links` | number of external links found on the referring page | Number of external links on the source page. | 来源页面上的外部链接数量。 |
| `resp.200.tasks.result.page_intersection.1.page_from_internal_links` | number of internal links found on the referring page | Number of internal links on the source page. | 来源页面上的内部链接数量。 |
| `resp.200.tasks.result.page_intersection.1.page_from_size` | size of the referring page, in bytes example: 63357 | Size of the source page in bytes. | 来源页面的字节大小。 |
| `resp.200.tasks.result.page_intersection.1.page_from_encoding` | character encoding of the referring page example: utf-8 | Character encoding of the source page. | 来源页面的字符编码。 |
| `resp.200.tasks.result.page_intersection.1.page_from_language` | language of the referring page in ISO 639-1 format example: en | Detected language of the source page. | 来源页面检测到的语言。 |
| `resp.200.tasks.result.page_intersection.1.page_from_title` | title of the referring page | Title of the source page. | 来源页面的标题。 |
| `resp.200.tasks.result.page_intersection.1.page_from_status_code` | HTTP status code returned by the referring page example: 200 | HTTP status code returned by the source page when last fetched. | 最近一次抓取来源页面返回的 HTTP 状态码。 |
| `resp.200.tasks.result.page_intersection.1.first_seen` | date and time when our crawler found the backlink for the first time in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date the entity (link/anchor/page) was first discovered in the index. | 该实体（链接/锚文本/页面）首次被索引发现的日期。 |
| `resp.200.tasks.result.page_intersection.1.prev_seen` | previous to the most recent date when our crawler visited the backlink in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date of the previous time the entity was seen before the most recent check. | 最近一次检测之前，该实体上一次被发现的日期。 |
| `resp.200.tasks.result.page_intersection.1.last_seen` | most recent date when our crawler visited the backlink in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Most recent date the entity was confirmed present. | 该实体最近一次被确认存在的日期。 |
| `resp.200.tasks.result.page_intersection.1.item_type` | link type possible values: anchor, image, link, meta, canonical, alternate, redirect | Specific type of this backlink record (e.g. anchor, redirect, image). | 该外链记录的具体类型（如锚文本、重定向、图片）。 |
| `resp.200.tasks.result.page_intersection.1.attributes` | link attributes of the referring links example: nofollow | HTML rel attributes of the link (e.g. nofollow, ugc, sponsored). | 链接的 HTML rel 属性（如 nofollow、ugc、sponsored）。 |
| `resp.200.tasks.result.page_intersection.1.dofollow` | indicates whether the backlink is dofollow if false, the backlink is nofollow | Whether the link passes authority (true = dofollow, false = nofollow). | 该链接是否传递权重（true 为 dofollow，false 为 nofollow）。 |
| `resp.200.tasks.result.page_intersection.1.original` | indicates whether the backlink was present on the referring page when our crawler first visited it | Whether this is the original link as opposed to a duplicate occurrence. | 该链接是否为原始链接（而非重复出现）。 |
| `resp.200.tasks.result.page_intersection.1.alt` | alternative text of the image this field will be null if backlink type is not image | Alt text of the link when it is an image link. | 图片链接时该链接的 alt 文本。 |
| `resp.200.tasks.result.page_intersection.1.anchor` | anchor text of the backlink | The anchor text used by the backlink(s); for the backlinks endpoint, the clickable text of this link. | 外链所用的锚文本；在外链明细接口中即该链接的可点击文字。 |
| `resp.200.tasks.result.page_intersection.1.text_pre` | text snippet before the anchor text | Text appearing immediately before the anchor on the source page. | 来源页面上锚文本前紧邻的文字。 |
| `resp.200.tasks.result.page_intersection.1.text_post` | snippet after the anchor text | Text appearing immediately after the anchor on the source page. | 来源页面上锚文本后紧邻的文字。 |
| `resp.200.tasks.result.page_intersection.1.semantic_location` | indicates semantic element in HTML where the backlink is found you can get the full list of semantic elements here examples: article, section, summary | Semantic location of the link on the source page (e.g. content, header, footer). | 链接在来源页面上的语义位置（如正文、页眉、页脚）。 |
| `resp.200.tasks.result.page_intersection.1.links_count` | number of identical backlinks found on the referring page | Number of links from the source page to the target. | 来源页面指向目标的链接数量。 |
| `resp.200.tasks.result.page_intersection.1.group_count` | indicates total number of backlinks from this domain for example, if mode is set to one_per_domain, this field will indicate the total number of backlinks coming from this domain | Number of grouped duplicate links represented by this record. | 该记录所代表的合并重复链接数量。 |
| `resp.200.tasks.result.page_intersection.1.is_broken` | indicates whether the backlink is broken if true, the backlink is pointing to a page responding with a 4xx or 5xx status code | Whether the link is broken (its destination does not resolve). | 该链接是否失效（目标无法访问）。 |
| `resp.200.tasks.result.page_intersection.1.url_to_status_code` | status code of the referenced page if the value is null, our crawler hasn’t yet visited the webpage the link is pointing to example: 200 | HTTP status code returned by the destination URL. | 目标 URL 返回的 HTTP 状态码。 |
| `resp.200.tasks.result.page_intersection.1.url_to_spam_score` | spam score of the referenced page if the value is null, our crawler hasn’t yet visited the webpage the link is pointing to learn more about how the metric is calculated on this help center page | Spam score of the destination URL. | 目标 URL 的垃圾分。 |
| `resp.200.tasks.result.page_intersection.1.url_to_redirect_target` | target url of the redirect target page the redirect is pointing to | Final URL the destination redirects to, if any. | 目标若发生重定向，则最终跳转到的 URL。 |
| `resp.200.tasks.result.page_intersection.1.is_indirect_link` | indicates whether the backlink is an indirect link if true, the backlink is an indirect link pointing to a page that either redirects to url_to, or points to a canonical page | Whether the link reaches the target indirectly via a redirect or canonical chain. | 该链接是否经由重定向或 canonical 链间接到达目标。 |
| `resp.200.tasks.result.page_intersection.1.indirect_link_path` | indirect link path indicates a URL or a sequence of URLs that lead to url_to | The redirect/canonical hops the indirect link passes through to reach the target. | 间接链接到达目标所经过的重定向/canonical 跳转路径。 |
| `resp.200.tasks.result.page_intersection.1.indirect_link_path.type` | indirect link type possible values: redirect, canonical | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.page_intersection.1.indirect_link_path.status_code` | HTTP status code of the URL | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.result.page_intersection.1.indirect_link_path.url` | indirect link URL | URL of the page this item describes. | 该条目所描述页面的 URL。 |
| `resp.200.tasks.result.summary` | contains the page intersections summary | Roll-up of intersection statistics for this item. | 该条目交集统计的汇总。 |
| `resp.200.tasks.result.summary.intersections_count` | total number of intersections | Number of targets this item intersects with. | 该条目所交集的目标数量。 |

## post_dataforseo_backlinks_referring_domains_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with a detailed overview of referring domains pointing to the target you specify. | Returns the domains that link to a target, each with aggregated backlink metrics — the standard way to audit who links to a site at domain granularity. | 返回链向目标的引荐域名列表，每条附带聚合外链指标，是按域名粒度审计「谁链向站点」的标准方式。 |
| `req.target` | domain, subdomain or webpage to get referring domains for required field a domain or a subdomain should be specified without https:// and www. a page should be specified with absolute URL (including http:// or https://) | Domain, subdomain or webpage whose backlink data you want. Specify a domain/subdomain without the `http(s)://` scheme and without `www.`; specify a page as an absolute URL including the scheme. | 要查询外链数据的域名、子域名或网页。域名/子域名填写时去掉 `http(s)://` 和 `www.`；网页则填写含协议的完整绝对 URL。 |
| `req.limit` | the maximum number of returned domains optional field default value: 100 maximum value: 1000 | Page size: how many items to return in this call. Combine with `offset` to page through the full result set. | 单页返回条数，配合 `offset` 翻页遍历完整结果集。 |
| `req.offset` | offset in the results array of returned domains optional field default value: 0 if you specify the 10 value, the first ten domains in the results array will be omitted and the data will be provided for the successive pages | Number of leading items to skip before returning results, for offset-based pagination together with `limit`. | 返回前跳过的结果条数，与 `limit` 配合实现偏移分页。 |
| `req.internal_list_limit` | maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: referring_links_tld referring_links_types referring_links_attributes referring_links_platform_types referring_links_semantic_locations default value: 10 maximum value: 1000 | Caps how many elements are returned inside the nested aggregation arrays of each item (e.g. `referring_links_tld`, `referring_links_types`); lower it to keep responses small. | 限制每个条目内嵌聚合数组（如 `referring_links_tld`、`referring_links_types`）的元素数量，调小可精简响应体。 |
| `req.backlinks_status_type` | set what backlinks to return and count optional field you can use this field to choose what backlinks will be returned and used for aggregated metrics for your target; possible values: all – all backlinks will be returned and counted; live – backlinks found during the last check will be returned and counted; lost – lost backlinks will be returned and counted; default value: live | Selects which backlinks feed both the returned list and the aggregated metrics: `all` counts every known link, `live` only links confirmed in the latest check, `lost` only links that have disappeared. | 决定纳入列表与聚合指标的外链范围：`all` 计入全部已知外链，`live` 仅最近一次检测仍存活的，`lost` 仅已消失的。 |
| `req.filters` | array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, =, , in, not_in, like, not_like, match, not_match you can use the % operator with like and not_like to match any string of zero or more characters example: ["referring_pages",">","1"] [["referring_pages",">","2"], "and", ["backlinks",">","10"]] [["first_seen",">","2017-10-23 11:31:45 +00:00"], "and", [["domain","like","%dataforseo.com%"],"or",["referring_domains",">","10"]]] The full list of possible filters is available here. | Conditions applied to the returned items, combined with `and`/`or` logical operators (up to 8). Reference any item field by name to narrow large result sets. | 对返回条目施加的筛选条件，用 `and`/`or` 组合（最多 8 条），可引用任意条目字段名以收窄大结果集。 |
| `req.order_by` | results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["backlinks,desc"] note that you can set no more than three sorting rules in a single request you should use a comma to separate several sorting rules example: ["backlinks,desc","rank,asc"] | Sorting rules for the returned items; each rule pairs a field with `asc` or `desc`, up to three rules applied in order. | 返回条目的排序规则，每条规则指定字段加 `asc`/`desc`，最多三条按顺序生效。 |
| `req.backlinks_filters` | filter the backlinks of your target optional field you can use this field to filter the initial backlinks that will be included in the dataset for aggregated metrics for your target you can filter the backlinks by all fields available in the response of this endpoint using this parameter, you can include only dofollow backlinks in the response and create a flexible backlinks dataset to calculate the metrics for example: "backlinks_filters": ["dofollow", "=", true] | Pre-filters the raw backlinks that go into the aggregated metrics for the target, so metrics can be computed over a custom subset (e.g. dofollow-only). | 在计算目标聚合指标前先筛选原始外链，从而基于自定义子集（如仅 dofollow）计算指标。 |
| `req.include_subdomains` | indicates if the subdomains of the target will be included in the search optional field if set to false, the subdomains will be ignored default value: true | When true, subdomains of the target are included in the search; set false to restrict analysis to the target host only. | 为 true 时把目标的子域名一并纳入检索；设为 false 则只分析目标主机本身。 |
| `req.include_indirect_links` | indicates if indirect links to the target will be included in the results optional field if set to true, the results will include data on indirect links pointing to a page that either redirects to the target, or points to a canonical page if set to false, indirect links will be ignored default value: true | When true, results also count indirect links — links to a page that redirects to the target or points to the target's canonical page. | 为 true 时一并统计间接链接，即指向「会重定向到目标」或「指向目标 canonical 页」的页面的链接。 |
| `req.exclude_internal_backlinks` | indicates whether the backlinks from subdomains of the target are excluded optional field if set to false, the backlinks from subdomains of the target will be ommited and you won’t receive the same domain in the response; default value: true | When true, links coming from the target's own subdomains are dropped so the same domain is not reported as its own referrer; set false to keep such internal links. | 为 true 时剔除来自目标自身子域名的链接，避免目标域名成为自己的引荐方；设为 false 则保留这类内部链接。 |
| `req.rank_scale` | defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on a 0–1000 scale default value: one_thousand learn more about how this parameter works and how ranking metrics are calculated in this Help Center article | Chooses the numeric scale for all rank-style metrics (`rank`, `domain_from_rank`, `page_from_rank`): `one_hundred` for a 0–100 scale or `one_thousand` for a 0–1000 scale. | 选择各类 rank 指标（`rank`、`domain_from_rank`、`page_from_rank`）的数值刻度：`one_hundred` 为 0–100，`one_thousand` 为 0–1000。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.target` | target in a POST array | The target this result block refers to, echoing the requested domain/subdomain/page. | 该结果块所对应的目标，回显请求的域名/子域名/页面。 |
| `resp.200.tasks.result.total_count` | total number of relevant items in the database total number of main domains referring to your target; example.com and blog.example.com are counted as one referring domain | Total number of items matching the request, ignoring pagination. | 匹配请求的条目总数（不受分页影响）。 |
| `resp.200.tasks.result.items_count` | number of items in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | items array | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.type` | type of element = ‘backlinks_referring_domain’ | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.items.domain` | referring domain | The referring domain that links to the target. | 链向目标的引荐域名。 |
| `resp.200.tasks.result.items.rank` | domain rank rank volume that a referring website passes to the target rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity, on the chosen `rank_scale`; higher means stronger link authority. | 该实体基于外链的权重评分，按所选 `rank_scale` 计算，越高表示链接权重越强。 |
| `resp.200.tasks.result.items.backlinks` | indicates the number of backlinks pointing to the target | Number of backlinks pointing to the entity. | 指向该实体的外链数量。 |
| `resp.200.tasks.result.items.first_seen` | date and time when our crawler found the backlink for the first time in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date the entity (link/anchor/page) was first discovered in the index. | 该实体（链接/锚文本/页面）首次被索引发现的日期。 |
| `resp.200.tasks.result.items.lost_date` | date and time when the last backlink from this domain was lost indicates the date and time when our crawler visited the page and it responded with 4xx or 5xx status code or the last backlink was removed in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2017-01-24 13:20:59 +00:00 | Date the entity was detected as lost; null if it is still live. | 该实体被检测为丢失的日期；仍存活则为 null。 |
| `resp.200.tasks.result.items.backlinks_spam_score` | average spam score of all backlinks pointing to the domain learn more about how the metric is calculated on this help center page | Aggregated spam score of the entity's backlink profile; higher values flag riskier link sources. | 该实体外链画像的聚合垃圾分，越高表示链接来源风险越大。 |
| `resp.200.tasks.result.items.broken_backlinks` | number of broken backlinks number of broken backlinks pointing to the domain | Number of backlinks pointing to broken (non-resolving) pages of the entity. | 指向该实体失效（无法访问）页面的外链数量。 |
| `resp.200.tasks.result.items.broken_pages` | number of broken pages number of pages that respond with 4xx or 5xx status codes where backlinks are pointing to | Number of the entity's pages that are broken but still receive backlinks. | 该实体仍在接收外链但已失效的页面数量。 |
| `resp.200.tasks.result.items.referring_domains` | indicates the number of referring domains note that we calculate main domains (root domains, like example.com) and their subdomains (e.g. blog.example.com) separately for this metric | Number of distinct domains linking to the entity. | 链向该实体的不同域名数量。 |
| `resp.200.tasks.result.items.referring_domains_nofollow` | number of domains pointing at least one nofollow link to the target | Number of referring domains whose links are all nofollow. | 链接全为 nofollow 的引荐域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains` | indicates the number of referring main domains the number of primary (root) domains referring to your target | Number of distinct main (root) domains linking to the entity. | 链向该实体的不同主（根）域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains_nofollow` | number of main domains pointing at least one nofollow link to the target | Number of referring main domains whose links are all nofollow. | 链接全为 nofollow 的引荐主域名数量。 |
| `resp.200.tasks.result.items.referring ips` | number of referring IP addresses number of IP addresses pointing to this page | Number of distinct IP addresses the backlinks originate from. | 外链来源的不同 IP 地址数量。 |
| `resp.200.tasks.result.items.referring subnets` | number of referring subnetworks | Number of distinct subnets the backlinks originate from. | 外链来源的不同子网数量。 |
| `resp.200.tasks.result.items.referring_pages` | indicates the number of pages pointing to the target specified | Number of distinct pages linking to the entity. | 链向该实体的不同页面数量。 |
| `resp.200.tasks.result.items.referring_links_tld` | top-level domains of the referring links contains top level domains and referring link count per each | Breakdown of referring links by the top-level domain of their source. | 引荐链接按来源顶级域名（TLD）的分布。 |
| `resp.200.tasks.result.items.referring_links_types` | types of referring links indicates the types of the referring links and link count per each type possible values: anchor, image, link, meta, canonical, alternate, redirect | Breakdown of referring links by link type (e.g. anchor, image, redirect). | 引荐链接按链接类型（如锚文本、图片、重定向）的分布。 |
| `resp.200.tasks.result.items.referring_links_attributes` | link attributes of the referring links indicates link attributes of the referring links and link count per each attribute | Breakdown of referring links by their HTML attributes (e.g. nofollow, sponsored). | 引荐链接按其 HTML 属性（如 nofollow、sponsored）的分布。 |
| `resp.200.tasks.result.items.referring_links_platform_types` | types of referring platforms indicates referring platform types and link count per each platform possible values: cms, blogs, ecommerce, message-boards, wikis, news, organization | Breakdown of referring links by the platform type of their source site. | 引荐链接按来源站点平台类型的分布。 |
| `resp.200.tasks.result.items.referring_links_semantic_locations` | semantic locations of the referring links indicates semantic elements in HTML where the referring links are located and the link count per each semantic location you can get the full list of semantic elements here examples: article, section, summary | Breakdown of referring links by the semantic location on the source page (e.g. content, footer). | 引荐链接按其在来源页面上的语义位置（如正文、页脚）的分布。 |
| `resp.200.tasks.result.items.referring_links_countries` | ISO country codes of the referring links indicates ISO country codes of the domains where the referring links are located and the link count per each country | Breakdown of referring links by the country of their source. | 引荐链接按来源国家/地区的分布。 |
| `resp.200.tasks.result.items.referring_pages_nofollow` | number of referring pages pointing at least one nofollow link to the target | Number of referring pages whose links are all nofollow. | 链接全为 nofollow 的引荐页面数量。 |

## post_dataforseo_backlinks_referring_networks_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with a detailed overview of referring IPs and subnets pointing to the `target` you specify. | Returns the IP networks or subnets that backlinks originate from, with aggregated metrics — useful for detecting link networks and IP diversity. | 返回外链来源的 IP 网络或子网及其聚合指标，便于识别链接网络与 IP 多样性。 |
| `req.target` | domain, subdomain or webpage to get referring networks for required field a domain or a subdomain should be specified without https:// and www. a page should be specified with absolute URL (including http:// or https://) | Domain, subdomain or webpage whose backlink data you want. Specify a domain/subdomain without the `http(s)://` scheme and without `www.`; specify a page as an absolute URL including the scheme. | 要查询外链数据的域名、子域名或网页。域名/子域名填写时去掉 `http(s)://` 和 `www.`；网页则填写含协议的完整绝对 URL。 |
| `req.network_address_type` | indicates the type of network to get data for optional field possible values: ip, subnet default value: ip | Whether referring networks are grouped by individual IP (`ip`) or by subnet (`subnet`). | 引荐网络的归并粒度：按单个 IP（`ip`）或按子网（`subnet`）。 |
| `req.limit` | the maximum number of returned networks optional field default value: 100 maximum value: 1000 | Page size: how many items to return in this call. Combine with `offset` to page through the full result set. | 单页返回条数，配合 `offset` 翻页遍历完整结果集。 |
| `req.offset` | offset in the results array of returned networks optional field default value: 0 if you specify the 10 value, the first ten domains in the results array will be omitted and the data will be provided for the successive pages | Number of leading items to skip before returning results, for offset-based pagination together with `limit`. | 返回前跳过的结果条数，与 `limit` 配合实现偏移分页。 |
| `req.internal_list_limit` | maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: referring_links_tld referring_links_types referring_links_attributes referring_links_platform_types referring_links_semantic_locations default value: 10 maximum value: 1000 | Caps how many elements are returned inside the nested aggregation arrays of each item (e.g. `referring_links_tld`, `referring_links_types`); lower it to keep responses small. | 限制每个条目内嵌聚合数组（如 `referring_links_tld`、`referring_links_types`）的元素数量，调小可精简响应体。 |
| `req.backlinks_status_type` | set what backlinks to return and count optional field you can use this field to choose what backlinks will be returned and used for aggregated metrics for your target; possible values: all – all backlinks will be returned and counted; live – backlinks found during the last check will be returned and counted; lost – lost backlinks will be returned and counted; default value: live | Selects which backlinks feed both the returned list and the aggregated metrics: `all` counts every known link, `live` only links confirmed in the latest check, `lost` only links that have disappeared. | 决定纳入列表与聚合指标的外链范围：`all` 计入全部已知外链，`live` 仅最近一次检测仍存活的，`lost` 仅已消失的。 |
| `req.filters` | array of results filtering parameters optional field you can add several filters at once (8 filters maximum) you should set a logical operator and, or between the conditions the following operators are supported: regex, not_regex, =, , in, not_in, like, not_like, ilike, not_ilike, match, not_match you can use the % operator with like and not_like to match any string of zero or more characters example: ["referring_pages",">","1"] [["referring_pages",">","2"], "and", ["backlinks",">","10"]] [["first_seen",">","2017-10-23 11:31:45 +00:00"], "and", [["network_address","like","194.1.%"],"or",["referring_ips",">","10"]]] The full list of possible filters is available here. | Conditions applied to the returned items, combined with `and`/`or` logical operators (up to 8). Reference any item field by name to narrow large result sets. | 对返回条目施加的筛选条件，用 `and`/`or` 组合（最多 8 条），可引用任意条目字段名以收窄大结果集。 |
| `req.order_by` | results sorting rules optional field you can use the same values as in the filters array to sort the results possible sorting types: asc – results will be sorted in the ascending order desc – results will be sorted in the descending order you should use a comma to set up a sorting type example: ["backlinks,desc"] note that you can set no more than three sorting rules in a single request you should use a comma to separate several sorting rules example: ["backlinks,desc","rank,asc"] | Sorting rules for the returned items; each rule pairs a field with `asc` or `desc`, up to three rules applied in order. | 返回条目的排序规则，每条规则指定字段加 `asc`/`desc`，最多三条按顺序生效。 |
| `req.backlinks_filters` | filter the backlinks of your target optional field you can use this field to filter the initial backlinks that will be included in the dataset for aggregated metrics for your target you can filter the backlinks by all fields available in the response of this endpoint using this parameter, you can include only dofollow backlinks in the response and create a flexible backlinks dataset to calculate the metrics for example: "backlinks_filters": [["dofollow", "=", true]] | Pre-filters the raw backlinks that go into the aggregated metrics for the target, so metrics can be computed over a custom subset (e.g. dofollow-only). | 在计算目标聚合指标前先筛选原始外链，从而基于自定义子集（如仅 dofollow）计算指标。 |
| `req.include_subdomains` | indicates if the subdomains of the target will be included in the search optional field if set to false, the subdomains will be ignored default value: true | When true, subdomains of the target are included in the search; set false to restrict analysis to the target host only. | 为 true 时把目标的子域名一并纳入检索；设为 false 则只分析目标主机本身。 |
| `req.include_indirect_links` | indicates if indirect links to the target will be included in the results optional field if set to true, the results will include data on indirect links pointing to a page that either redirects to the target, or points to a canonical page if set to false, indirect links will be ignored default value: true | When true, results also count indirect links — links to a page that redirects to the target or points to the target's canonical page. | 为 true 时一并统计间接链接，即指向「会重定向到目标」或「指向目标 canonical 页」的页面的链接。 |
| `req.exclude_internal_backlinks` | indicates whether the backlinks from subdomains of the target are excluded optional field if set to false, the backlinks from subdomains of the target will be ommited and you won’t receive the same domain in the response; default value: true | When true, links coming from the target's own subdomains are dropped so the same domain is not reported as its own referrer; set false to keep such internal links. | 为 true 时剔除来自目标自身子域名的链接，避免目标域名成为自己的引荐方；设为 false 则保留这类内部链接。 |
| `req.rank_scale` | defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on a 0–1000 scale default value: one_thousand learn more about how this parameter works and how ranking metrics are calculated in this Help Center article | Chooses the numeric scale for all rank-style metrics (`rank`, `domain_from_rank`, `page_from_rank`): `one_hundred` for a 0–100 scale or `one_thousand` for a 0–1000 scale. | 选择各类 rank 指标（`rank`、`domain_from_rank`、`page_from_rank`）的数值刻度：`one_hundred` 为 0–100，`one_thousand` 为 0–1000。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.target` | target in a POST array | The target this result block refers to, echoing the requested domain/subdomain/page. | 该结果块所对应的目标，回显请求的域名/子域名/页面。 |
| `resp.200.tasks.result.total_count` | total number of relevant items in the database | Total number of items matching the request, ignoring pagination. | 匹配请求的条目总数（不受分页影响）。 |
| `resp.200.tasks.result.items_count` | number of items in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | items array | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.type` | type of element = ‘backlinks_referring_network’ | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.items.network_address` | address of the referring subnetwork or IP | The IP network or subnet address that backlinks originate from. | 外链来源的 IP 网络或子网地址。 |
| `resp.200.tasks.result.items.rank` | network rank rank volume that a referring network passes to the target rank is calculated based on the method for node ranking in a linked database – a principle used in the original Google PageRank algorithm learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity, on the chosen `rank_scale`; higher means stronger link authority. | 该实体基于外链的权重评分，按所选 `rank_scale` 计算，越高表示链接权重越强。 |
| `resp.200.tasks.result.items.backlinks` | indicates the number of backlinks pointing to the target | Number of backlinks pointing to the entity. | 指向该实体的外链数量。 |
| `resp.200.tasks.result.items.first_seen` | date and time when our crawler found the backlink for the first time in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date the entity (link/anchor/page) was first discovered in the index. | 该实体（链接/锚文本/页面）首次被索引发现的日期。 |
| `resp.200.tasks.result.items.lost_date` | date and time when the last backlink from this domain was lost indicates the date and time when our crawler visited the page and it responded with 4xx or 5xx status code or the last backlink was removed in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2017-01-24 13:20:59 +00:00 | Date the entity was detected as lost; null if it is still live. | 该实体被检测为丢失的日期；仍存活则为 null。 |
| `resp.200.tasks.result.items.broken_backlinks` | number of broken backlinks number of broken backlinks pointing to the domain | Number of backlinks pointing to broken (non-resolving) pages of the entity. | 指向该实体失效（无法访问）页面的外链数量。 |
| `resp.200.tasks.result.items.broken_pages` | number of broken pages number of pages that respond with 4xx or 5xx status codes where backlinks are pointing to | Number of the entity's pages that are broken but still receive backlinks. | 该实体仍在接收外链但已失效的页面数量。 |
| `resp.200.tasks.result.items.referring_domains` | indicates the number of referring domains referring domains include subdomains that are counted as separate domains for this metric | Number of distinct domains linking to the entity. | 链向该实体的不同域名数量。 |
| `resp.200.tasks.result.items.referring_domains_nofollow` | number of domains pointing at least one nofollow link to the target | Number of referring domains whose links are all nofollow. | 链接全为 nofollow 的引荐域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains` | indicates the number of referring main domains | Number of distinct main (root) domains linking to the entity. | 链向该实体的不同主（根）域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains_nofollow` | number of main domains pointing at least one nofollow link to the target | Number of referring main domains whose links are all nofollow. | 链接全为 nofollow 的引荐主域名数量。 |
| `resp.200.tasks.result.items.referring ips` | number of referring IP addresses number of IP addresses pointing to this page | Number of distinct IP addresses the backlinks originate from. | 外链来源的不同 IP 地址数量。 |
| `resp.200.tasks.result.items.referring subnets` | number of referring subnetworks | Number of distinct subnets the backlinks originate from. | 外链来源的不同子网数量。 |
| `resp.200.tasks.result.items.referring_pages` | indicates the number of pages pointing to the target specified | Number of distinct pages linking to the entity. | 链向该实体的不同页面数量。 |
| `resp.200.tasks.result.items.referring_links_tld` | top-level domains of the referring links contains top level domains and referring link count per each | Breakdown of referring links by the top-level domain of their source. | 引荐链接按来源顶级域名（TLD）的分布。 |
| `resp.200.tasks.result.items.referring_links_types` | types of referring links indicates the types of the referring links and link count per each type possible values: anchor, image, link, meta, canonical, alternate, redirect | Breakdown of referring links by link type (e.g. anchor, image, redirect). | 引荐链接按链接类型（如锚文本、图片、重定向）的分布。 |
| `resp.200.tasks.result.items.referring_links_attributes` | link attributes of the referring links indicates link attributes of the referring links and link count per each attribute | Breakdown of referring links by their HTML attributes (e.g. nofollow, sponsored). | 引荐链接按其 HTML 属性（如 nofollow、sponsored）的分布。 |
| `resp.200.tasks.result.items.referring_links_platform_types` | types of referring platforms indicates referring platform types and and link count per each platform possible values: cms, blogs, ecommerce, message-boards, wikis, news, organization | Breakdown of referring links by the platform type of their source site. | 引荐链接按来源站点平台类型的分布。 |
| `resp.200.tasks.result.items.referring_links_semantic_locations` | semantic locations of the referring links indicates semantic elements in HTML where the referring links are located and the link count per each semantic location you can get the full list of semantic elements here examples: article, section, summary | Breakdown of referring links by the semantic location on the source page (e.g. content, footer). | 引荐链接按其在来源页面上的语义位置（如正文、页脚）的分布。 |
| `resp.200.tasks.result.items.referring_links_countries` | ISO country codes of the referring links indicates ISO country codes of the domains where the referring links are located and the link count per each country | Breakdown of referring links by the country of their source. | 引荐链接按来源国家/地区的分布。 |
| `resp.200.tasks.result.items.referring_pages_nofollow` | number of referring pages pointing at least one nofollow link to the target | Number of referring pages whose links are all nofollow. | 链接全为 nofollow 的引荐页面数量。 |

## post_dataforseo_backlinks_summary_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with an overview of backlinks data available for a given domain, subdomain, or webpage. | Returns a single aggregated snapshot of a target's entire backlink profile (rank, counts, spam score, host info). The go-to overview endpoint for one target. | 返回目标整体外链画像的单条聚合快照（rank、各类计数、垃圾分、主机信息），是针对单一目标的总览首选接口。 |
| `req.target` | domain, subdomain or webpage to get data for required field a domain or a subdomain should be specified without https:// and www. a page should be specified with absolute URL (including http:// or https://) | Domain, subdomain or webpage whose backlink data you want. Specify a domain/subdomain without the `http(s)://` scheme and without `www.`; specify a page as an absolute URL including the scheme. | 要查询外链数据的域名、子域名或网页。域名/子域名填写时去掉 `http(s)://` 和 `www.`；网页则填写含协议的完整绝对 URL。 |
| `req.include_subdomains` | indicates if the subdomains of the target will be included in the search optional field if set to false, the subdomains will be ignored default value: true | When true, subdomains of the target are included in the search; set false to restrict analysis to the target host only. | 为 true 时把目标的子域名一并纳入检索；设为 false 则只分析目标主机本身。 |
| `req.include_indirect_links` | indicates if indirect links to the target will be included in the results optional field if set to true, the results will include data on indirect links pointing to a page that either redirects to the target, or points to a canonical page if set to false, indirect links will be ignored default value: true | When true, results also count indirect links — links to a page that redirects to the target or points to the target's canonical page. | 为 true 时一并统计间接链接，即指向「会重定向到目标」或「指向目标 canonical 页」的页面的链接。 |
| `req.exclude_internal_backlinks` | indicates if internal backlinks from subdomains to the target will be excluded from the results optional field if set to true, the results will not include data on internal backlinks from subdomains of the same domain as target if set to false, internal links will be included in the results default value: true | When true, links coming from the target's own subdomains are dropped so the same domain is not reported as its own referrer; set false to keep such internal links. | 为 true 时剔除来自目标自身子域名的链接，避免目标域名成为自己的引荐方；设为 false 则保留这类内部链接。 |
| `req.internal_list_limit` | maximum number of elements within internal arrays optional field you can use this field to limit the number of elements within the following arrays: referring_links_tld referring_links_types referring_links_attributes referring_links_platform_types referring_links_semantic_locations default value: 10 maximum value: 1000 | Caps how many elements are returned inside the nested aggregation arrays of each item (e.g. `referring_links_tld`, `referring_links_types`); lower it to keep responses small. | 限制每个条目内嵌聚合数组（如 `referring_links_tld`、`referring_links_types`）的元素数量，调小可精简响应体。 |
| `req.backlinks_status_type` | set what backlinks to return and count optional field you can use this field to choose what backlinks will be returned and used for aggregated metrics for your target; possible values: all – all backlinks will be returned and counted; live – backlinks found during the last check will be returned and counted; lost – lost backlinks will be returned and counted; default value: live | Selects which backlinks feed both the returned list and the aggregated metrics: `all` counts every known link, `live` only links confirmed in the latest check, `lost` only links that have disappeared. | 决定纳入列表与聚合指标的外链范围：`all` 计入全部已知外链，`live` 仅最近一次检测仍存活的，`lost` 仅已消失的。 |
| `req.backlinks_filters` | filter the backlinks of your target optional field you can use this field to filter the initial backlinks that will be included in the dataset for aggregated metrics for your target you can filter the backlinks by all fields available in the response of this endpoint using this parameter, you can include only dofollow backlinks in the response and create a flexible backlinks dataset to calculate the metrics for example: "backlinks_filters": ["dofollow", "=", true] | Pre-filters the raw backlinks that go into the aggregated metrics for the target, so metrics can be computed over a custom subset (e.g. dofollow-only). | 在计算目标聚合指标前先筛选原始外链，从而基于自定义子集（如仅 dofollow）计算指标。 |
| `req.rank_scale` | defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on a 0–1000 scale default value: one_thousand learn more about how this parameter works and how ranking metrics are calculated in this Help Center article | Chooses the numeric scale for all rank-style metrics (`rank`, `domain_from_rank`, `page_from_rank`): `one_hundred` for a 0–100 scale or `one_thousand` for a 0–1000 scale. | 选择各类 rank 指标（`rank`、`domain_from_rank`、`page_from_rank`）的数值刻度：`one_hundred` 为 0–100，`one_thousand` 为 0–1000。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.target` | target in a POST array | The target this result block refers to, echoing the requested domain/subdomain/page. | 该结果块所对应的目标，回显请求的域名/子域名/页面。 |
| `resp.200.tasks.result.first_seen` | date and time when our crawler found the backlink for the target for the first time in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date the entity (link/anchor/page) was first discovered in the index. | 该实体（链接/锚文本/页面）首次被索引发现的日期。 |
| `resp.200.tasks.result.lost_date` | date and time when the backlink was lost indicates the date and time when our crawler visited the target and it responded with a 4xx or 5xx status code or when its last backlink was removed in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date the entity was detected as lost; null if it is still live. | 该实体被检测为丢失的日期；仍存活则为 null。 |
| `resp.200.tasks.result.rank` | target rank learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity, on the chosen `rank_scale`; higher means stronger link authority. | 该实体基于外链的权重评分，按所选 `rank_scale` 计算，越高表示链接权重越强。 |
| `resp.200.tasks.result.backlinks` | indicates the number of backlinks | Number of backlinks pointing to the entity. | 指向该实体的外链数量。 |
| `resp.200.tasks.result.backlinks_spam_score` | spam score of the backlinks displays the total spam score of all backlinks pointing to the target domain, subdomain, or webpage; to learn more about how the metric is calculated, refer to this Help Center page | Aggregated spam score of the entity's backlink profile; higher values flag riskier link sources. | 该实体外链画像的聚合垃圾分，越高表示链接来源风险越大。 |
| `resp.200.tasks.result.crawled_pages` | number of crawled pages for the target | Number of the target's pages crawled by DataForSEO. | DataForSEO 已抓取的目标页面数量。 |
| `resp.200.tasks.result.info` | information about the target | Host and technology information about the target. | 目标的主机与技术信息。 |
| `resp.200.tasks.result.info.server` | server | Web server software reported for the host. | 主机上报的 Web 服务器软件。 |
| `resp.200.tasks.result.info.cms` | content management system | Detected content management systems. | 检测到的内容管理系统（CMS）。 |
| `resp.200.tasks.result.info.platform_type` | platform type | Platform type(s) detected for the page (e.g. cms, blogs). | 页面检测到的平台类型（如 cms、blogs）。 |
| `resp.200.tasks.result.info.ip_address` | IP address of the target | IP address of the target host. | 目标主机的 IP 地址。 |
| `resp.200.tasks.result.info.country` | country code that the target domain is determined to belong to | Country associated with the target host. | 目标主机关联的国家/地区。 |
| `resp.200.tasks.result.info.is_ip` | indicates if the target is IP if true, the domain, subdomain or webpage functions as an IP address and does not have a domain name | Whether the target is addressed by a bare IP rather than a domain name. | 目标是否以裸 IP（而非域名）寻址。 |
| `resp.200.tasks.result.info.target_spam_score` | spam score of the target if the target is a domain/subdomain, this fields indicates the average spam score of all pages of that domain/subdomain; learn more about how the metric is calculated on this help center page | Spam score computed for the target host. | 针对目标主机计算的垃圾分。 |
| `resp.200.tasks.result.internal_links_count` | number of internal links calculated as the sum of internal links on the pages of the specified target | Number of internal links on the page. | 页面上的内部链接数量。 |
| `resp.200.tasks.result.external_links_count` | number of external links on the page calculated as the sum of external links on the pages of the specified target | Number of external links on the page. | 页面上的外部链接数量。 |
| `resp.200.tasks.result.broken_backlinks` | number of broken backlinks number of broken backlinks pointing to the target | Number of backlinks pointing to broken (non-resolving) pages of the entity. | 指向该实体失效（无法访问）页面的外链数量。 |
| `resp.200.tasks.result.broken_pages` | number of broken pages number of pages on the target that respond with 4xx or 5xx status codes note that the number of broken pages includes pages on the target discovered by following external links, but it may also include pages discovered by following the target’s sitemap | Number of the entity's pages that are broken but still receive backlinks. | 该实体仍在接收外链但已失效的页面数量。 |
| `resp.200.tasks.result.referring_domains` | indicates the number of referring domains referring domains include subdomains that are counted as separate domains for this metric | Number of distinct domains linking to the entity. | 链向该实体的不同域名数量。 |
| `resp.200.tasks.result.referring_domains_nofollow` | number of domains pointing at least one nofollow link to the target | Number of referring domains whose links are all nofollow. | 链接全为 nofollow 的引荐域名数量。 |
| `resp.200.tasks.result.referring_main_domains` | indicates the number of referring main domains | Number of distinct main (root) domains linking to the entity. | 链向该实体的不同主（根）域名数量。 |
| `resp.200.tasks.result.referring_main_domains_nofollow` | number of main domains pointing at least one nofollow link to the target | Number of referring main domains whose links are all nofollow. | 链接全为 nofollow 的引荐主域名数量。 |
| `resp.200.tasks.result.referring_ips` | number of referring IP addresses number of IP addresses pointing to this page | Number of distinct IP addresses the backlinks originate from. | 外链来源的不同 IP 地址数量。 |
| `resp.200.tasks.result.referring_subnets` | number of referring subnetworks | Number of distinct subnets the backlinks originate from. | 外链来源的不同子网数量。 |
| `resp.200.tasks.result.referring_pages` | indicates the number of pages pointing to the target | Number of distinct pages linking to the entity. | 链向该实体的不同页面数量。 |
| `resp.200.tasks.result.referring_links_tld` | top-level domains of the referring links contains top level domains and referring link count per each | Breakdown of referring links by the top-level domain of their source. | 引荐链接按来源顶级域名（TLD）的分布。 |
| `resp.200.tasks.result.referring_links_types` | types of referring links indicates the types of the referring links and link count per each type possible values: anchor, image, link, meta, canonical, alternate, redirect | Breakdown of referring links by link type (e.g. anchor, image, redirect). | 引荐链接按链接类型（如锚文本、图片、重定向）的分布。 |
| `resp.200.tasks.result.referring_links_attributes` | link attributes of the referring links indicates link attributes of the referring links and link count per each attribute example values: nofollow, noopener, noreferrer, external, ugc, sponsored | Breakdown of referring links by their HTML attributes (e.g. nofollow, sponsored). | 引荐链接按其 HTML 属性（如 nofollow、sponsored）的分布。 |
| `resp.200.tasks.result.referring_links_platform_types` | types of referring platforms indicates referring platform types and and link count per each platform possible values: cms, blogs, ecommerce, message-boards, wikis, news, organization | Breakdown of referring links by the platform type of their source site. | 引荐链接按来源站点平台类型的分布。 |
| `resp.200.tasks.result.referring_links_semantic_locations` | semantic locations of the referring links indicates semantic elements in HTML where the referring links are located and link count per each semantic location you can get the full list of semantic elements here example values: article, section, summary, "" | Breakdown of referring links by the semantic location on the source page (e.g. content, footer). | 引荐链接按其在来源页面上的语义位置（如正文、页脚）的分布。 |
| `resp.200.tasks.result.referring_links_countries` | ISO country codes of the referring links indicates ISO country codes of the domains where the referring links are located and the link count per each country | Breakdown of referring links by the country of their source. | 引荐链接按来源国家/地区的分布。 |
| `resp.200.tasks.result.referring_pages_nofollow` | number of referring pages pointing at least one nofollow link to the target | Number of referring pages whose links are all nofollow. | 链接全为 nofollow 的引荐页面数量。 |

## post_dataforseo_backlinks_timeseries_new_lost_summary_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with the number of new and lost backlinks and referring domains for the domain specified in the `target` field. | Returns a time series of newly gained and lost backlinks and referring domains for a target, bucketed by the chosen range. | 返回目标新增与丢失外链及引荐域名的时间序列，按指定粒度分桶。 |
| `req.target` | domain to get data for required field a domain should be specified without https:// and www. example: "forbes.com" | Domain whose new/lost timeseries summary you want, specified without `http(s)://` and without `www.`. | 要查询新增/丢失时间序列汇总的域名，去掉 `http(s)://` 和 `www.` 填写。 |
| `req.date_from` | starting date of the time range optional field this field indicates the date which will be used as a threshold for new and lost backlinks and referring domains; the backlinks and referring domains that appeared in our index after the specified date will be considered as new; the backlinks and referring domains that weren’t found after the specified date, but were present before, will be considered as lost; minimum value: 2019-01-30 maximum value shouldn’t exceed the date specified in the date_to date format: "yyyy-mm-dd" example: "2021-01-01" | Start of the time range (format `yyyy-mm-dd`); if omitted, the endpoint's minimum supported date is used. | 时间范围起始日期（格式 `yyyy-mm-dd`）；不填则使用该接口支持的最小日期。 |
| `req.date_to` | ending date of the time range optional field if you don’t specify this field, the today’s date will be used by default minimum value shouldn’t preceed the date specified in the date_from maximum value: today’s date date format: "yyyy-mm-dd" example: "2021-01-15" | End of the time range (format `yyyy-mm-dd`); if omitted, today's date is used. | 时间范围结束日期（格式 `yyyy-mm-dd`）；不填则默认到今天。 |
| `req.group_range` | time range which will be used to group the results optional field default value: month possible values: day, week, month, year note: for day, we will return items corresponding to all dates between and including date_from and date_to; for week/month/year, we will return items corresponding to full weeks/months/years, where each item will indicate the last day of the week/month/year for example, if you specify: "group_range": "month", "date_from": "2022-03-23", "date_to": "2022-05-13" we will return items falling between 2022-03-01 and 2022-05-31, namely, three items corresponding to the following dates: 2022-03-31, 2022-04-30, 2022-05-31 if there is no data for a certain day/week/month/year, we will return 0 | Bucket size for aggregating the timeseries: `day`, `week`, `month` or `year`; week/month/year items are stamped with the last day of each full period, and empty periods return 0. | 时间序列的聚合粒度：`day`、`week`、`month` 或 `year`；周/月/年的条目以各完整周期的最后一天标记，无数据的周期返回 0。 |
| `req.include_subdomains` | indicates if the subdomains of the target will be included in the search optional field if set to false, the subdomains will be ignored default value: true | When true, subdomains of the target are included in the search; set false to restrict analysis to the target host only. | 为 true 时把目标的子域名一并纳入检索；设为 false 则只分析目标主机本身。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.target` | target from a POST array | The target this result block refers to, echoing the requested domain/subdomain/page. | 该结果块所对应的目标，回显请求的域名/子域名/页面。 |
| `resp.200.tasks.result.date_from` | starting date of the time range in the UTC format: “yyyy-mm-dd” example: 2019-01-01 | Start date of the time range covered by this result. | 该结果覆盖时间范围的起始日期。 |
| `resp.200.tasks.result.date_to` | ending date of the time range in the UTC format: "yyyy-mm-dd" example: "2019-01-15" | End date of the time range covered by this result. | 该结果覆盖时间范围的结束日期。 |
| `resp.200.tasks.result.group_range` | group_range from the POST array | Bucket size used to aggregate the timeseries, echoing the request. | 时间序列聚合所用的粒度，回显请求。 |
| `resp.200.tasks.result.total_count` | total amount of results relevant the request | Total number of items matching the request, ignoring pagination. | 匹配请求的条目总数（不受分页影响）。 |
| `resp.200.tasks.result.items_count` | the number of results returned in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | contains relevant backlinks and referring domains data | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.type` | type of element = ‘backlinks_timeseries_new_lost_summary’ | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.items.date` | date and time when the data for the target was stored in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date this timeseries data point corresponds to. | 该时间序列数据点对应的日期。 |
| `resp.200.tasks.result.items.new_backlinks` | number of new backlinks number of new backlinks pointing to the target | Number of backlinks newly gained since `date_from`. | 自 `date_from` 起新增的外链数量。 |
| `resp.200.tasks.result.items.lost_backlinks` | number of lost backlinks number of lost backlinks of the target | Number of backlinks lost since `date_from`. | 自 `date_from` 起丢失的外链数量。 |
| `resp.200.tasks.result.items.new_referring_domains` | number of new referring domains number of new referring domains pointing to the target | Number of referring domains newly gained since `date_from`. | 自 `date_from` 起新增的引荐域名数量。 |
| `resp.200.tasks.result.items.lost_referring_domains` | number of lost referring domains number of lost referring domains of the target | Number of referring domains lost since `date_from`. | 自 `date_from` 起丢失的引荐域名数量。 |
| `resp.200.tasks.result.items.new_referring_main_domains` | number of new referring main domains number of new referring main domains pointing to the target | Number of referring main domains newly gained since `date_from`. | 自 `date_from` 起新增的引荐主域名数量。 |
| `resp.200.tasks.result.items.lost_referring_main_domains` | number of lost referring main domains number of lost referring main domains of the target | Number of referring main domains lost since `date_from`. | 自 `date_from` 起丢失的引荐主域名数量。 |

## post_dataforseo_backlinks_timeseries_summary_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with an overview of backlink data for the `target` domain available during a period between the two indicated dates. Backlink metrics will be grouped by the time range that you define: day, week, month, or year. | Returns a time series of a target's aggregated backlink metrics (rank, backlinks, referring domains/pages) bucketed by the chosen range. | 返回目标聚合外链指标（rank、外链、引荐域名/页面）的时间序列，按指定粒度分桶。 |
| `req.target` | domain to get data for required field a domain should be specified without https:// and www. example: "forbes.com" | Domain whose timeseries summary you want, specified without `http(s)://` and without `www.`. | 要查询时间序列汇总的域名，去掉 `http(s)://` 和 `www.` 填写。 |
| `req.date_from` | starting date of the time range optional field this field indicates the date which will be used as a threshold for summary data; minimum value: 2019-01-30 maximum value shouldn’t exceed the date specified in the date_to date format: "yyyy-mm-dd" example: "2021-01-01" | Start of the time range (format `yyyy-mm-dd`); if omitted, the endpoint's minimum supported date is used. | 时间范围起始日期（格式 `yyyy-mm-dd`）；不填则使用该接口支持的最小日期。 |
| `req.date_to` | ending date of the time range optional field if you don’t specify this field, the today’s date will be used by default minimum value shouldn’t preceed the date specified in the date_from maximum value: today’s date date format: "yyyy-mm-dd" example: "2021-01-15" | End of the time range (format `yyyy-mm-dd`); if omitted, today's date is used. | 时间范围结束日期（格式 `yyyy-mm-dd`）；不填则默认到今天。 |
| `req.group_range` | time range which will be used to group the results optional field default value: month possible values: day, week, month, year note: for day, we will return items corresponding to all dates between and including date_from and date_to; for week/month/year, we will return items corresponding to full weeks/months/years, where each item will indicate the last day of the week/month/year for example, if you specify: "group_range": "month", "date_from": "2022-03-23", "date_to": "2022-05-13" we will return items falling between 2022-03-01 and 2022-05-31, namely, three items corresponding to the following dates: 2022-03-31, 2022-04-30, 2022-05-31 if there is no data for a certain day/week/month/year, we will return 0 | Bucket size for aggregating the timeseries: `day`, `week`, `month` or `year`; week/month/year items are stamped with the last day of each full period, and empty periods return 0. | 时间序列的聚合粒度：`day`、`week`、`month` 或 `year`；周/月/年的条目以各完整周期的最后一天标记，无数据的周期返回 0。 |
| `req.include_subdomains` | indicates if the subdomains of the target will be included in the search optional field if set to false, the subdomains will be ignored default value: true | When true, subdomains of the target are included in the search; set false to restrict analysis to the target host only. | 为 true 时把目标的子域名一并纳入检索；设为 false 则只分析目标主机本身。 |
| `req.rank_scale` | defines the scale used for calculating and displaying the rank, domain_from_rank, and page_from_rank values optional field you can use this parameter to choose whether rank values are presented on a 0–100 or 0–1000 scale possible values: one_hundred — rank values are displayed on a 0–100 scale one_thousand — rank values are displayed on a 0–1000 scale default value: one_thousand learn more about how this parameter works and how ranking metrics are calculated in this Help Center article | Chooses the numeric scale for all rank-style metrics (`rank`, `domain_from_rank`, `page_from_rank`): `one_hundred` for a 0–100 scale or `one_thousand` for a 0–1000 scale. | 选择各类 rank 指标（`rank`、`domain_from_rank`、`page_from_rank`）的数值刻度：`one_hundred` 为 0–100，`one_thousand` 为 0–1000。 |
| `req.tag` | user-defined task identifier optional field the character limit is 255 you can use this parameter to identify the task and match it with the result you will find the specified tag value in the data object of the response | Free-text label you attach to the task to correlate it with its result; echoed back in the response `data` object. | 自定义任务标签，用于把任务与结果对应起来，会原样回显在响应的 `data` 对象中。 |
| `resp.200.version` | the current version of the API | DataForSEO API version that produced this response. | 生成该响应的 DataForSEO API 版本号。 |
| `resp.200.status_code` | general status code you can find the full list of the response codes here Note: we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.status_message` | general informational message you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.cost` | total tasks cost, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks_count` | the number of tasks in the tasks array | Number of tasks contained in this response. | 本响应包含的任务数量。 |
| `resp.200.tasks_error` | the number of tasks in the tasks array returned with an error | Number of tasks in this response that ended in error. | 本响应中执行出错的任务数量。 |
| `resp.200.tasks` | array of tasks | Array of task objects, one per requested task, each carrying its own status and result. | 任务对象数组，每个请求任务一项，各自带有状态与结果。 |
| `resp.200.tasks.id` | task identifier unique task identifier in our system in the UUID format | Unique identifier of the task, usable to retrieve it later. | 任务的唯一标识，可用于后续检索该任务。 |
| `resp.200.tasks.status_code` | status code of the task generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes here | Overall status code of the response; a 20000-series value indicates success. | 整个响应的状态码，20000 系列表示成功。 |
| `resp.200.tasks.status_message` | informational message of the task you can find the full list of general informational messages here | Human-readable message that accompanies the overall status code. | 与整体状态码对应的可读说明。 |
| `resp.200.tasks.time` | execution time, seconds | Total processing time for the response. | 该响应的总处理耗时。 |
| `resp.200.tasks.cost` | cost of the task, USD | Total cost in account credits charged for this response. | 本次响应消耗的账户额度费用。 |
| `resp.200.tasks.result_count` | number of elements in the result array | Number of result entries returned for this task. | 该任务返回的结果条数。 |
| `resp.200.tasks.path` | URL path | API endpoint path the task was routed to. | 该任务对应的 API 接口路径。 |
| `resp.200.tasks.data` | contains the same parameters that you specified in the POST request | Echo of the parameters this task was created with, including your `tag`. | 创建该任务时所用参数的回显，包含你设置的 `tag`。 |
| `resp.200.tasks.result` | array of results | Result payload for the task; its shape depends on the endpoint. | 任务的结果载荷，结构随接口而异。 |
| `resp.200.tasks.result.target` | target from a POST array | The target this result block refers to, echoing the requested domain/subdomain/page. | 该结果块所对应的目标，回显请求的域名/子域名/页面。 |
| `resp.200.tasks.result.date_from` | starting date of the time range in the UTC format: “yyyy-mm-dd” example: 2019-01-01 | Start date of the time range covered by this result. | 该结果覆盖时间范围的起始日期。 |
| `resp.200.tasks.result.date_to` | ending date of the time range in the UTC format: "yyyy-mm-dd" example: "2019-01-15" | End date of the time range covered by this result. | 该结果覆盖时间范围的结束日期。 |
| `resp.200.tasks.result.group_range` | group_range from a POST array | Bucket size used to aggregate the timeseries, echoing the request. | 时间序列聚合所用的粒度，回显请求。 |
| `resp.200.tasks.result.total_count` | total amount of results relevant the request | Total number of items matching the request, ignoring pagination. | 匹配请求的条目总数（不受分页影响）。 |
| `resp.200.tasks.result.items_count` | the number of results returned in the items array | Number of items actually returned in this `items` array. | 本次 `items` 数组实际返回的条目数。 |
| `resp.200.tasks.result.items` | contains relevant summary data | Array of result items; each element's fields depend on the endpoint. | 结果条目数组，元素字段随接口而异。 |
| `resp.200.tasks.result.items.type` | type of element = ‘backlinks_timeseries_summary’ | Type marker of the item, identifying which result schema it follows. | 条目类型标记，标识其遵循的结果结构。 |
| `resp.200.tasks.result.items.date` | date and time when the data for the target was stored in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: 2019-11-15 12:57:46 +00:00 | Date this timeseries data point corresponds to. | 该时间序列数据点对应的日期。 |
| `resp.200.tasks.result.items.rank` | target rank for the given date learn more about the metric and how it is calculated in this help center article | Backlink-based authority score of the entity, on the chosen `rank_scale`; higher means stronger link authority. | 该实体基于外链的权重评分，按所选 `rank_scale` 计算，越高表示链接权重越强。 |
| `resp.200.tasks.result.items.backlinks` | number of backlinks for the given date | Number of backlinks pointing to the entity. | 指向该实体的外链数量。 |
| `resp.200.tasks.result.items.backlinks_nofollow` | number of nofollow backlinks for the given date | Number of nofollow backlinks pointing to the entity. | 指向该实体的 nofollow 外链数量。 |
| `resp.200.tasks.result.items.referring_pages` | number of pages pointing to target for the given date | Number of distinct pages linking to the entity. | 链向该实体的不同页面数量。 |
| `resp.200.tasks.result.items.referring_domains` | number of referring domains for the given date referring domains include subdomains that are counted as separate domains for this metric | Number of distinct domains linking to the entity. | 链向该实体的不同域名数量。 |
| `resp.200.tasks.result.items.referring_domains_nofollow` | number of domains pointing at least one nofollow link to the target for the given date | Number of referring domains whose links are all nofollow. | 链接全为 nofollow 的引荐域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains` | number of referring main domains for the given date | Number of distinct main (root) domains linking to the entity. | 链向该实体的不同主（根）域名数量。 |
| `resp.200.tasks.result.items.referring_main_domains_nofollow` | number of main domains pointing at least one nofollow link to the target for the given date | Number of referring main domains whose links are all nofollow. | 链接全为 nofollow 的引荐主域名数量。 |
| `resp.200.tasks.result.items.referring_ips` | number of referring IP addresses for the given date number of IP addresses pointing to this page | Number of distinct IP addresses the backlinks originate from. | 外链来源的不同 IP 地址数量。 |
| `resp.200.tasks.result.items.referring_subnets` | number of referring subnetworks for the given date | Number of distinct subnets the backlinks originate from. | 外链来源的不同子网数量。 |
| `resp.200.tasks.result.items.referring_pages_nofollow` | number of referring pages pointing at least one nofollow link to the target for the given date | Number of referring pages whose links are all nofollow. | 链接全为 nofollow 的引荐页面数量。 |

