# 增强对照表（源英文 → 加强英文 → 中文本地化）

> spec 共 21 个接口，856 个对外字段/参数。逐字段对照「原文 → 加强 → 本地化」三态，判断改得更好还是更坏。

## get_dataforseo_business_data_google_extended_reviews_task_get_id

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | The returned results are specific to the indicated local establishment name, search engine, [location](https://docs.dataforseo.com/v3/business_data/google/locations.md) and [language](https://docs.dataforseo.com/v3/business_data/google/languages.md) parameters. We emulate set location and search engine with the highest accuracy so that the results you receive will match the actual search results for the specified parameters at the time of task setting. You can always check the returned results accessing the `check_url` in the Incognito mode to make sure the received data is entirely relevant. Note that user preferences, search history, and other personalized search factors are ignored by our system and thus would not be reflected in the returned results. | Fetch the results of a previously created Google Extended Reviews task by its id, returning the establishment's aggregated rating and the collected review entries, including reviews sourced from third-party sites. | 按任务 id 拉取此前创建的 Google 扩展评论任务结果，返回商家的综合评分及采集到的评论条目，并包含来自第三方站点的评论。 |
| `param:id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** you will be able to use it within **30 days** to request the results of the task at any time | UUID of the task whose results you want to fetch. The task remains retrievable for 30 days after creation. | 要拉取结果的任务 UUID。任务自创建起 30 天内可随时取回结果。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total *tasks* cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array that were returned an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the same parameters that you specified in the POST request* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.keyword` | *keyword received in a POST array* **keyword is returned with decoded %## (plus character ‘+’ will be decoded to a space character)** | Keyword echoed from the POST request, with percent-encoding decoded (a `+` becomes a space). | 从 POST 请求回显的关键词，已解码百分号转义（`+` 会被还原为空格）。 |
| `resp.200.result.type` | *search engine type in a POST array* | Search engine type echoed from the POST request. | 从 POST 请求回显的搜索引擎类型。 |
| `resp.200.result.se_domain` | *search engine domain in a POST array* | Search engine domain echoed from the POST request. | 从 POST 请求回显的搜索引擎域名。 |
| `resp.200.result.location_code` | *location code in a POST array* | Location code echoed from the POST request. | 从 POST 请求回显的地区代码。 |
| `resp.200.result.language_code` | *language code in a POST array* | Language code echoed from the POST request. | 从 POST 请求回显的语言代码。 |
| `resp.200.result.check_url` | *direct URL to search engine results* you can use it to make sure that we provided accurate results | Direct URL to the search engine results page, so you can verify the data against the live SERP. | 指向搜索引擎结果页的直达 URL，可用于将数据与实时 SERP 核对。 |
| `resp.200.result.datetime` | *date and time when the result was received* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | UTC timestamp when the result was collected, formatted `yyyy-mm-dd hh-mm-ss +00:00`. | 结果采集的时间（UTC），格式为 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.result.title` | *title of the ‘reviews’ element in SERP* the name of the local establishment for which the reviews are collected | Name of the local establishment that the 'reviews' SERP element collects reviews for. | SERP 中 'reviews' 元素所采集评论对应的本地商家名称。 |
| `resp.200.result.sub_title` | *subtitle of the ‘reviews’ element in SERP* additional information (e.g., address) on the ‘reviews’ element for which the reviews are collected | Supplementary information (such as the address) shown beneath the title of the 'reviews' element. | 'reviews' 元素标题下方展示的补充信息（如地址）。 |
| `resp.200.result.rating` | *rating of the corresponding local establishment* popularity rate based on reviews and displayed in SERP | Aggregated rating of the entity, as shown in the SERP. | 该实体的综合评分，与 SERP 中展示的一致。 |
| `resp.200.result.rating_type` | *type of rating* here you can find the following elements: `Max5`, `Percents`, `CustomMax` | Scale used to express the rating; the business meaning is: `Max5` rated out of 5, `Percents` expressed as a percentage, `CustomMax` rated against a custom maximum. | 评分所采用的标度，业务含义为：`Max5` 表示满分 5 分制，`Percents` 表示百分比制，`CustomMax` 表示自定义满分制。 |
| `resp.200.result.value` | *the average rating based on all reviews* | Average rating value computed from all reviews. | 基于全部评论计算出的平均评分值。 |
| `resp.200.result.votes_count` | *the number of votes* | Number of votes (ratings) the rating is based on. | 该评分所依据的投票（打分）数量。 |
| `resp.200.result.rating_max` | *the maximum value for a `rating_type`* | Maximum possible value for the given `rating_type` (for example 5 when the type is `Max5`). | 对应 `rating_type` 的最大可能取值（例如类型为 `Max5` 时为 5）。 |
| `resp.200.result.feature_id` | *the unique identifier of the ‘reviews’ element in SERP* learn more about the identifier in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Unique identifier of the 'reviews' SERP element (feature id). | SERP 中 'reviews' 元素的唯一标识（feature id）。 |
| `resp.200.result.place_id` | *unique identifier of a business location assigned by Google* learn more about the identifier in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Google-assigned unique identifier of the business location (place id). | Google 为该商家地点分配的唯一标识（place id）。 |
| `resp.200.result.cid` | *google-defined client id* unique id of a local establishment learn more about the identifier in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Google-defined client id (cid), a unique id of the local establishment. | Google 定义的客户端 id（cid），本地商家的唯一标识。 |
| `resp.200.result.reviews_count` | *the total number of reviews* | Total number of reviews available for the establishment. | 该商家可获取的评论总数。 |
| `resp.200.result.items_count` | *the number of reviews items in the results array* you can get more results by using the `depth` parameter when setting a task | Number of elements returned in the `items` array; raise the `depth` parameter at task creation to get more. | `items` 数组中返回的元素数量；在创建任务时调大 `depth` 参数可获取更多。 |
| `resp.200.items` | *found reviews* you can get more results by using the `depth` parameter when setting a task | Review entries collected for the establishment; raise `depth` at task creation to retrieve more. | 为该商家采集到的评论条目；创建任务时调大 `depth` 可获取更多。 |
| `resp.200.items.type` | *type of element = **‘images\_element’*** | Element type marker of the review entry. | 评论条目的元素类型标记。 |
| `resp.200.items.rank_group` | *position within a group of elements with identical `type` values* positions of elements with different `type` values are omitted from `rank_group` | Position of this entry within the group of elements sharing the same `type`. | 该条目在相同 `type` 元素分组内的位置。 |
| `resp.200.items.rank_absolute` | *absolute rank among all the listed reviews* absolute position among all reviews on the list | Absolute position of this review among all listed reviews. | 该评论在全部已列出评论中的绝对位置。 |
| `resp.200.items.position` | *the alignment of the review in SERP* can take the following values: `right` | Alignment of the review within the SERP layout. | 评论在 SERP 版式中的对齐位置。 |
| `resp.200.items.xpath` | *the [XPath](https://en.wikipedia.org/wiki/XPath) of the review* | XPath locating this review element within the parsed page. | 在解析页面中定位该评论元素的 XPath。 |
| `resp.200.items.review_text` | *the content of the review* | Body text of the review (auto-translated when applicable). | 评论正文内容（在适用时为自动翻译后的文本）。 |
| `resp.200.items.original_review_text` | *original content of the review* the original content of the review, no auto-translate applied | Review body in its original language, with no auto-translation applied. | 评论正文的原始语言版本，未做自动翻译。 |
| `resp.200.items.time_ago` | *the time of publication* indicates the time (in the ‘time ago’ format) when the review was listed | Relative publication time of the review in 'time ago' wording. | 评论发布时间的相对表述（'time ago' 格式）。 |
| `resp.200.items.timestamp` | *date and time when a review was published* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | Absolute publication time of the review in UTC, `yyyy-mm-dd hh-mm-ss +00:00`. | 评论发布的绝对时间（UTC），格式 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.items.rating` | *the rating score submitted by the reviewer* | Rating score given by the reviewer. | 该评论作者给出的评分。 |
| `resp.200.items.rating_type` | *the type of the rating* can take the following values: `Max5`, `CustomMax` | Scale used to express the rating; the business meaning is: `Max5` rated out of 5, `Percents` expressed as a percentage, `CustomMax` rated against a custom maximum. | 评分所采用的标度，业务含义为：`Max5` 表示满分 5 分制，`Percents` 表示百分比制，`CustomMax` 表示自定义满分制。 |
| `resp.200.items.value` | *the value of the rating* | Numeric value of the reviewer's rating. | 该评论作者评分的数值。 |
| `resp.200.items.votes_count` | *the amount of feedback* indicated the number of votes the review obtained | Number of helpfulness votes this review received. | 该评论获得的有用性投票数。 |
| `resp.200.items.rating_max` | *the maximum value for a `rating_type`* the maximum value for `Max5` is 5 | Maximum possible value for the given `rating_type` (for example 5 when the type is `Max5`). | 对应 `rating_type` 的最大可能取值（例如类型为 `Max5` 时为 5）。 |
| `resp.200.items.reviews_count` | *total number of reviews submitted by the reviewer* | Total number of reviews the reviewer has submitted overall. | 该评论作者累计提交的评论总数。 |
| `resp.200.items.photos_count` | *total number of photos submitted by the reviewer* | Total number of photos the reviewer has submitted overall. | 该评论作者累计提交的照片总数。 |
| `resp.200.items.local_guide` | *indicates whether the reviewer has a ‘local guide’ status* | Whether the reviewer holds Google 'Local Guide' status. | 该评论作者是否拥有 Google '本地向导'（Local Guide）身份。 |
| `resp.200.items.profile_name` | *profile name of the reviewer* | Display name of the reviewer's profile. | 评论作者的资料显示名称。 |
| `resp.200.items.profile_url` | *URL of the reviewer’s profile* | URL of the reviewer's profile. | 评论作者的资料页 URL。 |
| `resp.200.items.review_url` | *the URL of the review* | URL pointing directly to this review. | 直接指向该评论的 URL。 |
| `resp.200.items.profile_image_url` | *URL of the reviewer’s profile image* | URL of the reviewer's profile image. | 评论作者的头像图片 URL。 |
| `resp.200.items.owner_answer` | *text of the owner’s response* the owner’s response to the review | Owner's reply text to the review (auto-translated when applicable). | 商家所有者对该评论的回复文本（适用时为自动翻译）。 |
| `resp.200.items.original_owner_answer` | *original text of the owner’s response* the original response to the review, no auto-translate applied | Owner's reply in its original language, with no auto-translation applied. | 商家所有者回复的原始语言版本，未做自动翻译。 |
| `resp.200.items.owner_time_ago` | *publication time* indicates the time (in the ‘time ago’ format) when the owner submitted the response to the review | Relative time of the owner's reply in 'time ago' wording. | 商家回复时间的相对表述（'time ago' 格式）。 |
| `resp.200.items.owner_timestamp` | *date and time of the owner’s reply to the review* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | Absolute time of the owner's reply in UTC, `yyyy-mm-dd hh-mm-ss +00:00`. | 商家回复的绝对时间（UTC），格式 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.items.review_id` | *the unique identifier of a review on Google* example: `ChZDSUhNMG9nS0VJQ0FnSUMxbHFyMFlnEAE` | Unique identifier of the review on Google. | 该评论在 Google 上的唯一标识。 |
| `resp.200.items.images` | *images submitted by the reviewer* | Images attached to the review by the reviewer. | 评论作者随评论附上的图片。 |
| `resp.200.items.alt` | *alt tag of the image* | Alt text of the review image. | 评论图片的 alt 替代文本。 |
| `resp.200.items.image_url` | *URL of the image featured in the review* | URL of an image featured in the review. | 评论中所附图片的 URL。 |
| `resp.200.items.review_highlights` | *review highlights* contains highlighted review criteria and assessments | Highlighted review criteria together with their assessments. | 评论中被高亮的评价维度及其评价结论。 |
| `resp.200.items.feature` | *reviewed feature* | Reviewed feature or aspect. | 被评价的特性或维度。 |
| `resp.200.items.assessment` | *feature assessment* | Assessment given for the reviewed feature. | 针对被评价特性给出的评价结论。 |
| `resp.200.items.source` | *source of the review* contains information about the source where the review was posted | Information about the external source the review was posted on. | 该评论所发布外部来源的相关信息。 |
| `resp.200.items.title` | *name of the source where the review was posted* | Name of the source site where the review was posted. | 该评论所发布来源站点的名称。 |
| `resp.200.items.image` | *featured image of the source* | Featured image of the review source. | 评论来源的特色图片。 |
| `resp.200.items.domain` | *domain of the source where the review was posted* | Domain of the source site where the review was posted. | 该评论所发布来源站点的域名。 |

## get_dataforseo_business_data_google_extended_reviews_tasks_ready

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | The **‘Tasks Ready’** endpoint is designed to provide you with the list of completed tasks, which haven’t been collected yet. If you don’t use the `postback_url`, you can receive the list of `id` for all completed tasks using this endpoint. Then, you can collect the results using the **‘Task GET’** endpoint. | List Google Extended Reviews tasks that have finished processing and are ready to be collected via the matching task_get endpoint. | 列出已处理完成、可通过对应 task_get 端点拉取的 Google 扩展评论任务。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total tasks cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array returned with an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the parameters passed in the request’s URL* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.id` | *task identifier of the completed task* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | UUID of a completed task that is now ready to be collected; pass it to the matching `task_get` endpoint to download the results. | 已完成、可供拉取的任务的 UUID；将其传给对应的 `task_get` 端点即可下载结果。 |
| `resp.200.result.se` | *search engine specified when setting the task* can take the following values: `google` | Search engine the completed task was set for. | 该已完成任务所针对的搜索引擎。 |
| `resp.200.result.se_type` | *type of search engine* | Search engine type the completed task was set for. | 该已完成任务所针对的搜索引擎类型。 |
| `resp.200.result.date_posted` | *date when the task was posted (in the UTC format)* | Timestamp (UTC) when the task was originally submitted. | 任务最初提交的时间（UTC）。 |
| `resp.200.result.tag` | *user-defined task identifier* | User-defined label that was attached to the task when it was created, echoed back for matching. | 创建任务时附加的用户自定义标签，原样回显以便匹配。 |
| `resp.200.result.endpoint` | *URL for collecting the results of the task* | Relative endpoint URL to call for collecting this completed task's results. | 用于拉取该已完成任务结果的相对端点 URL。 |

## post_dataforseo_business_data_google_hotel_info_live_html

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | Google Hotel Info will provide you with unstructured HTML data available for a specific hotel entity on the [Google Hotels](http://www.google.com/travel/hotels) platform: such as service description, location details, rating, amenities, reviews, images, prices, and more. | Synchronously retrieve Google hotel info for a given hotel identifier and stay dates, returning the captured result as raw HTML pages in a single request. | 针对给定酒店标识与入住日期，同步获取 Google 酒店信息，并在单次请求中以原始 HTML 页面形式返回采集结果。 |
| `req.hotel_identifier` | *unique hotel identifier* **required field** unique identifier of a hotel entity in Google search; you can obtain the value by making a request to Advanced [Google SERP API](https://docs.dataforseo.com/v3/serp/google/organic/overview.md) (enclosed in the `hotels` element of the response), or the [Hotel Searches endpoint](https://docs.dataforseo.com/v3/business_data/google/hotel_searches/task_post.md) of Business Data API example: `ChYIq6SB--i6p6cpGgovbS8wN2s5ODZfEAE` | Unique identifier of a hotel entity in Google search. Obtain it from the Google SERP API `hotels` element or from the Hotel Searches endpoint. | Google 搜索中酒店实体的唯一标识。可从 Google SERP API 的 `hotels` 元素或 Hotel Searches 端点获取。 |
| `req.priority` | *task priority* optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the [Pricing](https://dataforseo.com/pricing/business-data/google-hotels-api "Pricing") page. | Execution priority of the task: `1` runs at normal priority, `2` runs at high priority for faster turnaround at an extra charge. | 任务执行优先级：`1` 为普通优先级，`2` 为高优先级，可更快返回但会额外计费。 |
| `req.location_name` | *full name of search engine location* **required field if you don’t specify `location_code` or `location_coordinate`** **if you use this field, you don’t need to specify `location_code` or `location_coordinate`** you can receive the list of available locations with `location_name` by making a separate request to `https://api.dataforseo.com/v3/business_data/google/locations` example: `London,England,United Kingdom` | Full location name (e.g. `London,England,United Kingdom`) that sets the geographic context of the search. Provide this or `location_code`/`location_coordinate`; the available list comes from the corresponding `/locations` endpoint. | 完整地区名称（如 `London,England,United Kingdom`），用于设定搜索的地理范围。需在此与 `location_code`/`location_coordinate` 中择一提供；可用列表来自对应的 `/locations` 端点。 |
| `req.location_code` | *search engine location code* **required field if you don’t specify `location_name` or `location_coordinate`** **if you use this field, you don’t need to specify `location_name` or `location_coordinate`** you can receive the list of available locations with `location_code` by making a separate request to the `https://api.dataforseo.com/v3/business_data/google/locations` example: `2840` | Numeric location code that sets the geographic context of the search. Provide this or `location_name`/`location_coordinate`; the available list comes from the corresponding `/locations` endpoint. | 数字地区代码，用于设定搜索的地理范围。需在此与 `location_name`/`location_coordinate` 中择一提供；可用列表来自对应的 `/locations` 端点。 |
| `req.location_coordinate` | *GPS coordinates of a location* **required field if you don’t specify `location_name` or `location_code`** **if you use this field, you don’t need to specify `location_name` or `location_code`** `location_coordinate` parameter should be specified in the *“latitude,longitude”* format the maximum number of decimal digits for *“latitude”* and *“longitude”*: 7 **Note**: if the coordinates are used to set a location, the search will occur in the nearest settlement; example: `53.476225,-2.243572` | GPS coordinates that set the geographic context of the search, in `latitude,longitude` (some endpoints also accept a radius). Provide this or `location_name`/`location_code`. | 用于设定搜索地理范围的 GPS 坐标，格式为 `latitude,longitude`（部分端点还接受半径）。需与 `location_name`/`location_code` 择一提供。 |
| `req.language_name` | *full name of search engine language* **required field if you don’t specify `language_code`** **if you use this field, you don’t need to specify `language_code`** you can receive the list of available languages with `language_name` by making a separate request to `https://api.dataforseo.com/v3/business_data/google/languages` example: `English` | Full language name (e.g. `English`) for the search. Provide this or `language_code`; the available list comes from the corresponding `/languages` endpoint. | 搜索所用语言的完整名称（如 `English`）。需与 `language_code` 择一提供；可用列表来自对应的 `/languages` 端点。 |
| `req.language_code` | *search engine language code* **required field if you don’t specify `language_name`** **if you use this field, you don’t need to specify `language_name`** you can receive the list of available languages with their `language_code` by making a separate request to `https://api.dataforseo.com/v3/business_data/google/languages` example: `en` | Language code (e.g. `en`) for the search. Provide this or `language_name`; the available list comes from the corresponding `/languages` endpoint. | 搜索所用的语言代码（如 `en`）。需与 `language_name` 择一提供；可用列表来自对应的 `/languages` 端点。 |
| `req.check_in` | *check-in date* optional field if you don’t specify this field, tomorrow’s date will be used by default; date format: `"yyyy-mm-dd"` example: `"2019-01-15"` | Check-in date in `yyyy-mm-dd`; if omitted, tomorrow's date is used. | 入住日期，格式 `yyyy-mm-dd`；不填则默认使用明天的日期。 |
| `req.check_out` | *check-out date* optional field if you don’t specify this field, our system will apply the date of two days from now by default; date format: `"yyyy-mm-dd"` example: `"2019-01-15"` | Check-out date in `yyyy-mm-dd`; if omitted, two days from now is used. | 退房日期，格式 `yyyy-mm-dd`；不填则默认使用后天的日期。 |
| `req.currency` | *currency* optional field example: `"USD"` | Currency for the returned prices, e.g. `USD`. | 返回价格所用的货币，例如 `USD`。 |
| `req.adults` | *number of adults* optional field if you don’t specify this field, two adults will be used by default example: `1` | Number of adult guests; if omitted, two adults are assumed. | 成人入住人数；不填则默认按两名成人计算。 |
| `req.children` | *number and age of children* optional field if you don’t specify this field, no children will be included in the search; set the following value if you want to include one 14-years-old child: `[14]` set the following value if you want to include one 13-years-old child and one 8-years-old child: `[13,8]` | Children to include, as an array of their ages (e.g. `[13,8]`); if omitted, no children are included. | 随行儿童，以年龄数组表示（如 `[13,8]`）；不填则不计入儿童。 |
| `req.tag` | *user-defined task identifier* optional field *the character limit is 255* you can use this parameter to identify the task and match it with the result you will find the specified `tag` value in the `data` array of the response | Your own label for the task (up to 255 characters). It is echoed back in the response `data` so you can match results to requests. | 你为任务自定义的标签（最多 255 个字符）。会在响应的 `data` 中回显，便于将结果与请求匹配。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total *tasks* cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array that were returned an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the same parameters that you specified in the POST request* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.keyword` | *unique hotel identifier specified as `"hotel_id:$"`* | Hotel identifier echoed for this result, formatted `hotel_id:$`. | 本结果回显的酒店标识，格式为 `hotel_id:$`。 |
| `resp.200.result.type` | *type of search results* in this case, `"hotel_info"` | Result type, here `hotel_info`. | 结果类型，此处为 `hotel_info`。 |
| `resp.200.result.se_domain` | *search engine domain in a POST array* | Search engine domain echoed from the POST request. | 从 POST 请求回显的搜索引擎域名。 |
| `resp.200.result.location_code` | *location code in a POST array* | Location code echoed from the POST request. | 从 POST 请求回显的地区代码。 |
| `resp.200.result.language_code` | *language code in a POST array* | Language code echoed from the POST request. | 从 POST 请求回显的语言代码。 |
| `resp.200.result.datetime` | *date and time when the result was received* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | UTC timestamp when the result was collected, formatted `yyyy-mm-dd hh-mm-ss +00:00`. | 结果采集的时间（UTC），格式为 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.result.items_count` | *the number of results returned in the **`items`** array* | Number of HTML page elements returned in the `items` array. | `items` 数组中返回的 HTML 页面元素数量。 |
| `resp.200.items` | *HTML pages* | Raw HTML pages captured for the hotel info result. | 为该酒店信息结果抓取到的原始 HTML 页面。 |
| `resp.200.items.page` | *serial number of the returned HTML page* | Sequential number of the returned HTML page. | 返回 HTML 页面的序号。 |
| `resp.200.items.date` | *date and time when the HTML page was scanned* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | UTC timestamp when the HTML page was scanned, `yyyy-mm-dd hh-mm-ss +00:00`. | 该 HTML 页面被抓取的时间（UTC），格式 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.items.html` | *HTML page* | Full HTML markup of the captured page. | 抓取页面的完整 HTML 源码。 |

## get_dataforseo_business_data_google_hotel_info_task_get_html_id

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | HTML Google Hotel Info will provide you with unstructured HTML data available for a specific hotel entity on the [Google Hotels](http://www.google.com/travel/hotels) platform. | Fetch the HTML results of a previously created advanced Google Hotel Info task by its id. | 按任务 id 拉取此前创建的高级 Google 酒店信息任务的 HTML 结果。 |
| `param:id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** you will be able to use it within **7 days** to request the results of the task at any time | UUID of the task whose results you want to fetch. The task remains retrievable for 7 days after creation. | 要拉取结果的任务 UUID。任务自创建起 7 天内可随时取回结果。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total *tasks* cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.result_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array that were returned an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the same parameters that you specified in the POST request* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.hotel_identifier` | *identifier received in a POST array* this field will contain the `hotel_identifier` parameter specified when setting a task; example: `CgoI-KWyzenM_MV3EAE` | Hotel identifier echoed from the POST request that created the task. | 从创建任务的 POST 请求回显的酒店标识。 |
| `resp.200.result.location_code` | *location code in a POST array* | Location code echoed from the POST request. | 从 POST 请求回显的地区代码。 |
| `resp.200.result.language_code` | *language code in a POST array* | Language code echoed from the POST request. | 从 POST 请求回显的语言代码。 |
| `resp.200.result.check_url` | *direct URL to search engine results* you can use it to make sure that we provided accurate results | Direct URL to the search engine results page, so you can verify the data against the live SERP. | 指向搜索引擎结果页的直达 URL，可用于将数据与实时 SERP 核对。 |
| `resp.200.result.datetime` | *date and time when the result was received* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | UTC timestamp when the result was collected, formatted `yyyy-mm-dd hh-mm-ss +00:00`. | 结果采集的时间（UTC），格式为 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.result.items_count` | *the number of results returned in the **`items`** array* | Number of HTML page elements returned in the `items` array. | `items` 数组中返回的 HTML 页面元素数量。 |
| `resp.200.items` | *HTML pages* | Raw HTML pages captured for the hotel info result. | 为该酒店信息结果抓取到的原始 HTML 页面。 |
| `resp.200.items.page` | *serial number of the returned HTML page* | Sequential number of the returned HTML page. | 返回 HTML 页面的序号。 |
| `resp.200.items.date` | *date and time when the HTML page was scanned* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | UTC timestamp when the HTML page was scanned, `yyyy-mm-dd hh-mm-ss +00:00`. | 该 HTML 页面被抓取的时间（UTC），格式 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.items.html` | *HTML page* | Full HTML markup of the captured page. | 抓取页面的完整 HTML 源码。 |

## get_dataforseo_business_data_google_hotel_info_tasks_ready

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | The **‘Tasks Ready’** endpoint is designed to provide you with the list of completed tasks, which haven’t been collected yet. If you don’t use the `postback_url`, you can receive the list of `id` for all completed tasks using this endpoint. Then, you can collect the results using the **‘Task GET’** endpoint. | List Google Hotel Info tasks that have finished processing and are ready to be collected. | 列出已处理完成、可供拉取的 Google 酒店信息任务。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total tasks cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array returned with an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the parameters passed in the request’s URL* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.id` | *task identifier of the completed task* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | UUID of a completed task that is now ready to be collected; pass it to the matching `task_get` endpoint to download the results. | 已完成、可供拉取的任务的 UUID；将其传给对应的 `task_get` 端点即可下载结果。 |
| `resp.200.result.se` | *search engine specified when setting the task* can take the following values: `google` | Search engine the completed task was set for. | 该已完成任务所针对的搜索引擎。 |
| `resp.200.result.se_type` | *search engine specified when setting the task* | Search engine type the completed task was set for. | 该已完成任务所针对的搜索引擎类型。 |
| `resp.200.result.date_posted` | *date when the task was posted (in the UTC format)* | Timestamp (UTC) when the task was originally submitted. | 任务最初提交的时间（UTC）。 |
| `resp.200.result.tag` | *user-defined task identifier* | User-defined label that was attached to the task when it was created, echoed back for matching. | 创建任务时附加的用户自定义标签，原样回显以便匹配。 |
| `resp.200.result.endpoint` | *URL for collecting the results of the task* | Relative endpoint URL to call for collecting this completed task's results. | 用于拉取该已完成任务结果的相对端点 URL。 |

## get_dataforseo_business_data_google_hotel_searches_task_get_id

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | Hotel Searches API will provide you with a list of hotels and including prices, images, rating data, and more for each hotel. Using different parameters in a POST array you can get a filtered list of hotels according to the supported parameters. | Fetch the results of a previously created Google Hotel Searches task by its id, returning matched hotel listings with pricing, location and rating data. | 按任务 id 拉取此前创建的 Google 酒店搜索任务结果，返回匹配的酒店列表及其价格、位置与评分数据。 |
| `param:id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** you will be able to use it within **30 days** to request the results of the task at any time | UUID of the task whose results you want to fetch. The task remains retrievable for 30 days after creation. | 要拉取结果的任务 UUID。任务自创建起 30 天内可随时取回结果。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total *tasks* cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array that were returned an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the same parameters that you specified in the POST request* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.keyword` | *keyword received in a POST array* keyword is returned with decoded %## (plus character ‘+’ will be decoded to a space character); in order to obtain accurate search results, the location name is appended to the keyword automatically | Keyword echoed from the POST request (percent-encoding decoded); the location name is automatically appended to improve accuracy. | 从 POST 请求回显的关键词（已解码百分号转义）；系统会自动追加地区名以提高准确度。 |
| `resp.200.result.location_code` | *location code in a POST array* | Location code echoed from the POST request. | 从 POST 请求回显的地区代码。 |
| `resp.200.result.language_code` | *language code in a POST array* | Language code echoed from the POST request. | 从 POST 请求回显的语言代码。 |
| `resp.200.result.check_url` | *direct URL to search engine results* you can use it to make sure that we provided accurate results | Direct URL to the search engine results page, so you can verify the data against the live SERP. | 指向搜索引擎结果页的直达 URL，可用于将数据与实时 SERP 核对。 |
| `resp.200.result.datetime` | *date and time when the result was received* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | UTC timestamp when the result was collected, formatted `yyyy-mm-dd hh-mm-ss +00:00`. | 结果采集的时间（UTC），格式为 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.result.items_count` | *item types* the number of items in the `items` array | Number of items in the `items` array. | `items` 数组中的元素数量。 |
| `resp.200.items` | *encountered item types* types of search engine results encountered in the `items` array; possible item types: `hotel_search_item` | Hotel search listing entries; possible item type: `hotel_search_item`. | 酒店搜索列表条目；可能的元素类型为 `hotel_search_item`。 |
| `resp.200.items.type` | *type of element = **‘hotel\_search\_item’*** | Element type marker, here `hotel_search_item`. | 元素类型标记，此处为 `hotel_search_item`。 |
| `resp.200.items.hotel_identifier` | *unique identifier of a hotel entity in Google search* example: `CgoI-KWyzenM_MV3EAE` | Unique identifier of the hotel entity in Google search; reuse it with the Hotel Info endpoint. | Google 搜索中该酒店实体的唯一标识；可配合 Hotel Info 端点使用。 |
| `resp.200.items.title` | *title of the hotel* | Hotel name. | 酒店名称。 |
| `resp.200.items.stars` | *hotel class rating* class rating that ranges between 1-5 stars | Hotel class rating, ranging from 1 to 5 stars. | 酒店星级评定，范围 1 至 5 星。 |
| `resp.200.items.is_paid` | *indicates a paid hotel listing* if `true`, related `hotel_search_item` is a paid ad if `false`, related `hotel_search_item` is an organic hotel listing | Whether the listing is a paid ad (`true`) or an organic hotel listing (`false`). | 该条目是付费广告（`true`）还是自然酒店列表（`false`）。 |
| `resp.200.items.location` | *GPS coordinates of the hotel’s location* | GPS coordinates of the hotel's location. | 酒店所在位置的 GPS 坐标。 |
| `resp.200.items.latitude` | *latitude coordinate of the hotel in google maps* example: `"latitude": 51.584091` | Latitude of the hotel on Google Maps. | 酒店在 Google 地图上的纬度。 |
| `resp.200.items.longitude` | *longitude coordinate of the hotel in google maps* example: `"longitude": -0.31365919999999997` | Longitude of the hotel on Google Maps. | 酒店在 Google 地图上的经度。 |
| `resp.200.items.reviews` | *hotel reviews and rating information* | Hotel review and rating information. | 酒店的评论与评分信息。 |
| `resp.200.items.value` | *the average rating based on all reviews* | Average rating based on all reviews. | 基于全部评论的平均评分。 |
| `resp.200.items.votes_count` | *the number of votes* | Number of votes the rating is based on. | 该评分所依据的投票数。 |
| `resp.200.items.mentions` | *hotel mentions* **note:** this field always equals `null`; use it to facilitate integration and ensure interoperability with the [Hotel Info](https://docs.dataforseo.com/v3/business_data/google/hotel_info/live/advanced.md) endpoint | Hotel mentions. This field is always `null` here and exists only for interoperability with the Hotel Info endpoint. | 酒店提及信息。此处该字段恒为 `null`，仅为与 Hotel Info 端点保持结构互通而存在。 |
| `resp.200.items.rating_distribution` | *rating distribution by votes* **note:** this field always equals `null`; use it to facilitate integration and ensure interoperability with the [Hotel Info](https://docs.dataforseo.com/v3/business_data/google/hotel_info/live/advanced.md) endpoint | Rating distribution by votes. This field is always `null` here and exists only for interoperability with the Hotel Info endpoint. | 按投票统计的评分分布。此处该字段恒为 `null`，仅为与 Hotel Info 端点保持结构互通而存在。 |
| `resp.200.items.other_sites_reviews` | *reviews on third-party sites* **note:** this field always equals `null`; use it to facilitate integration and ensure interoperability with the [Hotel Info](https://docs.dataforseo.com/v3/business_data/google/hotel_info/live/advanced.md) endpoint | Reviews on third-party sites. This field is always `null` here and exists only for interoperability with the Hotel Info endpoint. | 第三方站点上的评论。此处该字段恒为 `null`，仅为与 Hotel Info 端点保持结构互通而存在。 |
| `resp.200.items.overview_images` | *featured images for a hotel* | Featured overview images of the hotel. | 酒店的概览特色图片。 |
| `resp.200.items.prices` | *hotel price* | Hotel pricing block. | 酒店价格信息块。 |
| `resp.200.items.price` | *price per night* | Price per night. | 每晚价格。 |
| `resp.200.items.price_without_discount` | *full price per night without a discount applied* | Full price per night before any discount. | 未应用折扣前的每晚原价。 |
| `resp.200.items.currency` | *price currency* `USD` is applied by default, unless specified in the POST array | Currency of the price; defaults to `USD` unless otherwise set in the POST request. | 价格所用货币；除非在 POST 请求中另行指定，否则默认为 `USD`。 |
| `resp.200.items.discount_text` | *text about a discount applied* | Text describing the discount applied. | 描述所应用折扣的文本。 |
| `resp.200.items.check_in` | *check-in date and time* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | Check-in date and time in UTC, `yyyy-mm-dd hh-mm-ss +00:00`. | 入住日期与时间（UTC），格式 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.items.check_out` | *check-out date and time* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | Check-out date and time in UTC, `yyyy-mm-dd hh-mm-ss +00:00`. | 退房日期与时间（UTC），格式 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.items.visitors` | *number of hotel visitors for this price* | Number of hotel visitors associated with this price. | 与该价格关联的酒店入住人数。 |
| `resp.200.items.items` | *array of items* **note:** this field always equals `null`; use it to facilitate integration and ensure interoperability with the [Hotel Info](https://docs.dataforseo.com/v3/business_data/google/hotel_info/live/advanced.md) endpoint | Nested items array. This field is always `null` here and exists only for interoperability with the Hotel Info endpoint. | 嵌套的 items 数组。此处该字段恒为 `null`，仅为与 Hotel Info 端点保持结构互通而存在。 |

## get_dataforseo_business_data_google_hotel_searches_tasks_ready

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | The **‘Tasks Ready’** endpoint is designed to provide you with the list of completed tasks, which haven’t been collected yet. If you don’t use the `postback_url`, you can receive the list of `id` for all completed tasks using this endpoint. Then, you can collect the results using the **‘Task GET’** endpoint. | List Google Hotel Searches tasks that have finished processing and are ready to be collected. | 列出已处理完成、可供拉取的 Google 酒店搜索任务。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total tasks cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array returned with an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the parameters passed in the request’s URL* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.id` | *task identifier of the completed task* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | UUID of a completed task that is now ready to be collected; pass it to the matching `task_get` endpoint to download the results. | 已完成、可供拉取的任务的 UUID；将其传给对应的 `task_get` 端点即可下载结果。 |
| `resp.200.result.se` | *search engine specified when setting the task* can take the following values: `google` | Search engine the completed task was set for. | 该已完成任务所针对的搜索引擎。 |
| `resp.200.result.se_type` | *search engine type* | Search engine type the completed task was set for. | 该已完成任务所针对的搜索引擎类型。 |
| `resp.200.result.date_posted` | *date when the task was posted (in the UTC format)* | Timestamp (UTC) when the task was originally submitted. | 任务最初提交的时间（UTC）。 |
| `resp.200.result.tag` | *user-defined task identifier* | User-defined label that was attached to the task when it was created, echoed back for matching. | 创建任务时附加的用户自定义标签，原样回显以便匹配。 |
| `resp.200.result.endpoint` | *URL for collecting the results of the task* | Relative endpoint URL to call for collecting this completed task's results. | 用于拉取该已完成任务结果的相对端点 URL。 |

## get_dataforseo_business_data_google_my_business_info_task_get_id

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | My Business Info API will provide you with public details of any business entity with Google My Business profile, including its service description, address, contact phone, domain, rating, and work hours. | Fetch the results of a previously created Google My Business Info task by its id, returning the full business profile: categories, address, contacts, ratings, attributes, working and popular hours, and any directory sub-listings. | 按任务 id 拉取此前创建的 Google 商家信息任务结果，返回完整的商家档案：分类、地址、联系方式、评分、属性、营业与热门时段，以及目录子条目。 |
| `param:id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** you will be able to use it within **30 days** to request the results of the task at any time | UUID of the task whose results you want to fetch. The task remains retrievable for 30 days after creation. | 要拉取结果的任务 UUID。任务自创建起 30 天内可随时取回结果。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total *tasks* cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array that were returned an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the same parameters that you specified in the POST request* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.keyword` | *keyword received in a POST array* **keyword is returned with decoded %## (plus character ‘+’ will be decoded to a space character)** this field will contain the `cid` parameter if you specified it in the `keyword` field when setting a task; example: `cid:2946633002421908862` learn more about the parameter in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Keyword echoed from the POST request (percent-encoding decoded); if you passed a `cid` in the keyword field, it is returned here as `cid:...`. | 从 POST 请求回显的关键词（已解码百分号转义）；若你在关键词字段中传入了 `cid`，此处会以 `cid:...` 形式返回。 |
| `resp.200.result.se_domain` | *search engine domain as specified in a POST array* | Search engine domain echoed from the POST request. | 从 POST 请求回显的搜索引擎域名。 |
| `resp.200.result.location_code` | *location code in a POST array* | Location code echoed from the POST request. | 从 POST 请求回显的地区代码。 |
| `resp.200.result.language_code` | *language code in a POST array* | Language code echoed from the POST request. | 从 POST 请求回显的语言代码。 |
| `resp.200.result.check_url` | *direct URL to search engine results* you can use it to make sure that we provided accurate results | Direct URL to the search engine results page, so you can verify the data against the live SERP. | 指向搜索引擎结果页的直达 URL，可用于将数据与实时 SERP 核对。 |
| `resp.200.result.datetime` | *date and time when the result was received* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | UTC timestamp when the result was collected, formatted `yyyy-mm-dd hh-mm-ss +00:00`. | 结果采集的时间（UTC），格式为 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.result.item_types` | *item types* types of search engine results encountered in the `items` array; possible item types: `google_business_info` | Item types present in the `items` array; possible value: `google_business_info`. | `items` 数组中出现的元素类型；可能取值为 `google_business_info`。 |
| `resp.200.result.items_count` | *item types* the number of items in the `items` array | Number of items in the `items` array. | `items` 数组中的元素数量。 |
| `resp.200.items` | *encountered item types* types of search engine results encountered in the `items` array; possible item types: `google_business_info` | Google My Business info entries; possible item type: `google_business_info`. | Google 商家信息条目；可能的元素类型为 `google_business_info`。 |
| `resp.200.items.type` | *type of element = **‘google\_business\_info’*** | Element type marker, here `google_business_info`. | 元素类型标记，此处为 `google_business_info`。 |
| `resp.200.items.rank_group` | *position within a group of elements with identical `type` values* positions of elements with different `type` values are omitted from `rank_group` | Position of this entry within the group of elements sharing the same `type`. | 该条目在相同 `type` 元素分组内的位置。 |
| `resp.200.items.rank_absolute` | *absolute rank among all the elements* | Absolute rank among all the elements. | 在全部元素中的绝对排名。 |
| `resp.200.items.position` | *the alignment in SERP* | Alignment of the element within the SERP. | 元素在 SERP 中的对齐位置。 |
| `resp.200.items.title` | *title of the element in SERP* the name of the business entity for which the results are collected | Business name shown in the SERP element. | SERP 元素中展示的商家名称。 |
| `resp.200.items.original_title` | *original title of the element* original title not translated by Google | Business title in its original language, not translated by Google. | 商家标题的原始语言版本，未经 Google 翻译。 |
| `resp.200.items.description` | *description of the element in SERP* the description of the business entity for which the results are collected | Description of the business entity shown in the SERP. | SERP 中展示的商家描述。 |
| `resp.200.items.category` | *business category* Google My Business general category that best describes the services provided by the business entity | Primary Google My Business category that best describes the business. | 最能概括该商家的 Google 商家主分类。 |
| `resp.200.items.category_ids` | *global category IDs* universal category IDs that do not change based on the selected country | Universal category IDs that stay constant regardless of country. | 通用分类 ID，不随所选国家变化。 |
| `resp.200.items.additional_categories` | *additional business categories* additional Google My Business categories that describe the services provided by the business entity in more detail | Additional Google My Business categories describing the business in more detail. | 更细致描述该商家的附加 Google 商家分类。 |
| `resp.200.items.cid` | *google-defined client id* unique id of a local establishment learn more about the identifier in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Google-defined client id (cid), unique id of the local establishment. | Google 定义的客户端 id（cid），本地商家的唯一标识。 |
| `resp.200.items.feature_id` | *the unique identifier of the element in SERP* learn more about the identifier in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Unique identifier of the element in the SERP (feature id). | SERP 中该元素的唯一标识（feature id）。 |
| `resp.200.items.address` | *street address of the business entity* | Street address of the business. | 商家的街道地址。 |
| `resp.200.items.address_info` | *object containing address components of the business entity* | Object holding the structured address components of the business. | 包含商家结构化地址组成部分的对象。 |
| `resp.200.items.borough` | *administrative unit or district the business entity location belongs to* | Administrative unit or district the business location belongs to. | 商家所在的行政区或辖区。 |
| `resp.200.items.city` | *name of the city where the business entity is located* | City where the business is located. | 商家所在城市。 |
| `resp.200.items.zip` | *ZIP code of the business entity* | Postal/ZIP code of the business. | 商家的邮政编码。 |
| `resp.200.items.region` | *DMA region of the business entity location* | DMA region of the business location. | 商家所在的 DMA 区域。 |
| `resp.200.items.country_code` | *ISO country code of the business entity location* | ISO country code of the business location. | 商家所在地的 ISO 国家代码。 |
| `resp.200.items.place_id` | *unique place identifier* [place id](https://developers.google.com/places/place-id) of the local establishment featured in the element learn more about the identifier in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Google place id of the local establishment featured in the element. | 元素中所示本地商家的 Google place id。 |
| `resp.200.items.phone` | *phone number of the business entity* | Phone number of the business. | 商家的电话号码。 |
| `resp.200.items.url` | *absolute url of the business entity* | Absolute URL of the business website. | 商家网站的绝对 URL。 |
| `resp.200.items.contact_url` | *URL of the preferred contact page* | URL of the preferred contact page. | 首选联系页面的 URL。 |
| `resp.200.items.contributor_url` | *URL of the user’s or entity’s Local Guides profile, if available* | URL of the user's or entity's Local Guides profile, when available. | 用户或实体的 Local Guides 资料页 URL（如有）。 |
| `resp.200.items.book_online_url` | *URL in the ‘book online’ button of the element* URL directing users to the online booking or order page of the business entity | URL behind the element's 'book online' button, leading to the booking/order page. | 元素中 'book online' 按钮指向的 URL，通往预订/下单页面。 |
| `resp.200.items.domain` | *domain of the business entity* | Domain of the business website. | 商家网站的域名。 |
| `resp.200.items.logo` | *URL of the logo featured in Google My Business profile* | URL of the logo shown on the Google My Business profile. | Google 商家资料中展示的 logo URL。 |
| `resp.200.items.main_image` | *URL of the main image featured in Google My Business profile* | URL of the main image shown on the Google My Business profile. | Google 商家资料中展示的主图 URL。 |
| `resp.200.items.total_photos` | *total count of images featured in Google My Business profile* | Total number of images featured on the Google My Business profile. | Google 商家资料中收录的图片总数。 |
| `resp.200.items.snippet` | *additional information on the business entity* | Additional descriptive information about the business. | 关于该商家的附加描述信息。 |
| `resp.200.items.latitude` | *latitude coordinate of the local establishments in google maps* example: `"latitude": 51.584091` | Latitude of the establishment on Google Maps. | 商家在 Google 地图上的纬度。 |
| `resp.200.items.longitude` | *longitude coordinate of the local establishment in google maps* example: `"longitude": -0.31365919999999997` | Longitude of the establishment on Google Maps. | 商家在 Google 地图上的经度。 |
| `resp.200.items.is_claimed` | *shows whether the entity is verified by its owner on Google Maps* | Whether the entity is verified by its owner on Google Maps. | 该实体是否已由其所有者在 Google 地图上认领验证。 |
| `resp.200.items.attributes` | *service details in a form of user-reviewed checks;* service details of a business entity displayed in a form of checks and based on user feedback and business `category` | Service details presented as user-reviewed checks, based on user feedback and the business category. | 以用户核验勾选形式呈现的服务详情，依据用户反馈与商家分类生成。 |
| `resp.200.items.available_attributes` | *available attributes* indicates attributes a business entity can offer | Attributes the business is indicated to offer. | 标识为该商家提供的属性。 |
| `resp.200.items.unavailable_attributes` | *unavailable attributes* indicates attributes a business entity cannot offer | Attributes the business is indicated not to offer. | 标识为该商家不提供的属性。 |
| `resp.200.items.place_topics` | *keywords mentioned in customer reviews* contains most popular keywords related to products/services mentioned in customer reviews of a business entity and the number of reviews mentioning each keyword example: `"place_topics": {"egg roll": 48,"birthday": 33}` | Most popular keywords mentioned in customer reviews, each mapped to how many reviews mention it. | 客户评论中最常被提及的关键词，每个关键词对应其被提及的评论数。 |
| `resp.200.items.rating` | *the element’s rating* the popularity rate based on reviews and displayed in SERP | Aggregated rating of the entity, as shown in the SERP. | 该实体的综合评分，与 SERP 中展示的一致。 |
| `resp.200.items.rating_type` | *the type of rating* here you can find the following elements: `Max5`, `Percents`, `CustomMax` | Scale used to express the rating; the business meaning is: `Max5` rated out of 5, `Percents` expressed as a percentage, `CustomMax` rated against a custom maximum. | 评分所采用的标度，业务含义为：`Max5` 表示满分 5 分制，`Percents` 表示百分比制，`CustomMax` 表示自定义满分制。 |
| `resp.200.items.value` | *the value of the rating* | Numeric value of the rating. | 评分的数值。 |
| `resp.200.items.votes_count` | *the amount of feedback* | Number of votes the rating is based on. | 该评分所依据的投票数。 |
| `resp.200.items.rating_max` | *the maximum value for a `rating_type`* | Maximum possible value for the given `rating_type` (for example 5 when the type is `Max5`). | 对应 `rating_type` 的最大可能取值（例如类型为 `Max5` 时为 5）。 |
| `resp.200.items.rating_distribution` | *the distribution of ratings of the business entity* the object displays the number of 1-star to 5-star ratings, as reviewed by users | Breakdown of how many 1- to 5-star ratings the business received. | 该商家所获 1 至 5 星评分各自数量的分布。 |
| `resp.200.items.1` | *the number of 1-star ratings* | Number of 1-star ratings. | 1 星评分的数量。 |
| `resp.200.items.2` | *the number of 2-star ratings* | Number of 2-star ratings. | 2 星评分的数量。 |
| `resp.200.items.3` | *the number of 3-star ratings* | Number of 3-star ratings. | 3 星评分的数量。 |
| `resp.200.items.4` | *the number of 4-star ratings* | Number of 4-star ratings. | 4 星评分的数量。 |
| `resp.200.items.5` | *the number of 5-star ratings* | Number of 5-star ratings. | 5 星评分的数量。 |
| `resp.200.items.people_also_search` | *related business entities* | Related business entities that users also searched for. | 用户还会搜索的相关商家实体。 |
| `resp.200.items.work_time` | *work time details* information related to operational hours of the business entity | Operating-hours details of the business. | 商家营业时间的相关详情。 |
| `resp.200.items.work_hours` | *open hours* information about work hours of the local establishment | Working hours information of the establishment. | 商家的营业时间信息。 |
| `resp.200.items.timetable` | *work hours timetable* | Weekly opening-hours timetable. | 每周营业时间表。 |
| `resp.200.items.sunday` | *work hours on Sundays* | Working hours on Sundays. | 周日的营业时间。 |
| `resp.200.items.open` | *opening time* | Opening time entry. | 开始营业时间。 |
| `resp.200.items.hour` | *hours in the 24-hour format* | Hour component in 24-hour format. | 小时（24 小时制）。 |
| `resp.200.items.minute` | *minutes* | Minute component. | 分钟。 |
| `resp.200.items.close` | *closing time* | Closing time entry. | 结束营业时间。 |
| `resp.200.items.monday` | *work hours on Mondays* | Working hours on Mondays. | 周一的营业时间。 |
| `resp.200.items.tuesday` | *work hours on Tuesdays* | Working hours on Tuesdays. | 周二的营业时间。 |
| `resp.200.items.wednesday` | *work hours on Wednesdays* | Working hours on Wednesdays. | 周三的营业时间。 |
| `resp.200.items.thursday` | *work hours on Thursdays* | Working hours on Thursdays. | 周四的营业时间。 |
| `resp.200.items.friday` | *work hours on Fridays* | Working hours on Fridays. | 周五的营业时间。 |
| `resp.200.items.saturday` | *work hours on Saturday* | Working hours on Saturdays. | 周六的营业时间。 |
| `resp.200.items.current_status` | *current status of the establishment* possible values: `opened`, `closed`, `temporarily_closed`, `closed_forever` | Current operating status of the establishment; business meaning of values: `opened`, `closed`, `temporarily_closed`, `closed_forever`. | 商家当前的营业状态；取值业务含义：`opened` 营业中、`closed` 已关门、`temporarily_closed` 暂停营业、`closed_forever` 永久关闭。 |
| `resp.200.popular_times` | *popular times* information related to busy hours of the business entity | Popularity-by-hour information indicating the busy hours of the business. | 按小时统计的热门时段信息，反映商家的繁忙时间。 |
| `resp.200.popular_times.popular_times_by_days` | *popular hours* information about busy hours of the local establishment on each day of the week | Busy-hours breakdown for each day of the week. | 一周内每天的繁忙时段明细。 |
| `resp.200.popular_times.sunday` | *busy hours on sunday* **can take values of the corresponding days of the week** | Busy hours on Sunday; the same structure applies to the other weekdays. | 周日的繁忙时段；其余各工作日采用相同结构。 |
| `resp.200.popular_times.time` | *busy hours* | Busy-hour time entry. | 繁忙时段的时间项。 |
| `resp.200.popular_times.hour` | *hours in a 24-hour format* | Hour component in 24-hour format. | 小时（24 小时制）。 |
| `resp.200.popular_times.minute` | *minutes* | Minute component. | 分钟。 |
| `resp.200.popular_times.popular_index` | *popularity index* relative time-bound popularity index measured from `0` to `100`; higher value corresponds to a busier time of a day | Relative popularity index from 0 to 100; higher means a busier time of day. | 相对热度指数，范围 0 到 100；数值越高表示该时段越繁忙。 |
| `resp.200.local_business_links` | *available interactions with the business* list of options to interact with the business directly from search results | Direct interaction options offered for the business in search results. | 搜索结果中为该商家提供的直接互动入口列表。 |
| `resp.200.local_business_links.type` | *type of element = **‘menu’*** | Element type marker of the interaction link, e.g. `menu`. | 互动链接的元素类型标记，例如 `menu`。 |
| `resp.200.local_business_links.title` | *title of the element* domain of the online menu system | Title of the link, e.g. the domain of the online menu system. | 链接标题，例如在线菜单系统的域名。 |
| `resp.200.local_business_links.url` | *URL to view the menu* | URL the interaction link points to. | 互动链接指向的 URL。 |
| `resp.200.local_business_links.delivery_services` | *lists available delivery services* | Available delivery services for the business. | 该商家可用的配送服务。 |
| `resp.200.local_business_links.is_directory_item` | *business establishment is a part of the directory* indicates whether the business establishment is a part of the directory; if `true`, the item is a part of the larger directory of businesses with the same address (e.g., a mall or a business centre); **note:** if the business establishment is a parent item in the directory, the value will be `null` | Whether this entry belongs to a directory of businesses sharing one address (e.g. a mall); for a parent directory item the value is `null`. | 该条目是否属于共用同一地址的商家目录（如商场）；若其为目录的父级条目，取值为 `null`。 |
| `resp.200.directory` | *items of the directory* includes information about businesses that are located within the target business establishment and have the same address | Businesses located within the target establishment that share its address. | 位于目标商家内部、与其共用地址的商家集合。 |
| `resp.200.directory.title` | *title of the element in SERP* the name of the business entity | Business name of the directory entry. | 目录条目的商家名称。 |
| `resp.200.directory.items` | ***array of directory items*** | Array of directory items. | 目录条目数组。 |
| `resp.200.directory.type` | *type of element = **‘maps\_search’*** | Element type marker of the directory item, e.g. `maps_search`. | 目录条目的元素类型标记，例如 `maps_search`。 |
| `resp.200.directory.rank_group` | *position within a group of elements with identical `type` values* positions of elements with different `type` values are omitted from the `rank_group` | Position of this entry within the group of elements sharing the same `type`. | 该条目在相同 `type` 元素分组内的位置。 |
| `resp.200.directory.rank_absolute` | *absolute rank among all the elements* | Absolute rank among all the elements. | 在全部元素中的绝对排名。 |
| `resp.200.directory.domain` | *domain of the business entity* | Domain of the business website. | 商家网站的域名。 |
| `resp.200.directory.url` | *absolute url of the business entity* | Absolute URL of the business website. | 商家网站的绝对 URL。 |
| `resp.200.directory.rating` | *the element’s rating* the popularity rate based on reviews and displayed in SERP | Aggregated rating of the entity, as shown in the SERP. | 该实体的综合评分，与 SERP 中展示的一致。 |
| `resp.200.directory.rating_type` | *the type of rating* here you can find the following elements: `Max5`, `Percents`, `CustomMax` | Scale used to express the rating; the business meaning is: `Max5` rated out of 5, `Percents` expressed as a percentage, `CustomMax` rated against a custom maximum. | 评分所采用的标度，业务含义为：`Max5` 表示满分 5 分制，`Percents` 表示百分比制，`CustomMax` 表示自定义满分制。 |
| `resp.200.directory.value` | *the value of the rating* | Numeric value of the rating. | 评分的数值。 |
| `resp.200.directory.votes_count` | *the amount of feedback* | Number of votes the rating is based on. | 该评分所依据的投票数。 |
| `resp.200.directory.rating_max` | *the maximum value for a `rating_type`* | Maximum possible value for the given `rating_type` (for example 5 when the type is `Max5`). | 对应 `rating_type` 的最大可能取值（例如类型为 `Max5` 时为 5）。 |
| `resp.200.directory.rating_distribution` | *the distribution of ratings of the business entity* the object displays the number of 1-star to 5-star ratings, as reviewed by users | Breakdown of how many 1- to 5-star ratings the business received. | 该商家所获 1 至 5 星评分各自数量的分布。 |
| `resp.200.directory.1` | *the number of 1-star ratings* | Number of 1-star ratings. | 1 星评分的数量。 |
| `resp.200.directory.2` | *the number of 2-star ratings* | Number of 2-star ratings. | 2 星评分的数量。 |
| `resp.200.directory.3` | *the number of 3-star ratings* | Number of 3-star ratings. | 3 星评分的数量。 |
| `resp.200.directory.4` | *the number of 4-star ratings* | Number of 4-star ratings. | 4 星评分的数量。 |
| `resp.200.directory.5` | *the number of 5-star ratings* | Number of 5-star ratings. | 5 星评分的数量。 |
| `resp.200.directory.snippet` | *additional information about the business entity* | Additional descriptive information about the business. | 关于该商家的附加描述信息。 |
| `resp.200.directory.address` | *street address of the business entity* | Street address of the business. | 商家的街道地址。 |
| `resp.200.directory.address_info` | *object containing address components of the business entity* | Object holding the structured address components of the business. | 包含商家结构化地址组成部分的对象。 |
| `resp.200.directory.borough` | *administrative unit or district the business entity location belongs to* | Administrative unit or district the business location belongs to. | 商家所在的行政区或辖区。 |
| `resp.200.directory.city` | *name of the city where the business entity is located* | City where the business is located. | 商家所在城市。 |
| `resp.200.directory.zip` | *ZIP code of the business entity* | Postal/ZIP code of the business. | 商家的邮政编码。 |
| `resp.200.directory.region` | *DMA region of the business entity location* | DMA region of the business location. | 商家所在的 DMA 区域。 |
| `resp.200.directory.country_code` | *ISO country code of the business entity location* | ISO country code of the business location. | 商家所在地的 ISO 国家代码。 |
| `resp.200.directory.place_id` | *unique place identifier* [place id](https://developers.google.com/places/place-id) of the local establishment featured in the element learn more about the identifier in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Google place id of the local establishment featured in the element. | 元素中所示本地商家的 Google place id。 |
| `resp.200.directory.phone` | *phone number of the business entity* | Phone number of the business. | 商家的电话号码。 |
| `resp.200.directory.main_image` | *URL of the main image featured in Google My Business profile* | URL of the main image shown on the Google My Business profile. | Google 商家资料中展示的主图 URL。 |
| `resp.200.directory.total_photos` | *total count of images featured in Google My Business profile* | Total number of images featured on the Google My Business profile. | Google 商家资料中收录的图片总数。 |
| `resp.200.directory.category` | *business category* Google My Business general category that best describes the services provided by the business entity | Primary Google My Business category that best describes the business. | 最能概括该商家的 Google 商家主分类。 |
| `resp.200.directory.category_ids` | *global category IDs* universal category IDs that do not change based on the selected country | Universal category IDs that stay constant regardless of country. | 通用分类 ID，不随所选国家变化。 |
| `resp.200.directory.work_hours` | *work hours* information about work hours of the local establishment | Working hours information of the establishment. | 商家的营业时间信息。 |
| `resp.200.directory.feature_id` | *the unique identifier of the element in SERP* learn more about the identifier in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Unique identifier of the element in the SERP (feature id). | SERP 中该元素的唯一标识（feature id）。 |
| `resp.200.directory.cid` | *google-defined client id* unique id of a local establishment; can be used with [Google Reviews API](https://docs.dataforseo.com/v3/reviews/google/overview.md) to get a full list of reviews learn more about the identifier in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Google-defined client id (cid); can be used with the Google Reviews API to fetch the full review list. | Google 定义的客户端 id（cid）；可配合 Google Reviews API 获取完整评论列表。 |
| `resp.200.directory.latitude` | *latitude coordinate of the local establishments in google maps* example: `"latitude": 51.584091` | Latitude of the establishment on Google Maps. | 商家在 Google 地图上的纬度。 |
| `resp.200.directory.longitude` | *longitude coordinate of the local establishment in google maps* example: `"longitude": -0.31365919999999997` | Longitude of the establishment on Google Maps. | 商家在 Google 地图上的经度。 |
| `resp.200.directory.is_claimed` | *shows whether the entity is verified by its owner on Google Maps* | Whether the entity is verified by its owner on Google Maps. | 该实体是否已由其所有者在 Google 地图上认领验证。 |
| `resp.200.directory.local_justifications` | *Google local justifications* snippets of text that “justify” why the business is showing up for search query | Google local justification snippets explaining why the business appears for the query. | Google 本地佐证片段，说明该商家为何会出现在该查询结果中。 |
| `resp.200.directory.is_directory_item` | *business establishment is a part of the directory* indicates whether the business establishment is a part of the directory; if `true`, the item is a part of the larger directory of businesses with the same address (e.g., a mall or a business centre); **note:** if the business establishment is a parent item in the directory, the value will be `null` | Whether this entry belongs to a directory of businesses sharing one address; for a parent directory item the value is `null`. | 该条目是否属于共用同一地址的商家目录；若其为目录的父级条目，取值为 `null`。 |
| `resp.200.directory.price_level` | *property price level* can take values: `inexpensive`, `moderate`, `expensive`, `very_expensive` if there is no price level information, the value will be `null` | Price level of the property; business meaning: `inexpensive`, `moderate`, `expensive`, `very_expensive`; `null` when unavailable. | 场所的价格档次；业务含义：`inexpensive` 便宜、`moderate` 适中、`expensive` 偏贵、`very_expensive` 非常贵；无信息时为 `null`。 |
| `resp.200.directory.hotel_rating` | *hotel class rating* class ratings range between 1-5 stars, [learn more](https://support.google.com/business/answer/7660515?hl=en) if there is no hotel class rating information, the value will be `null` | Hotel class rating ranging 1-5 stars; `null` when unavailable. | 酒店星级评定，范围 1 至 5 星；无信息时为 `null`。 |

## get_dataforseo_business_data_google_my_business_info_tasks_ready

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | The **‘Tasks Ready’** endpoint is designed to provide you with the list of completed tasks, which haven’t been collected yet. If you don’t use the `postback_url`, you can receive the list of `id` for all completed tasks using this endpoint. Then, you can collect the results using the **‘Task GET’** endpoint. | List Google My Business Info tasks that have finished processing and are ready to be collected. | 列出已处理完成、可供拉取的 Google 商家信息任务。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total tasks cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array returned with an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the parameters passed in the request’s URL* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.id` | *task identifier of the completed task* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | UUID of a completed task that is now ready to be collected; pass it to the matching `task_get` endpoint to download the results. | 已完成、可供拉取的任务的 UUID；将其传给对应的 `task_get` 端点即可下载结果。 |
| `resp.200.result.se` | *search engine specified when setting the task* can take the following values: `google` | Search engine the completed task was set for. | 该已完成任务所针对的搜索引擎。 |
| `resp.200.result.se_type` | *search engine specified when setting the task* | Search engine type the completed task was set for. | 该已完成任务所针对的搜索引擎类型。 |
| `resp.200.result.date_posted` | *date when the task was posted (in the UTC format)* | Timestamp (UTC) when the task was originally submitted. | 任务最初提交的时间（UTC）。 |
| `resp.200.result.tag` | *user-defined task identifier* | User-defined label that was attached to the task when it was created, echoed back for matching. | 创建任务时附加的用户自定义标签，原样回显以便匹配。 |
| `resp.200.result.endpoint` | *URL for collecting the results of the task* | Relative endpoint URL to call for collecting this completed task's results. | 用于拉取该已完成任务结果的相对端点 URL。 |

## post_dataforseo_business_data_google_my_business_updates_task_post

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoints provides the latest updates of a specific business entity from Google SERP. The provided results are specific to the selected location (see [the List of Locations](https://docs.dataforseo.com/v3/business_data/google/locations.md)) and language (see [the List of Languages](https://docs.dataforseo.com/v3/business_data/google/languages.md)) settings. | Create an asynchronous task that collects Google My Business posts/updates for a named establishment; poll tasks_ready and then fetch results by id. | 创建一个异步任务，采集指定商家的 Google 商家动态/帖子；随后轮询 tasks_ready 并按 id 拉取结果。 |
| `req.keyword` | *keyword* **required field** the keyword you specify should indicate the name of the local establishment you can specify **up to 700 characters** in the `keyword` filed **all %## will be decoded (plus character ‘+’ will be decoded to a space character)** if you need to use the “%” character for your `keyword`, please specify it as “%25”;this field can also be used to pass `cid` (unique, google-defined id of the business entity) or `place_id` (identifier of the business entity in Google Maps) parameters example: `cid:194604053573767737` `place_id:GhIJQWDl0CIeQUARxks3icF8U8A`learn more about the `cid` and `place_id` identifiers in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Name of the local establishment to look up (up to 700 characters); percent-encoding is decoded. This field can also carry a Google-defined `cid` to target a specific establishment. | 要查询的本地商家名称（最多 700 个字符）；会解码百分号转义。该字段也可传入 Google 定义的 `cid` 以精确定位某个商家。 |
| `req.priority` | *task priority* optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the [Pricing](https://dataforseo.com/pricing/business-data/business-data-api "Pricing") page. | Execution priority of the task: `1` runs at normal priority, `2` runs at high priority for faster turnaround at an extra charge. | 任务执行优先级：`1` 为普通优先级，`2` 为高优先级，可更快返回但会额外计费。 |
| `req.location_name` | *full name of search engine location* **required field if you don’t specify** `location_code` or `location_coordinate` **if you use this field, you don’t need to specify `location_code` or `location_coordinate`** you can receive the list of available locations with `location_name` by making a separate request to the `https://api.dataforseo.com/v3/business_data/google/locations` example: `London,England,United Kingdom` | Full location name (e.g. `London,England,United Kingdom`) that sets the geographic context of the search. Provide this or `location_code`/`location_coordinate`; the available list comes from the corresponding `/locations` endpoint. | 完整地区名称（如 `London,England,United Kingdom`），用于设定搜索的地理范围。需在此与 `location_code`/`location_coordinate` 中择一提供；可用列表来自对应的 `/locations` 端点。 |
| `req.location_code` | *search engine location code* **required field if you don’t specify** `location_name` or `location_coordinate` **if you use this field, you don’t need to specify `location_name` or `location_coordinate`** you can receive the list of available locations with `location_code` by making a separate request to the `https://api.dataforseo.com/v3/business_data/google/locations` example: `2840` | Numeric location code that sets the geographic context of the search. Provide this or `location_name`/`location_coordinate`; the available list comes from the corresponding `/locations` endpoint. | 数字地区代码，用于设定搜索的地理范围。需在此与 `location_name`/`location_coordinate` 中择一提供；可用列表来自对应的 `/locations` 端点。 |
| `req.location_coordinate` | *GPS coordinates of a location* **required field if you don’t specify** `location_name` or `location_code` **if you use this field, you don’t need to specify `location_name` or `location_code`** `location_coordinate` parameter should be specified in the *“latitude,longitude,radius”* format the maximum number of decimal digits for *“latitude”* and *“longitude”*: 7 the minimum value for *“radius”*: 199.9 example: `53.476225,-2.243572,200` | GPS coordinates that set the geographic context of the search, in `latitude,longitude` (some endpoints also accept a radius). Provide this or `location_name`/`location_code`. | 用于设定搜索地理范围的 GPS 坐标，格式为 `latitude,longitude`（部分端点还接受半径）。需与 `location_name`/`location_code` 择一提供。 |
| `req.language_name` | *full name of search engine language* **required field if you don’t specify** `language_code` **if you use this field, you don’t need to specify `language_code`** you can receive the list of available languages with `language_name` by making a separate request to the `https://api.dataforseo.com/v3/business_data/google/languages` example: `English` | Full language name (e.g. `English`) for the search. Provide this or `language_code`; the available list comes from the corresponding `/languages` endpoint. | 搜索所用语言的完整名称（如 `English`）。需与 `language_code` 择一提供；可用列表来自对应的 `/languages` 端点。 |
| `req.language_code` | *search engine language code* **required field if you don’t specify** `language_name` **if you use this field, you don’t need to specify `language_name`** you can receive the list of available languages with their `language_code` by making a separate request to the `https://api.dataforseo.com/v3/business_data/google/languages` example: `en` | Language code (e.g. `en`) for the search. Provide this or `language_name`; the available list comes from the corresponding `/languages` endpoint. | 搜索所用的语言代码（如 `en`）。需与 `language_name` 择一提供；可用列表来自对应的 `/languages` 端点。 |
| `req.depth` | *parsing depth* optional field number of updates in SERP we strongly recommend setting the parsing depth in the multiples of ten, because our systems processes ten updates in a row **please note that Google returns 4490 updates maximum** default value: `10` | Number of updates to parse. Best set in multiples of ten since updates are processed ten at a time; Google returns at most 4490 updates. | 要解析的动态更新条数。建议设为 10 的倍数，因为系统每次处理十条；Google 最多返回 4490 条更新。 |
| `req.tag` | *user-defined task identifier* optional field *the character limit is 255* you can use this parameter to identify the task and match it with the result you will find the specified `tag` value in the `data` object of the response | Your own label for the task (up to 255 characters). It is echoed back in the response `data` so you can match results to requests. | 你为任务自定义的标签（最多 255 个字符）。会在响应的 `data` 中回显，便于将结果与请求匹配。 |
| `req.postback_url` | *return URL for sending task results* optional field once the task is completed, we will send a POST request with its results compressed in the `gzip` format to the `postback_url` you specified you can use the ‘$id’ string as a `$id` variable and ‘$tag’ as urlencoded `$tag` variable. We will set the necessary values before sending the request. example: `http://your-server.com/postbackscript?id=$id` `http://your-server.com/postbackscript?id=$id&tag=$tag` **Note:** special characters in `postback_url` will be urlencoded; i.a., the `#` character will be encoded into `%23`learn more on our [Help Center](https://dataforseo.com/help-center/pingbacks-postbacks-with-dataforseo-api) | URL to which the completed task results are pushed via POST (gzip-compressed). Use the `$id`/`$tag` placeholders in the URL to have them substituted automatically. | 任务完成后用于以 POST 推送结果（gzip 压缩）的回调 URL。可在 URL 中使用 `$id`/`$tag` 占位符，系统会自动替换为实际值。 |
| `req.pingback_url` | *notification URL of a completed task* optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a `$id` variable and ‘$tag’ as urlencoded `$tag` variable. We will set the necessary values before sending the request. example: `http://your-server.com/pingscript?id=$id` `http://your-server.com/pingscript?id=$id&tag=$tag` **Note:** special characters in `pingback_url` will be urlencoded; i.a., the `#` character will be encoded into `%23`learn more on our [Help Center](https://dataforseo.com/help-center/pingbacks-postbacks-with-dataforseo-api) | URL that is pinged via GET when the task completes, signalling that results are ready to collect. Supports the `$id`/`$tag` placeholders. | 任务完成时通过 GET 触达的通知 URL，用于提示结果已就绪。支持 `$id`/`$tag` 占位符。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total tasks cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`**array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array returned with an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *unique task identifier in our system* in the [Universally unique identifier (UUID)](https://en.wikipedia.org/wiki/Universally_unique_identifier) format | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the same parameters that you specified in the POST request* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* in this case, the value will be `null` | Result array; for this task-creation response the value is `null`. | 结果数组；对于本任务创建响应，其值为 `null`。 |

## post_dataforseo_business_data_google_questions_and_answers_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with a detailed overview of questions and answers associated with a specific business entity listed on Google My Business. By submitting a request to this endpoint, you can access comprehensive data on the inquiries and responses related to a particular business, including the full text of the questions and answers, as well as metadata such as timestamps, user information. | Synchronously retrieve the Questions and Answers section of a Google My Business listing for the named establishment, returning both answered and unanswered questions. | 针对指定商家，同步获取其 Google 商家页面的问答区，返回已回答与未回答的问题。 |
| `req.keyword` | *keyword* **required field** the keyword you specify should indicate the name of the local establishment you can specify **up to 700 characters** in the `keyword` filed **all %## will be decoded (plus character ‘+’ will be decoded to a space character)** if you need to use the “%” character for your `keyword`, please specify it as “%25”; this field can also be used to pass the following parameters: `cid` – a unique, google-defined id of the business entity; `place_id` – an identifier of the business entity in Google Maps; `spp` – a unique identifier of local services featured in the `local_pack` element of Google SERP example: `cid:194604053573767737` `place_id:GhIJQWDl0CIeQUARxks3icF8U8A` `spp:CgsvZy8xdGN4cWRraBoUChIJPZDrEzLsZIgRoNrpodC5P30` learn more about the `cid` and `place_id` identifiers in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) learn more about rules and limitations of `keyword` and `keywords` fields in DataForSEO APIs in this [Help Center article](https://dataforseo.com/help-center/rules-and-limitations-of-keyword-and-keywords-fields-in-dataforseo-apis) | Name of the local establishment to look up (up to 700 characters); percent-encoding is decoded. This field can also carry a Google-defined `cid` to target a specific establishment. | 要查询的本地商家名称（最多 700 个字符）；会解码百分号转义。该字段也可传入 Google 定义的 `cid` 以精确定位某个商家。 |
| `req.location_name` | *full name of search engine location* **required field if you don’t specify** `location_code` or `location_coordinate` **if you use this field, you don’t need to specify `location_code` or `location_coordinate`** you can receive the list of available locations with `location_name` by making a separate request to `https://api.dataforseo.com/v3/business_data/google/locations` example: `London,England,United Kingdom` | Full location name (e.g. `London,England,United Kingdom`) that sets the geographic context of the search. Provide this or `location_code`/`location_coordinate`; the available list comes from the corresponding `/locations` endpoint. | 完整地区名称（如 `London,England,United Kingdom`），用于设定搜索的地理范围。需在此与 `location_code`/`location_coordinate` 中择一提供；可用列表来自对应的 `/locations` 端点。 |
| `req.location_code` | *search engine location code* **required field if you don’t specify** `location_name` or `location_coordinate` **if you use this field, you don’t need to specify `location_name` or `location_coordinate`** you can receive the list of available locations with `location_code` by making a separate request to the `https://api.dataforseo.com/v3/business_data/google/locations` example: `2840` | Numeric location code that sets the geographic context of the search. Provide this or `location_name`/`location_coordinate`; the available list comes from the corresponding `/locations` endpoint. | 数字地区代码，用于设定搜索的地理范围。需在此与 `location_name`/`location_coordinate` 中择一提供；可用列表来自对应的 `/locations` 端点。 |
| `req.location_coordinate` | *GPS coordinates of a location* **required field if you don’t specify** `location_name` or `location_code` **if you use this field, you don’t need to specify `location_name` or `location_code`** `location_coordinate` parameter should be specified in the *“latitude,longitude,radius”* format the maximum number of decimal digits for *“latitude”* and *“longitude”*: 7 the minimum value for *“radius”*: 199.9 (mm) the maximum value for *“radius”*: 199999 (mm) example: `53.476225,-2.243572,200` | GPS coordinates that set the geographic context of the search, in `latitude,longitude` (some endpoints also accept a radius). Provide this or `location_name`/`location_code`. | 用于设定搜索地理范围的 GPS 坐标，格式为 `latitude,longitude`（部分端点还接受半径）。需与 `location_name`/`location_code` 择一提供。 |
| `req.language_name` | *full name of search engine language* **required field if you don’t specify** `language_code` **if you use this field, you don’t need to specify `language_code`** you can receive the list of available languages with `language_name` by making a separate request to `https://api.dataforseo.com/v3/business_data/google/languages` example: `English` | Full language name (e.g. `English`) for the search. Provide this or `language_code`; the available list comes from the corresponding `/languages` endpoint. | 搜索所用语言的完整名称（如 `English`）。需与 `language_code` 择一提供；可用列表来自对应的 `/languages` 端点。 |
| `req.language_code` | *search engine language code* **required field if you don’t specify** `language_name` **if you use this field, you don’t need to specify `language_name`** you can receive the list of available languages with their `language_code` by making a separate request to `https://api.dataforseo.com/v3/business_data/google/languages` example: `en` | Language code (e.g. `en`) for the search. Provide this or `language_name`; the available list comes from the corresponding `/languages` endpoint. | 搜索所用的语言代码（如 `en`）。需与 `language_name` 择一提供；可用列表来自对应的 `/languages` 端点。 |
| `req.depth` | *parsing depth* optional field number of results in SERP default value: `20` max value: `100` **Your account will be billed per each SERP containing up to 20 results;** Setting depth above 20 may result in additional charges if the search engine returns more than 20 results; If the specified depth is higher than the number of questions in the response, the difference will be refunded automatically to your account balance; The cost can be calculated on the [Pricing](https://dataforseo.com/pricing/business-data/google-questions-and-answers-api-pricing "Pricing") page. | Number of results to parse; default 20, max 100. Billing is per SERP of up to 20 results, so going above 20 may add charges; unused depth is auto-refunded. | 要解析的结果数量；默认 20，最大 100。计费以每 20 条结果一个 SERP 为单位，超过 20 可能额外计费；未用尽的额度会自动退还。 |
| `req.tag` | *user-defined task identifier* optional field *the character limit is 255* you can use this parameter to identify the task and match it with the result you will find the specified `tag` value in the `data` object of the response | Your own label for the task (up to 255 characters). It is echoed back in the response `data` so you can match results to requests. | 你为任务自定义的标签（最多 255 个字符）。会在响应的 `data` 中回显，便于将结果与请求匹配。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total *tasks* cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array that were returned an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the same parameters that you specified in the POST request* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.keyword` | *keyword received in a POST array* **keyword is returned with decoded %## (plus character ‘+’ will be decoded to a space character)** this field will contain the `cid` parameter if you specified it in the `keyword` field when setting a task; example: `cid:2946633002421908862` learn more about the parameter in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Keyword echoed from the POST request (percent-encoding decoded); if you passed a `cid` in the keyword field, it is returned here as `cid:...`. | 从 POST 请求回显的关键词（已解码百分号转义）；若你在关键词字段中传入了 `cid`，此处会以 `cid:...` 形式返回。 |
| `resp.200.result.se_domain` | *search engine domain as specified in a POST array* | Search engine domain echoed from the POST request. | 从 POST 请求回显的搜索引擎域名。 |
| `resp.200.result.location_code` | *location code in a POST array* | Location code echoed from the POST request. | 从 POST 请求回显的地区代码。 |
| `resp.200.result.language_code` | *language code in a POST array* | Language code echoed from the POST request. | 从 POST 请求回显的语言代码。 |
| `resp.200.result.check_url` | *direct URL to search engine results* you can use it to make sure that we provided accurate results | Direct URL to the search engine results page, so you can verify the data against the live SERP. | 指向搜索引擎结果页的直达 URL，可用于将数据与实时 SERP 核对。 |
| `resp.200.result.datetime` | *date and time when the result was received* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | UTC timestamp when the result was collected, formatted `yyyy-mm-dd hh-mm-ss +00:00`. | 结果采集的时间（UTC），格式为 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.result.cid` | *google-defined client id* unique id of a local establishment; learn more about the identifier in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Google-defined client id (cid), unique id of the local establishment. | Google 定义的客户端 id（cid），本地商家的唯一标识。 |
| `resp.200.result.feature_id` | *unique identifier of the SERP feature* | Unique identifier of the SERP feature. | 该 SERP 特性的唯一标识。 |
| `resp.200.result.item_types` | *item types* types of search engine results encountered in the `items` array; possible item types: `google_business_question_item` | Item types present in the result; possible value: `google_business_question_item`. | 结果中出现的元素类型；可能取值为 `google_business_question_item`。 |
| `resp.200.items_without_answers` | *array of google business question items without answers* | Array of Google Business question items without any answers. | 无回答的 Google 商家问题条目数组。 |
| `resp.200.items_without_answers.type` | *type of element = **‘google\_business\_question\_item’*** | Element type marker of the question item. | 问题条目的元素类型标记。 |
| `resp.200.items_without_answers.rank_group` | *position within a group of elements with identical `type` values* positions of elements with different `type` values are omitted from `rank_group` | Position of this entry within the group of elements sharing the same `type`. | 该条目在相同 `type` 元素分组内的位置。 |
| `resp.200.items_without_answers.rank_absolute` | *absolute rank among all the elements* | Absolute rank among all the elements. | 在全部元素中的绝对排名。 |
| `resp.200.items_without_answers.question_id` | *ID of the question* | Unique id of the question. | 问题的唯一 id。 |
| `resp.200.items_without_answers.url` | *URL of the question* | URL of the question. | 问题的 URL。 |
| `resp.200.items_without_answers.profile_image_url` | *URL of the user’s profile image* | URL of the asker's profile image. | 提问者头像图片的 URL。 |
| `resp.200.items_without_answers.profile_url` | *URL of the user’s profile* | URL of the asker's profile. | 提问者资料页的 URL。 |
| `resp.200.items_without_answers.profile_name` | *displayed name of the user* | Display name of the asker. | 提问者的显示名称。 |
| `resp.200.items_without_answers.question_text` | *current text of the question* | Current text of the question. | 问题的当前文本。 |
| `resp.200.items_without_answers.original_question_text` | *original text of the question* | Original text of the question. | 问题的原始文本。 |
| `resp.200.items_without_answers.time_ago` | *estimated time when the question was posted* | Relative time when the question was posted. | 问题发布时间的相对表述。 |
| `resp.200.items_without_answers.timestamp` | *exact time when the question was posted* | Exact time when the question was posted. | 问题发布的精确时间。 |
| `resp.200.items_without_answers.items` | *array of items* items within `google_business_question_item` | Nested items belonging to this question item. | 归属于该问题条目的嵌套子项。 |
| `resp.200.items_without_answers.items_count` | *the number of items in the `items` array* | Number of items in this question item's `items` array. | 该问题条目 `items` 数组中的子项数量。 |
| `resp.200.items` | *array of google business question items with answers* possible item types: `google_business_question_item` | Array of Google Business question items that have answers. | 含回答的 Google 商家问题条目数组。 |
| `resp.200.items.type` | *type of the element = **‘google\_business\_answer\_element’*** | Element type marker of the question item. | 问题条目的元素类型标记。 |
| `resp.200.items.rank_group` | *position within a group of elements with identical `type` values* positions of elements with different `type` values are omitted from `rank_group` | Position of this entry within the group of elements sharing the same `type`. | 该条目在相同 `type` 元素分组内的位置。 |
| `resp.200.items.rank_absolute` | *absolute rank among all the elements* | Absolute rank among all the elements. | 在全部元素中的绝对排名。 |
| `resp.200.items.question_id` | *ID of the question* | Unique id of the question. | 问题的唯一 id。 |
| `resp.200.items.url` | *URL of the question* | URL of the question. | 问题的 URL。 |
| `resp.200.items.profile_image_url` | *URL of the user’s profile image* | URL of the asker's profile image. | 提问者头像图片的 URL。 |
| `resp.200.items.profile_url` | *URL of the user’s profile* | URL of the asker's profile. | 提问者资料页的 URL。 |
| `resp.200.items.profile_name` | *displayed name of the user* | Display name of the asker. | 提问者的显示名称。 |
| `resp.200.items.question_text` | *current text of the question* | Current text of the question. | 问题的当前文本。 |
| `resp.200.items.original_question_text` | *original text of the question* | Original text of the question. | 问题的原始文本。 |
| `resp.200.items.time_ago` | *estimated time when the answer was posted* | Relative time when the question was posted. | 问题发布时间的相对表述。 |
| `resp.200.items.timestamp` | *exact time when the answer was posted* | Exact time when the question was posted. | 问题发布的精确时间。 |
| `resp.200.items.items` | *array of items within `google_business_question_item`* contains answers to the google business questions; possible item types `google_business_answer_element` | Answers to the Google business question; possible item type: `google_business_answer_element`. | 对该 Google 商家问题的回答；可能的元素类型为 `google_business_answer_element`。 |
| `resp.200.items.answer_id` | *ID of the answer* | Unique id of the answer. | 回答的唯一 id。 |
| `resp.200.items.answer_text` | *current text of the answer* | Current text of the answer. | 回答的当前文本。 |
| `resp.200.items.original_answer_text` | *original text of the answer* | Original text of the answer. | 回答的原始文本。 |

## post_dataforseo_business_data_google_questions_and_answers_task_post

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with a detailed overview of questions and answers associated with a specific business entity listed on Google My Business. By submitting a request to this endpoint, you can access comprehensive data on the inquiries and responses related to a particular business, including the full text of the questions and answers, as well as metadata such as timestamps, user information. | Create an asynchronous task that collects the Questions and Answers section of a Google My Business listing; poll tasks_ready and fetch results by id. | 创建一个异步任务，采集 Google 商家页面的问答区；随后轮询 tasks_ready 并按 id 拉取结果。 |
| `req.keyword` | *keyword* **required field** the keyword you specify should indicate the name of the local establishment you can specify **up to 700 characters** in the `keyword` filed **all %## will be decoded (plus character ‘+’ will be decoded to a space character)** if you need to use the “%” character for your `keyword`, please specify it as “%25”; this field can also be used to pass the following parameters: `cid` – a unique, google-defined id of the business entity; `place_id` – an identifier of the business entity in Google Maps; `spp` – a unique identifier of local services featured in the `local_pack` element of Google SERP example: `cid:194604053573767737` `place_id:GhIJQWDl0CIeQUARxks3icF8U8A` `spp:CgsvZy8xdGN4cWRraBoUChIJPZDrEzLsZIgRoNrpodC5P30` learn more about the `cid` and `place_id` identifiers in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) learn more about rules and limitations of `keyword` and `keywords` fields in DataForSEO APIs in this [Help Center article](https://dataforseo.com/help-center/rules-and-limitations-of-keyword-and-keywords-fields-in-dataforseo-apis) | Name of the local establishment to look up (up to 700 characters); percent-encoding is decoded. This field can also carry a Google-defined `cid` to target a specific establishment. | 要查询的本地商家名称（最多 700 个字符）；会解码百分号转义。该字段也可传入 Google 定义的 `cid` 以精确定位某个商家。 |
| `req.priority` | *task priority* optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the [Pricing](https://dataforseo.com/pricing/business-data/google-questions-and-answers-api-pricing "Pricing") page. | Execution priority of the task: `1` runs at normal priority, `2` runs at high priority for faster turnaround at an extra charge. | 任务执行优先级：`1` 为普通优先级，`2` 为高优先级，可更快返回但会额外计费。 |
| `req.location_name` | *full name of search engine location* **required field if you don’t specify** `location_code` or `location_coordinate` **if you use this field, you don’t need to specify `location_code` or `location_coordinate`** you can receive the list of available locations with `location_name` by making a separate request to `https://api.dataforseo.com/v3/business_data/google/locations` example: `London,England,United Kingdom` | Full location name (e.g. `London,England,United Kingdom`) that sets the geographic context of the search. Provide this or `location_code`/`location_coordinate`; the available list comes from the corresponding `/locations` endpoint. | 完整地区名称（如 `London,England,United Kingdom`），用于设定搜索的地理范围。需在此与 `location_code`/`location_coordinate` 中择一提供；可用列表来自对应的 `/locations` 端点。 |
| `req.location_code` | *search engine location code* **required field if you don’t specify** `location_name` or `location_coordinate` **if you use this field, you don’t need to specify `location_name` or `location_coordinate`** you can receive the list of available locations with `location_code` by making a separate request to the `https://api.dataforseo.com/v3/business_data/google/locations` example: `2840` | Numeric location code that sets the geographic context of the search. Provide this or `location_name`/`location_coordinate`; the available list comes from the corresponding `/locations` endpoint. | 数字地区代码，用于设定搜索的地理范围。需在此与 `location_name`/`location_coordinate` 中择一提供；可用列表来自对应的 `/locations` 端点。 |
| `req.location_coordinate` | *GPS coordinates of a location* **required field if you don’t specify** `location_name` or `location_code` **if you use this field, you don’t need to specify `location_name` or `location_code`** `location_coordinate` parameter should be specified in the *“latitude,longitude,radius”* format the maximum number of decimal digits for *“latitude”* and *“longitude”*: 7 the minimum value for *“radius”*: 199.9 (mm) the maximum value for *“radius”*: 199999 (mm) example: `53.476225,-2.243572,200` | GPS coordinates that set the geographic context of the search, in `latitude,longitude` (some endpoints also accept a radius). Provide this or `location_name`/`location_code`. | 用于设定搜索地理范围的 GPS 坐标，格式为 `latitude,longitude`（部分端点还接受半径）。需与 `location_name`/`location_code` 择一提供。 |
| `req.language_name` | *full name of search engine language* **required field if you don’t specify** `language_code` **if you use this field, you don’t need to specify `language_code`** you can receive the list of available languages with `language_name` by making a separate request to `https://api.dataforseo.com/v3/business_data/google/languages` example: `English` | Full language name (e.g. `English`) for the search. Provide this or `language_code`; the available list comes from the corresponding `/languages` endpoint. | 搜索所用语言的完整名称（如 `English`）。需与 `language_code` 择一提供；可用列表来自对应的 `/languages` 端点。 |
| `req.language_code` | *search engine language code* **required field if you don’t specify** `language_name` **if you use this field, you don’t need to specify `language_name`** you can receive the list of available languages with their `language_code` by making a separate request to `https://api.dataforseo.com/v3/business_data/google/languages` example: `en` | Language code (e.g. `en`) for the search. Provide this or `language_name`; the available list comes from the corresponding `/languages` endpoint. | 搜索所用的语言代码（如 `en`）。需与 `language_name` 择一提供；可用列表来自对应的 `/languages` 端点。 |
| `req.depth` | *parsing depth* optional field number of question rows in the result default value: `20` max value: `700` **Your account will be billed per each SERP containing up to 20 results;** Setting depth above 20 may result in additional charges if the search engine returns more than 20 results; If the specified depth is higher than the number of questions in the response, the difference will be refunded automatically to your account balance; The cost can be calculated on the [Pricing](https://dataforseo.com/pricing/business-data/google-questions-and-answers-api-pricing "Pricing") page. | Number of question rows to parse; default 20, max 700. Billing is per SERP of up to 20 results, so going above 20 may add charges; unused depth is auto-refunded. | 要解析的问题行数；默认 20，最大 700。计费以每 20 条结果一个 SERP 为单位，超过 20 可能额外计费；未用尽的额度会自动退还。 |
| `req.tag` | *user-defined task identifier* optional field *the character limit is 255* you can use this parameter to identify the task and match it with the result you will find the specified `tag` value in the `data` object of the response | Your own label for the task (up to 255 characters). It is echoed back in the response `data` so you can match results to requests. | 你为任务自定义的标签（最多 255 个字符）。会在响应的 `data` 中回显，便于将结果与请求匹配。 |
| `req.postback_url` | *return URL for sending task results* optional field once the task is completed, we will send a POST request with its results compressed in the `gzip` format to the `postback_url` you specified you can use the ‘$id’ string as a `$id` variable and ‘$tag’ as urlencoded `$tag` variable. We will set the necessary values before sending the request. example: `http://your-server.com/postbackscript?id=$id` `http://your-server.com/postbackscript?id=$id&tag=$tag` **Note:** special characters in `postback_url` will be urlencoded; i.a., the `#` character will be encoded into `%23`learn more on our [Help Center](https://dataforseo.com/help-center/pingbacks-postbacks-with-dataforseo-api) | URL to which the completed task results are pushed via POST (gzip-compressed). Use the `$id`/`$tag` placeholders in the URL to have them substituted automatically. | 任务完成后用于以 POST 推送结果（gzip 压缩）的回调 URL。可在 URL 中使用 `$id`/`$tag` 占位符，系统会自动替换为实际值。 |
| `req.pingback_url` | *notification URL of a completed task* optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a `$id` variable and ‘$tag’ as urlencoded `$tag` variable. We will set the necessary values before sending the request. example: `http://your-server.com/pingscript?id=$id` `http://your-server.com/pingscript?id=$id&tag=$tag` **Note:** special characters in `pingback_url` will be urlencoded; i.a., the `#` character will be encoded into `%23` learn more on our [Help Center](https://dataforseo.com/help-center/pingbacks-postbacks-with-dataforseo-api) | URL that is pinged via GET when the task completes, signalling that results are ready to collect. Supports the `$id`/`$tag` placeholders. | 任务完成时通过 GET 触达的通知 URL，用于提示结果已就绪。支持 `$id`/`$tag` 占位符。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total tasks cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`**array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array returned with an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *unique task identifier in our system* in the [Universally unique identifier (UUID)](https://en.wikipedia.org/wiki/Universally_unique_identifier) format | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the same parameters that you specified in the POST request* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* in this case, the value will be `null` | Result array; for this task-creation response the value is `null`. | 结果数组；对于本任务创建响应，其值为 `null`。 |

## get_dataforseo_business_data_google_reviews_task_get_id

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | The returned results are specific to the indicated local establishment name, search engine, [location](https://docs.dataforseo.com/v3/business_data-google-locations.md) and [language](https://docs.dataforseo.com/v3/business_data-google-languages.md) parameters. We emulate set location and search engine with the highest accuracy so that the results you receive will match the actual search results for the specified parameters at the time of task setting. You can always check the returned results accessing the `check_url` in the Incognito mode to make sure the received data is entirely relevant. Note that user preferences, search history, and other personalized search factors are ignored by our system and thus would not be reflected in the returned results. | Fetch the results of a previously created Google Reviews task by its id, returning the establishment's aggregated rating and individual review entries. | 按任务 id 拉取此前创建的 Google 评论任务结果，返回商家的综合评分及单条评论条目。 |
| `param:id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** you will be able to use it within **30 days** to request the results of the task at any time | UUID of the task whose results you want to fetch. The task remains retrievable for 30 days after creation. | 要拉取结果的任务 UUID。任务自创建起 30 天内可随时取回结果。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total *tasks* cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array that were returned an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the same parameters that you specified in the POST request* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.keyword` | *keyword received in a POST array* **keyword is returned with decoded %## (plus character ‘+’ will be decoded to a space character)** | Keyword echoed from the POST request, with percent-encoding decoded (a `+` becomes a space). | 从 POST 请求回显的关键词，已解码百分号转义（`+` 会被还原为空格）。 |
| `resp.200.result.type` | *search engine type in a POST array* | Search engine type echoed from the POST request. | 从 POST 请求回显的搜索引擎类型。 |
| `resp.200.result.se_domain` | *search engine domain in a POST array* | Search engine domain echoed from the POST request. | 从 POST 请求回显的搜索引擎域名。 |
| `resp.200.result.location_code` | *location code in a POST array* | Location code echoed from the POST request. | 从 POST 请求回显的地区代码。 |
| `resp.200.result.language_code` | *language code in a POST array* | Language code echoed from the POST request. | 从 POST 请求回显的语言代码。 |
| `resp.200.result.check_url` | *direct URL to search engine results* you can use it to make sure that we provided accurate results | Direct URL to the search engine results page, so you can verify the data against the live SERP. | 指向搜索引擎结果页的直达 URL，可用于将数据与实时 SERP 核对。 |
| `resp.200.result.datetime` | *date and time when the result was received* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | UTC timestamp when the result was collected, formatted `yyyy-mm-dd hh-mm-ss +00:00`. | 结果采集的时间（UTC），格式为 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.result.title` | *title of the ‘reviews’ element in SERP* the name of the local establishment for which the reviews are collected | Name of the local establishment that the 'reviews' SERP element collects reviews for. | SERP 中 'reviews' 元素所采集评论对应的本地商家名称。 |
| `resp.200.result.sub_title` | *subtitle of the ‘reviews’ element in SERP* additional information (e.g., address) on the ‘reviews’ element for which the reviews are collected | Supplementary information (such as the address) shown beneath the title of the 'reviews' element. | 'reviews' 元素标题下方展示的补充信息（如地址）。 |
| `resp.200.result.rating` | *rating of the corresponding local establishment* popularity rate based on reviews and displayed in SERP | Aggregated rating of the entity, as shown in the SERP. | 该实体的综合评分，与 SERP 中展示的一致。 |
| `resp.200.result.rating_type` | *type of rating* here you can find the following elements: `Max5`, `Percents`, `CustomMax` | Scale used to express the rating; the business meaning is: `Max5` rated out of 5, `Percents` expressed as a percentage, `CustomMax` rated against a custom maximum. | 评分所采用的标度，业务含义为：`Max5` 表示满分 5 分制，`Percents` 表示百分比制，`CustomMax` 表示自定义满分制。 |
| `resp.200.result.value` | *the average rating based on all reviews* | Average rating value computed from all reviews. | 基于全部评论计算出的平均评分值。 |
| `resp.200.result.votes_count` | *the number of votes* | Number of votes (ratings) the rating is based on. | 该评分所依据的投票（打分）数量。 |
| `resp.200.result.rating_max` | *the maximum value for a `rating_type`* | Maximum possible value for the given `rating_type` (for example 5 when the type is `Max5`). | 对应 `rating_type` 的最大可能取值（例如类型为 `Max5` 时为 5）。 |
| `resp.200.result.feature_id` | *the unique identifier of the ‘reviews’ element in SERP* learn more about the identifier in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Unique identifier of the 'reviews' SERP element (feature id). | SERP 中 'reviews' 元素的唯一标识（feature id）。 |
| `resp.200.result.place_id` | *unique identifier of a business location assigned by Google* learn more about the identifier in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Google-assigned unique identifier of the business location (place id). | Google 为该商家地点分配的唯一标识（place id）。 |
| `resp.200.result.cid` | *google-defined client id* unique id of a local establishment learn more about the identifier in [this help center article](https://dataforseo.com/help-center/what-is-cid-place-id-feature-id) | Google-defined client id (cid), a unique id of the local establishment. | Google 定义的客户端 id（cid），本地商家的唯一标识。 |
| `resp.200.result.reviews_count` | *the total number of reviews* | Total number of reviews available for the establishment. | 该商家可获取的评论总数。 |
| `resp.200.result.items_count` | *the number of reviews items in the results array* you can get more results by using the `depth` parameter when setting a task | Number of elements returned in the `items` array; raise the `depth` parameter at task creation to get more. | `items` 数组中返回的元素数量；在创建任务时调大 `depth` 参数可获取更多。 |
| `resp.200.items` | *found reviews* you can get more results by using the `depth` parameter when setting a task | Review entries collected for the establishment; raise `depth` at task creation to retrieve more. | 为该商家采集到的评论条目；创建任务时调大 `depth` 可获取更多。 |
| `resp.200.items.type` | *type of element = **‘images\_element’*** | Element type marker of the review entry. | 评论条目的元素类型标记。 |
| `resp.200.items.rank_group` | *position within a group of elements with identical `type` values* positions of elements with different `type` values are omitted from `rank_group` | Position of this entry within the group of elements sharing the same `type`. | 该条目在相同 `type` 元素分组内的位置。 |
| `resp.200.items.rank_absolute` | *absolute rank among all the listed reviews* absolute position among all reviews on the list | Absolute position of this review among all listed reviews. | 该评论在全部已列出评论中的绝对位置。 |
| `resp.200.items.position` | *the alignment of the review in SERP* can take the following values: `right` | Alignment of the review within the SERP layout. | 评论在 SERP 版式中的对齐位置。 |
| `resp.200.items.xpath` | *the [XPath](https://en.wikipedia.org/wiki/XPath) of the review* | XPath locating this review element within the parsed page. | 在解析页面中定位该评论元素的 XPath。 |
| `resp.200.items.review_text` | *the content of the review* | Body text of the review (auto-translated when applicable). | 评论正文内容（在适用时为自动翻译后的文本）。 |
| `resp.200.items.original_review_text` | *original content of the review* the original content of the review, no auto-translate applied | Review body in its original language, with no auto-translation applied. | 评论正文的原始语言版本，未做自动翻译。 |
| `resp.200.items.time_ago` | *the time of publication* indicates the time (in the ‘time ago’ format) when the review was listed | Relative publication time of the review in 'time ago' wording. | 评论发布时间的相对表述（'time ago' 格式）。 |
| `resp.200.items.timestamp` | *date and time when a review was published* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | Absolute publication time of the review in UTC, `yyyy-mm-dd hh-mm-ss +00:00`. | 评论发布的绝对时间（UTC），格式 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.items.rating` | *the rating score submitted by the reviewer* | Rating score given by the reviewer. | 该评论作者给出的评分。 |
| `resp.200.items.rating_type` | *the type of the rating* can take the following values: `Max5` | Scale used to express the rating; the business meaning is: `Max5` rated out of 5, `Percents` expressed as a percentage, `CustomMax` rated against a custom maximum. | 评分所采用的标度，业务含义为：`Max5` 表示满分 5 分制，`Percents` 表示百分比制，`CustomMax` 表示自定义满分制。 |
| `resp.200.items.value` | *the value of the rating* | Numeric value of the reviewer's rating. | 该评论作者评分的数值。 |
| `resp.200.items.votes_count` | *the amount of feedback* indicated the number of votes the review obtained | Number of helpfulness votes this review received. | 该评论获得的有用性投票数。 |
| `resp.200.items.rating_max` | *the maximum value for a `rating_type`* the maximum value for `Max5` is 5 | Maximum possible value for the given `rating_type` (for example 5 when the type is `Max5`). | 对应 `rating_type` 的最大可能取值（例如类型为 `Max5` 时为 5）。 |
| `resp.200.items.reviews_count` | *total number of reviews submitted by the reviewer* | Total number of reviews the reviewer has submitted overall. | 该评论作者累计提交的评论总数。 |
| `resp.200.items.photos_count` | *total number of photos submitted by the reviewer* | Total number of photos the reviewer has submitted overall. | 该评论作者累计提交的照片总数。 |
| `resp.200.items.local_guide` | *indicates whether the reviewer has a ‘local guide’ status* | Whether the reviewer holds Google 'Local Guide' status. | 该评论作者是否拥有 Google '本地向导'（Local Guide）身份。 |
| `resp.200.items.profile_name` | *profile name of the reviewer* | Display name of the reviewer's profile. | 评论作者的资料显示名称。 |
| `resp.200.items.profile_url` | *URL of the reviewer’s profile* | URL of the reviewer's profile. | 评论作者的资料页 URL。 |
| `resp.200.items.review_url` | *the URL of the review* | URL pointing directly to this review. | 直接指向该评论的 URL。 |
| `resp.200.items.profile_image_url` | *URL of the reviewer’s profile image* | URL of the reviewer's profile image. | 评论作者的头像图片 URL。 |
| `resp.200.items.owner_answer` | *text of the owner’s response* the owner’s response to the review | Owner's reply text to the review (auto-translated when applicable). | 商家所有者对该评论的回复文本（适用时为自动翻译）。 |
| `resp.200.items.original_owner_answer` | *original text of the owner’s response* the original response to the review, no auto-translate applied | Owner's reply in its original language, with no auto-translation applied. | 商家所有者回复的原始语言版本，未做自动翻译。 |
| `resp.200.items.owner_time_ago` | *publication time* indicates the time (in the ‘time ago’ format) when the owner submitted the response to the review | Relative time of the owner's reply in 'time ago' wording. | 商家回复时间的相对表述（'time ago' 格式）。 |
| `resp.200.items.owner_timestamp` | *date and time of the owner’s reply to the review* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | Absolute time of the owner's reply in UTC, `yyyy-mm-dd hh-mm-ss +00:00`. | 商家回复的绝对时间（UTC），格式 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.items.review_id` | *the unique identifier of a review on Google* example: `ChZDSUhNMG9nS0VJQ0FnSUMxbHFyMFlnEAE` | Unique identifier of the review on Google. | 该评论在 Google 上的唯一标识。 |
| `resp.200.items.images` | *images submitted by the reviewer* | Images attached to the review by the reviewer. | 评论作者随评论附上的图片。 |
| `resp.200.items.alt` | *alt tag of the image* | Alt text of the review image. | 评论图片的 alt 替代文本。 |
| `resp.200.items.url` | *relevant image URL from Google* | Relevant image URL from Google. | 来自 Google 的相关图片 URL。 |
| `resp.200.items.image_url` | *URL of the image featured in the review* | URL of an image featured in the review. | 评论中所附图片的 URL。 |
| `resp.200.items.review_highlights` | *review highlights* contains highlighted review criteria and assessments | Highlighted review criteria together with their assessments. | 评论中被高亮的评价维度及其评价结论。 |
| `resp.200.items.feature` | *reviewed feature* | Reviewed feature or aspect. | 被评价的特性或维度。 |
| `resp.200.items.assessment` | *feature assessment* | Assessment given for the reviewed feature. | 针对被评价特性给出的评价结论。 |

## get_dataforseo_business_data_google_reviews_tasks_ready

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | The **‘Tasks Ready’** endpoint is designed to provide you with the list of completed tasks, which haven’t been collected yet. If you don’t use the `postback_url`, you can receive the list of `id` for all completed tasks using this endpoint. Then, you can collect the results using the **‘Task GET’** endpoint. | List Google Reviews tasks that have finished processing and are ready to be collected. | 列出已处理完成、可供拉取的 Google 评论任务。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total tasks cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array returned with an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the parameters passed in the request’s URL* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.id` | *task identifier of the completed task* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | UUID of a completed task that is now ready to be collected; pass it to the matching `task_get` endpoint to download the results. | 已完成、可供拉取的任务的 UUID；将其传给对应的 `task_get` 端点即可下载结果。 |
| `resp.200.result.se` | *search engine specified when setting the task* can take the following values: `google` | Search engine the completed task was set for. | 该已完成任务所针对的搜索引擎。 |
| `resp.200.result.se_type` | *type of search engine* | Search engine type the completed task was set for. | 该已完成任务所针对的搜索引擎类型。 |
| `resp.200.result.date_posted` | *date when the task was posted (in the UTC format)* | Timestamp (UTC) when the task was originally submitted. | 任务最初提交的时间（UTC）。 |
| `resp.200.result.tag` | *user-defined task identifier* | User-defined label that was attached to the task when it was created, echoed back for matching. | 创建任务时附加的用户自定义标签，原样回显以便匹配。 |
| `resp.200.result.endpoint` | *URL for collecting the results of the task* | Relative endpoint URL to call for collecting this completed task's results. | 用于拉取该已完成任务结果的相对端点 URL。 |

## post_dataforseo_business_data_social_media_reddit_live

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | Social Media Reddit API provides information for each share of the target webpage on Reddit. For each specified Reddit URL, you will get subreddit and author names, permalink, title, and the number of subreddit members. | Synchronously retrieve Reddit mentions (subreddit posts that share the URL) for one or more target pages, returning the subreddit and post details for each. | 针对一个或多个目标页面，同步获取其在 Reddit 上的提及（分享该 URL 的 subreddit 帖子），并返回各自的 subreddit 与帖子详情。 |
| `req.targets` | *target URLs* **required field** target page should be specified with its absolute URL (including http:// or https://) example: `https://dataforseo.com/` **Note:** you can specify 10 targets maximum. You will be charged per earch URL you specify in this array | Target page URLs to analyse, each an absolute URL including the scheme; up to 10 targets, each billed separately. | 要分析的目标页面 URL，每个都需为含协议头的绝对 URL；最多 10 个目标，每个目标单独计费。 |
| `req.tag` | *user-defined task identifier* optional field *the character limit is 255* you can use this parameter to identify the task and match it with the result you will find the specified `tag` value in the `data` object of the response | Your own label for the task (up to 255 characters). It is echoed back in the response `data` so you can match results to requests. | 你为任务自定义的标签（最多 255 个字符）。会在响应的 `data` 中回显，便于将结果与请求匹配。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total tasks cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`**array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array returned with an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *unique task identifier in our system* in the [Universally unique identifier (UUID)](https://en.wikipedia.org/wiki/Universally_unique_identifier) format | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the same parameters that you specified in the POST request* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.type` | *type of element = **‘social\_media\_reddit\_item’*** | Element type marker, here `social_media_reddit_item`. | 元素类型标记，此处为 `social_media_reddit_item`。 |
| `resp.200.result.page_url` | *URL of the page the data is provided for* corresponding URL you specified in the `targets` array when setting a task | Target page URL this Reddit data corresponds to, matching an entry from the `targets` array. | 该 Reddit 数据所对应的目标页面 URL，与 `targets` 数组中的某项一致。 |
| `resp.200.result.reddit reviews` | *reddit reviews for the page\_url* | Reddit mentions/posts associated with the page_url. | 与 page_url 关联的 Reddit 提及/帖子。 |
| `resp.200.result.subreddit` | *the name of the subreddit* | Name of the subreddit where the URL was shared. | 分享该 URL 所在的 subreddit 名称。 |
| `resp.200.result.author_name` | *nickname of the author* nicknname of the user who published the post in the subreddit and shared the URL | Nickname of the user who posted the URL in the subreddit. | 在该 subreddit 中发布该 URL 的用户昵称。 |
| `resp.200.result.title` | *title of the subreddit post* | Title of the subreddit post. | 该 subreddit 帖子的标题。 |
| `resp.200.result.permalink` | *URL to the subreddit post* | Permanent URL of the subreddit post. | 该 subreddit 帖子的固定链接 URL。 |
| `resp.200.result.subreddit_members` | *number of subreddit members* | Number of members in the subreddit. | 该 subreddit 的成员数量。 |

## post_dataforseo_business_data_tripadvisor_reviews_task_post

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint provides results from the “Reviews” element on the [Tripadvisor](https://www.tripadvisor.com/) platform. The results are specific to the URL path or keyword you indicate, and and the selected location (see [the List of Locations](https://docs.dataforseo.com/v3/business_data/tripadvisor/locations.md)). | Create an asynchronous task that collects Tripadvisor reviews for a business identified by URL path or keyword, with optional filters by rating, traveler type, visit month and review keyword. | 创建一个异步任务，采集由 URL 路径或关键词指定商家的 Tripadvisor 评论，并可按评分、出行者类型、到访月份及评论关键词进行筛选。 |
| `req.url_path` | *URL path of the business entity* **required field if you do not specify `keyword`** URL path to the Tripadvisor page of the business entity; examples: `Hotel_Review-g60763-d23462501-Reviews-Margaritaville_Times_Square-New_York_City_New_York.html` `https://www.tripadvisor.com/Hotel_Review-g60763-d23462501-Reviews-Margaritaville_Times_Square-New_York_City_New_York.html` | Tripadvisor page URL path of the business entity. Provide this or `keyword`. You can obtain it from the Tripadvisor Search items' `url_path`. | 商家在 Tripadvisor 上的页面 URL 路径。需与 `keyword` 择一提供。可从 Tripadvisor 搜索结果条目的 `url_path` 获取。 |
| `req.keyword` | *keyword* **required field if you do not specify `url_path`** the keyword you specify should indicate a name of an existing business or prominent place on Tripadvisor; you can specify up to 700 characters in the `keyword` filed; all %## will be decoded (plus character ‘+’ will be decoded to a space character); if you need to use the “%” character for your `keyword`, please specify it as “%25” | Name of an existing Tripadvisor business or place (up to 700 characters; percent-encoding decoded). Provide this or `url_path`. | Tripadvisor 上已存在的商家或地点名称（最多 700 个字符；会解码百分号转义）。需与 `url_path` 择一提供。 |
| `req.location_name` | *full name of search engine location* **required field if you don’t specify `location_code` or `url_path`** you can receive the list of available locations with `location_name` by making a separate request to the `https://api.dataforseo.com/v3/business_data/tripadvisor/locations` example: `London,England,United Kingdom` | Full location name; required if you specify neither `location_code` nor `url_path`. The available list comes from the Tripadvisor `/locations` endpoint. | 完整地区名称；若既未指定 `location_code` 也未指定 `url_path` 则必填。可用列表来自 Tripadvisor `/locations` 端点。 |
| `req.location_code` | *search engine location code* **required field if you don’t specify `location_name` or `url_path`** you can receive the list of available locations with `location_code` by making a separate request to the `https://api.dataforseo.com/v3/business_data/tripadvisor/locations` example: `1003854` | Numeric location code; required if you specify neither `location_name` nor `url_path`. The available list comes from the Tripadvisor `/locations` endpoint. | 数字地区代码；若既未指定 `location_name` 也未指定 `url_path` 则必填。可用列表来自 Tripadvisor `/locations` 端点。 |
| `req.priority` | *task priority* optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the [Pricing](https://dataforseo.com/pricing/business-data/business-data-api-tripadvisor-pricing "Pricing") page. | Execution priority of the task: `1` runs at normal priority, `2` runs at high priority for faster turnaround at an extra charge. | 任务执行优先级：`1` 为普通优先级，`2` 为高优先级，可更快返回但会额外计费。 |
| `req.language_name` | *full name of search engine language* optional field **if you use this field, your account will be charged for one extra request** you can receive the list of available languages with `language_name` by making a separate request to the `https://api.dataforseo.com/v3/business_data/tripadvisor/languages` example: `English` You will be additionally charged for setting a language parameter in this endpoint. The cost can be calculated on the [Pricing](https://dataforseo.com/pricing/business-data/business-data-api-tripadvisor-pricing "Pricing") page. | Full language name. Using this field incurs one extra request charge. The available list comes from the Tripadvisor `/languages` endpoint. | 完整语言名称。使用该字段会额外计费一次请求。可用列表来自 Tripadvisor `/languages` 端点。 |
| `req.language_code` | *search engine language code* optional field **if you use this field, your account will be charged for one extra request** you can receive the list of available languages with `language_code` by making a separate request to the `https://api.dataforseo.com/v3/business_data/tripadvisor/languages` example: `en` You will be additionally charged for setting a language parameter in this endpoint. The cost can be calculated on the [Pricing](https://dataforseo.com/pricing/business-data/business-data-api-tripadvisor-pricing "Pricing") page. | Language code. Using this field incurs one extra request charge. The available list comes from the Tripadvisor `/languages` endpoint. | 语言代码。使用该字段会额外计费一次请求。可用列表来自 Tripadvisor `/languages` 端点。 |
| `req.depth` | *parsing depth* optional field number of reviews in SERP; we strongly recommend setting the parsing depth in the multiples of ten, because our systems processes ten reviews in a row; default value: `10`; max value: `4490` **Your account will be billed per each SERP containing up to 10 results;** Setting depth above 10 may result in additional charges if the search engine returns more than 10 results; The cost can be calculated on the [Pricing](https://dataforseo.com/pricing/business-data/business-data-api-tripadvisor-pricing "Pricing") page. | Number of reviews to parse; best set in multiples of ten (processed ten at a time). Default 10, max 4490. Billing is per SERP of up to 10 results. | 要解析的评论数量；建议设为 10 的倍数（每次处理十条）。默认 10，最大 4490。计费以每 10 条结果一个 SERP 为单位。 |
| `req.ratings` | *Tripadvisor traveler rating for a place of interest* optional field rating based on the written reviews by a traveler after they visited a place. possible values: `excellent`, `very_good`, `average`, `poor`, `terrible` you can specify several values at once | Filter reviews by Tripadvisor traveler rating; business meaning of values: `excellent`, `very_good`, `average`, `poor`, `terrible`. Multiple values allowed. | 按 Tripadvisor 旅行者评分筛选评论；取值业务含义：`excellent` 极佳、`very_good` 很好、`average` 一般、`poor` 较差、`terrible` 很差。可同时指定多个值。 |
| `req.visit_type` | *filter by type of travelers who left a review* optional field possible values: `families`, `couples`, `solo`, `business`, `friends` you can specify several values at once | Filter by traveler type; business meaning of values: `families`, `couples`, `solo`, `business`, `friends`. Multiple values allowed. | 按出行者类型筛选；取值业务含义：`families` 家庭、`couples` 情侣、`solo` 独行、`business` 商务、`friends` 朋友。可同时指定多个值。 |
| `req.months` | *filter by months when a traveler made a visit* optional field possible values: `january`, `february`, `march`, `april`, `may`, `april`, `june`, `july`, `august`, `september`, `october`, `november`, `december` you can specify several values at once | Filter by the month of the traveler's visit, given as month names (e.g. `january`). Multiple values allowed. | 按旅行者到访的月份筛选，以月份英文名给出（如 `january`）。可同时指定多个值。 |
| `req.search_reviews_keyword` | *search reviews containing a specified keyword* example: `dessert` | Return only reviews that contain the specified keyword. | 仅返回包含指定关键词的评论。 |
| `req.sort_by` | *results sorting parameters* optional field you can use this field to sort the results; possible types of sorting: `most_recent` `detailed_reviews` | Sort order of the returned reviews; business meaning: `most_recent` newest first, `detailed_reviews` most detailed first. | 返回评论的排序方式；业务含义：`most_recent` 最新优先、`detailed_reviews` 内容最详尽优先。 |
| `req.translate_reviews` | *translate reviews according to the URL path* optional field if set to `true`, returned reviews will be translated to the language matching the specified `url_path`; for example, if `url_path` contains `tripadvisor.it` and `translate_reviews` is `true`, reviews will be translated to the Italian language; default value: `true` you can learn more about how reviews are translated in [this Help Center article](https://dataforseo.com/help-center/how-to-translate-reviews-in-tripadvisor-business-data-api) | When `true`, reviews are translated to the language matching the `url_path` (e.g. Italian for a `tripadvisor.it` path). | 为 `true` 时，评论会被翻译为与 `url_path` 匹配的语言（例如 `tripadvisor.it` 路径对应意大利语）。 |
| `req.tag` | *user-defined task identifier* optional field *the character limit is 255* you can use this parameter to identify the task and match it with the result you will find the specified `tag` value in the `data` object of the response | Your own label for the task (up to 255 characters). It is echoed back in the response `data` so you can match results to requests. | 你为任务自定义的标签（最多 255 个字符）。会在响应的 `data` 中回显，便于将结果与请求匹配。 |
| `req.postback_url` | *return URL for sending task results* optional field once the task is completed, we will send a POST request with its results compressed in the `gzip` format to the `postback_url` you specified you can use the ‘$id’ string as a `$id` variable and ‘$tag’ as urlencoded `$tag` variable. We will set the necessary values before sending the request. example: `http://your-server.com/postbackscript?id=$id` `http://your-server.com/postbackscript?id=$id&tag=$tag` **Note:** special characters in `postback_url` will be urlencoded; i.a., the `#` character will be encoded into `%23`learn more on our [Help Center](https://dataforseo.com/help-center/pingbacks-postbacks-with-dataforseo-api) | URL to which the completed task results are pushed via POST (gzip-compressed). Use the `$id`/`$tag` placeholders in the URL to have them substituted automatically. | 任务完成后用于以 POST 推送结果（gzip 压缩）的回调 URL。可在 URL 中使用 `$id`/`$tag` 占位符，系统会自动替换为实际值。 |
| `req.pingback_url` | *notification URL of a completed task* optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a `$id` variable and ‘$tag’ as urlencoded `$tag` variable. We will set the necessary values before sending the request. example: `http://your-server.com/pingscript?id=$id` `http://your-server.com/pingscript?id=$id&tag=$tag` **Note:** special characters in `pingback_url` will be urlencoded; i.a., the `#` character will be encoded into `%23`learn more on our [Help Center](https://dataforseo.com/help-center/pingbacks-postbacks-with-dataforseo-api) | URL that is pinged via GET when the task completes, signalling that results are ready to collect. Supports the `$id`/`$tag` placeholders. | 任务完成时通过 GET 触达的通知 URL，用于提示结果已就绪。支持 `$id`/`$tag` 占位符。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total tasks cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`**array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array returned with an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *unique task identifier in our system* in the [Universally unique identifier (UUID)](https://en.wikipedia.org/wiki/Universally_unique_identifier) format | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the same parameters that you specified in the POST request* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* in this case, the value will be `null` | Result array; for this task-creation response the value is `null`. | 结果数组；对于本任务创建响应，其值为 `null`。 |

## get_dataforseo_business_data_tripadvisor_search_task_get_id

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint will provide you with data on businesses listed on the [Tripadvisor](https://www.tripadvisor.com) platform. The results obtained through this endpoint are specific to the location (see [the List of Tripadvisor Locations](https://docs.dataforseo.com/v3/business_data/tripadvisor/locations.md)) and keyword parameters used in the POST request. | Fetch the results of a previously created Tripadvisor Search task by its id, returning matched business listings with category, price rate and rating data. | 按任务 id 拉取此前创建的 Tripadvisor 搜索任务结果，返回匹配的商家列表及其分类、价格水平与评分数据。 |
| `param:id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** you will be able to use it within **30 days** to request the results of the task at any time | UUID of the task whose results you want to fetch. The task remains retrievable for 30 days after creation. | 要拉取结果的任务 UUID。任务自创建起 30 天内可随时取回结果。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total *tasks* cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array that were returned an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the same parameters that you specified in the POST request* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.keyword` | *keyword received in a POST array* this field will contain the `alias` parameter if it was specified in a POST array | Keyword echoed from the POST request; if an `alias` was specified in the request it is returned here. | 从 POST 请求回显的关键词；若请求中指定了 `alias`，此处会返回该值。 |
| `resp.200.result.se_domain` | *search engine domain in a POST array* | Search engine domain echoed from the POST request. | 从 POST 请求回显的搜索引擎域名。 |
| `resp.200.result.location_code` | *location code in a POST array* | Location code echoed from the POST request. | 从 POST 请求回显的地区代码。 |
| `resp.200.result.language_code` | *language code in a POST array* | Language code echoed from the POST request. | 从 POST 请求回显的语言代码。 |
| `resp.200.result.check_url` | *direct URL to Tripadvisor results* you can use it to make sure that we provided accurate results | Direct URL to the Tripadvisor results, so you can verify the data. | 指向 Tripadvisor 结果的直达 URL，可用于核对数据。 |
| `resp.200.result.datetime` | *date and time when the result was received* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | UTC timestamp when the result was collected, formatted `yyyy-mm-dd hh-mm-ss +00:00`. | 结果采集的时间（UTC），格式为 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.result.item_types` | *item types encountered in the result* possible item types: `tripadvisor_search_organic` | Item types present in the result; possible value: `tripadvisor_search_organic`. | 结果中出现的元素类型；可能取值为 `tripadvisor_search_organic`。 |
| `resp.200.result.se_results_count` | *the total number of results* | Total number of results found. | 找到的结果总数。 |
| `resp.200.result.items_count` | *the number of items in the results array* you can get more results by using the `depth` parameter when setting a task | Number of elements returned in the `items` array; raise the `depth` parameter at task creation to get more. | `items` 数组中返回的元素数量；在创建任务时调大 `depth` 参数可获取更多。 |
| `resp.200.items` | *Tripadvisor search listing results* you can get more results by using the `depth` parameter when setting a task | Tripadvisor search listing results; raise `depth` at task creation for more. | Tripadvisor 搜索列表结果；创建任务时调大 `depth` 可获取更多。 |
| `resp.200.items.type` | *type of Tripadvisor search result* possible types: `tripadvisor_search_organic` | Result type, here `tripadvisor_search_organic`. | 结果类型，此处为 `tripadvisor_search_organic`。 |
| `resp.200.items.rank_group` | *position within a group of elements with identical `type` values* positions of elements with different `type` values are omitted from `rank_group` | Position of this entry within the group of elements sharing the same `type`. | 该条目在相同 `type` 元素分组内的位置。 |
| `resp.200.items.rank_absolute` | *absolute rank among all the listed results* absolute position among all reviews on the list | Absolute rank among all the listed results. | 在全部已列出结果中的绝对排名。 |
| `resp.200.items.title` | *name of the business entity* | Name of the business entity. | 商家实体的名称。 |
| `resp.200.items.url_path` | *URL path of the business entity* URL path to the Tripadvisor page of the business entity you can use this identifier to collect reviews for the business entity using [Tripadvisor Reviews](https://docs.dataforseo.com/v3/business_data/tripadvisor/reviews/task_post.md) | Tripadvisor page URL path of the business; reuse it with the Tripadvisor Reviews endpoint to collect its reviews. | 商家的 Tripadvisor 页面 URL 路径；可配合 Tripadvisor Reviews 端点采集其评论。 |
| `resp.200.items.is_sponsored` | *indicates a sponsored placement* if `true`, related `tripadvisor_search_organic` item is a paid advertising on Tripadvisor | Whether the item is a paid (sponsored) Tripadvisor placement. | 该条目是否为 Tripadvisor 上的付费（赞助）展示位。 |
| `resp.200.items.reviews_count` | *the total number of reviews* | Total number of reviews for the business. | 该商家的评论总数。 |
| `resp.200.items.category` | *place category* | Place category. | 地点分类。 |
| `resp.200.items.price_rate` | *average price rate* | Average price rate of the place. | 该地点的平均价格水平。 |
| `resp.200.items.rating` | *the rating score of the establishment submitted by the reviewers* | Rating score of the establishment submitted by reviewers. | 评论者给出的该商家评分。 |
| `resp.200.items.rating_type` | *the type of the rating* can take the following values: `Max5` | Scale used to express the rating; the business meaning is: `Max5` rated out of 5, `Percents` expressed as a percentage, `CustomMax` rated against a custom maximum. | 评分所采用的标度，业务含义为：`Max5` 表示满分 5 分制，`Percents` 表示百分比制，`CustomMax` 表示自定义满分制。 |
| `resp.200.items.value` | *the value of the rating* | Numeric value of the rating. | 评分的数值。 |
| `resp.200.items.votes_count` | *the amount of feedback* indicates the number of votes the establishment obtained | Number of votes the establishment received. | 该商家获得的投票数。 |
| `resp.200.items.rating_max` | *the maximum value for a `rating_type`* the maximum value for `Max5` is 5 | Maximum possible value for the given `rating_type` (for example 5 when the type is `Max5`). | 对应 `rating_type` 的最大可能取值（例如类型为 `Max5` 时为 5）。 |

## get_dataforseo_business_data_tripadvisor_search_tasks_ready

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | The **‘Tasks Ready’** endpoint is designed to provide you with the list of completed tasks, which haven’t been collected yet. If you don’t use the `postback_url`, you can receive the list of `id` for all completed tasks using this endpoint. Then, you can collect the results using the **‘Task GET’** endpoint. | List Tripadvisor Search tasks that have finished processing and are ready to be collected. | 列出已处理完成、可供拉取的 Tripadvisor 搜索任务。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total tasks cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array returned with an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the parameters passed in the request’s URL* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.id` | *task identifier of the completed task* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | UUID of a completed task that is now ready to be collected; pass it to the matching `task_get` endpoint to download the results. | 已完成、可供拉取的任务的 UUID；将其传给对应的 `task_get` 端点即可下载结果。 |
| `resp.200.result.se` | *search engine specified when setting the task* can take the following values: `tripadvisor` | Search engine the completed task was set for. | 该已完成任务所针对的搜索引擎。 |
| `resp.200.result.se_type` | *search engine type* | Search engine type the completed task was set for. | 该已完成任务所针对的搜索引擎类型。 |
| `resp.200.result.date_posted` | *date when the task was posted (in the UTC format)* | Timestamp (UTC) when the task was originally submitted. | 任务最初提交的时间（UTC）。 |
| `resp.200.result.tag` | *user-defined task identifier* | User-defined label that was attached to the task when it was created, echoed back for matching. | 创建任务时附加的用户自定义标签，原样回显以便匹配。 |
| `resp.200.result.endpoint` | *URL for collecting the results of the task* | Relative endpoint URL to call for collecting this completed task's results. | 用于拉取该已完成任务结果的相对端点 URL。 |

## post_dataforseo_business_data_trustpilot_reviews_task_post

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint provides reviews published on the [Trustpilot](https://www.trustpilot.com) platform for the local establishment specified in the `domain` field. | Create an asynchronous task that collects Trustpilot reviews for an establishment identified by its domain; poll tasks_ready and fetch results by id. | 创建一个异步任务，采集由域名指定商家的 Trustpilot 评论；随后轮询 tasks_ready 并按 id 拉取结果。 |
| `req.domain` | *domain of the local establishment* **required field** domain of the local establishment on Trustpilot; you can find the domain in the URL of every business listed on Trustpilot example: `www.thepearlsource.com` `https://www.trustpilot.com/review/www.thepearlsource.com` | Domain of the establishment on Trustpilot, found in the URL of any Trustpilot business listing, e.g. `www.thepearlsource.com`. | 该商家在 Trustpilot 上的域名，可在任一 Trustpilot 商家页面的 URL 中找到，例如 `www.thepearlsource.com`。 |
| `req.sort_by` | *results sorting parameter* optional field you can use this field to sort the results; possible sorting parameters: `recency` — most recent reviews first; `relevance` — most relevant reviews first; default value: `relevance` | Sort order of the returned reviews; business meaning: `recency` most recent first, `relevance` most relevant first (default). | 返回评论的排序方式；业务含义：`recency` 最新优先、`relevance` 最相关优先（默认）。 |
| `req.priority` | *task priority* optional field can take the following values: 1 – normal execution priority (set by default) 2 – high execution priority You will be additionally charged for the tasks with high execution priority. The cost can be calculated on the [Pricing](https://dataforseo.com/pricing/business-data/business-data-api-trustpilot-reviews-pricing "Pricing") page. | Execution priority of the task: `1` runs at normal priority, `2` runs at high priority for faster turnaround at an extra charge. | 任务执行优先级：`1` 为普通优先级，`2` 为高优先级，可更快返回但会额外计费。 |
| `req.depth` | *parsing depth* optional field number of reviews to be returned from the API response we strongly recommend setting the parsing depth in the multiples of twenty, because our system processes twenty reviews in a row default value: `20` maximum value: `200` **Your account will be billed per each SERP containing up to 20 results;** Setting depth above 20 may result in additional charges if the search engine returns more than 20 results; The cost can be calculated on the [Pricing](https://dataforseo.com/pricing/business-data/business-data-api-trustpilot-reviews-pricing "Pricing") page. | Number of reviews to return; best set in multiples of twenty (processed twenty at a time). Default 20, max 200. Billing is per SERP of up to 20 results. | 要返回的评论数量；建议设为 20 的倍数（每次处理二十条）。默认 20，最大 200。计费以每 20 条结果一个 SERP 为单位。 |
| `req.tag` | *user-defined task identifier* optional field *the character limit is 255* you can use this parameter to identify the task and match it with the result you will find the specified `tag` value in the `data` object of the response | Your own label for the task (up to 255 characters). It is echoed back in the response `data` so you can match results to requests. | 你为任务自定义的标签（最多 255 个字符）。会在响应的 `data` 中回显，便于将结果与请求匹配。 |
| `req.postback_url` | *return URL for sending task results* optional field once the task is completed, we will send a POST request with its results compressed in the `gzip` format to the `postback_url` you specified you can use the ‘$id’ string as a `$id` variable and ‘$tag’ as urlencoded `$tag` variable. We will set the necessary values before sending the request. example: `http://your-server.com/postbackscript?id=$id` `http://your-server.com/postbackscript?id=$id&tag=$tag` **Note:** special characters in `postback_url` will be urlencoded; i.a., the `#` character will be encoded into `%23`learn more on our [Help Center](https://dataforseo.com/help-center/pingbacks-postbacks-with-dataforseo-api) | URL to which the completed task results are pushed via POST (gzip-compressed). Use the `$id`/`$tag` placeholders in the URL to have them substituted automatically. | 任务完成后用于以 POST 推送结果（gzip 压缩）的回调 URL。可在 URL 中使用 `$id`/`$tag` 占位符，系统会自动替换为实际值。 |
| `req.pingback_url` | *notification URL of a completed task* optional field when a task is completed we will notify you by GET request sent to the URL you have specified you can use the ‘$id’ string as a `$id` variable and ‘$tag’ as urlencoded `$tag` variable. We will set the necessary values before sending the request. example: `http://your-server.com/pingscript?id=$id` `http://your-server.com/pingscript?id=$id&tag=$tag` **Note:** special characters in `pingback_url` will be urlencoded; i.a., the `#` character will be encoded into `%23`learn more on our [Help Center](https://dataforseo.com/help-center/pingbacks-postbacks-with-dataforseo-api) | URL that is pinged via GET when the task completes, signalling that results are ready to collect. Supports the `$id`/`$tag` placeholders. | 任务完成时通过 GET 触达的通知 URL，用于提示结果已就绪。支持 `$id`/`$tag` 占位符。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total tasks cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`**array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array returned with an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *unique task identifier in our system* in the [Universally unique identifier (UUID)](https://en.wikipedia.org/wiki/Universally_unique_identifier) format | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the same parameters that you specified in the POST request* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* in this case, the value will be `null` | Result array; for this task-creation response the value is `null`. | 结果数组；对于本任务创建响应，其值为 `null`。 |

## get_dataforseo_business_data_trustpilot_search_task_get_id

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | This endpoint provides a list of business profiles listed on the [Trustpilot](https://www.trustpilot.com) platform. The returned results are relevant to the `keyword` specified in a POST request. We emulate set parameters with the highest accuracy so that the results you receive match the actual search results for the specified parameters at the time of task setting. You can always check the returned results accessing the `check_url` in the Incognito mode to make sure the received data is entirely relevant. Note that user preferences, search history, and other personalized search factors are ignored by our system and thus will not be reflected in the returned results. | Fetch the results of a previously created Trustpilot Search task by its id, returning matched establishments with their domain, review count and rating data. | 按任务 id 拉取此前创建的 Trustpilot 搜索任务结果，返回匹配的商家及其域名、评论数与评分数据。 |
| `param:id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** you will be able to use it within **30 days** to request the results of the task at any time | UUID of the task whose results you want to fetch. The task remains retrievable for 30 days after creation. | 要拉取结果的任务 UUID。任务自创建起 30 天内可随时取回结果。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total *tasks* cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array that were returned an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the same parameters that you specified in the POST request* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.keyword` | *keyword in a POST array* | Keyword echoed from the POST request. | 从 POST 请求回显的关键词。 |
| `resp.200.result.se_domain` | *search engine domain in a POST array* | Search engine domain echoed from the POST request. | 从 POST 请求回显的搜索引擎域名。 |
| `resp.200.result.check_url` | *direct URL to search engine results* you can use it to make sure that we provided accurate results | Direct URL to the search engine results page, so you can verify the data against the live SERP. | 指向搜索引擎结果页的直达 URL，可用于将数据与实时 SERP 核对。 |
| `resp.200.result.datetime` | *date and time when the result was received* in the UTC format: “yyyy-mm-dd hh-mm-ss +00:00” example: `2019-11-15 12:57:46 +00:00` | UTC timestamp when the result was collected, formatted `yyyy-mm-dd hh-mm-ss +00:00`. | 结果采集的时间（UTC），格式为 `yyyy-mm-dd hh-mm-ss +00:00`。 |
| `resp.200.result.items_count` | *the number of items in the results array* you can get more results by using the `depth` parameter when setting a task | Number of items in the results array; raise `depth` at task creation for more. | 结果数组中的元素数量；创建任务时调大 `depth` 可获取更多。 |
| `resp.200.items` | *found reviews* you can get more results by using the `depth` parameter when setting a task | Trustpilot search listing results; raise `depth` at task creation for more. | Trustpilot 搜索列表结果；创建任务时调大 `depth` 可获取更多。 |
| `resp.200.items.type` | *the result’s type* possible result types: `"trustpilot_search_organic"` | Result type, here `trustpilot_search_organic`. | 结果类型，此处为 `trustpilot_search_organic`。 |
| `resp.200.items.rank_group` | *position within a group of elements with identical `type` values* positions of elements with different `type` values are omitted from `rank_group` | Position of this entry within the group of elements sharing the same `type`. | 该条目在相同 `type` 元素分组内的位置。 |
| `resp.200.items.rank_absolute` | *absolute rank among all the listed reviews* absolute position among all reviews on the list | Absolute rank among all the listed results. | 在全部已列出结果中的绝对排名。 |
| `resp.200.items.title` | *title of the establishment* | Title of the establishment. | 商家的名称。 |
| `resp.200.items.domain` | *domain of the establishment* | Domain of the establishment. | 商家的域名。 |
| `resp.200.items.url` | *URL to the establishment* | URL of the establishment on Trustpilot. | 商家在 Trustpilot 上的 URL。 |
| `resp.200.items.reviews_count` | *the total number of reviews* | Total number of reviews for the establishment. | 该商家的评论总数。 |
| `resp.200.items.rating` | *the rating score of the establishment submitted by reviewers* | Rating score of the establishment submitted by reviewers. | 评论者给出的该商家评分。 |
| `resp.200.items.rating_type` | *the type of the rating* can take the following values: `Max5` | Scale used to express the rating; the business meaning is: `Max5` rated out of 5, `Percents` expressed as a percentage, `CustomMax` rated against a custom maximum. | 评分所采用的标度，业务含义为：`Max5` 表示满分 5 分制，`Percents` 表示百分比制，`CustomMax` 表示自定义满分制。 |
| `resp.200.items.value` | *the value of the rating* | Numeric value of the rating. | 评分的数值。 |
| `resp.200.items.votes_count` | *the amount of feedback* indicates the number of votes the establishment obtained | Number of votes the establishment received. | 该商家获得的投票数。 |
| `resp.200.items.rating_max` | *the maximum value for a `rating_type`* the maximum value for `Max5` is 5 | Maximum possible value for the given `rating_type` (for example 5 when the type is `Max5`). | 对应 `rating_type` 的最大可能取值（例如类型为 `Max5` 时为 5）。 |

## get_dataforseo_business_data_trustpilot_search_tasks_ready

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | The **‘Tasks Ready’** endpoint is designed to provide you with the list of completed tasks, which haven’t been collected yet. If you don’t use the `postback_url`, you can receive the list of `id` for all completed tasks using this endpoint. Then, you can collect the results using the **‘Task GET’** endpoint. | List Trustpilot Search tasks that have finished processing and are ready to be collected. | 列出已处理完成、可供拉取的 Trustpilot 搜索任务。 |
| `resp.200.version` | *the current version of the API* | Top-level API metadata object describing this response, including the API version and the aggregated status, timing and cost of the request. | 顶层 API 元数据对象，包含本次响应的 API 版本，以及整批请求的聚合状态、耗时与计费信息。 |
| `resp.200.version.status_code` | *general status code* you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) **Note:** we strongly recommend designing a necessary system for handling related exceptional or error conditions | Overall status code for the whole request. Use it as the first signal of success or failure before reading the tasks array; the full code list is in the DataForSEO errors reference. | 整个请求的总体状态码。读取 tasks 数组前应先用它判断请求整体成功或失败；完整状态码列表见 DataForSEO 错误码文档。 |
| `resp.200.version.status_message` | *general informational message* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message that accompanies the overall status code, e.g. `Ok` or an error explanation. | 与总体状态码配套的可读说明信息，例如 `Ok` 或错误原因。 |
| `resp.200.version.time` | *execution time, seconds* | Wall-clock time the platform spent handling the whole request, in seconds. | 平台处理整个请求所花费的时间，单位为秒。 |
| `resp.200.version.cost` | *total tasks cost, USD* | Total amount charged for all tasks in this response, in USD. | 本次响应中所有任务的总计费金额，单位为美元（USD）。 |
| `resp.200.version.tasks_count` | *the number of tasks in the **`tasks`** array* | Number of task objects contained in the `tasks` array. | `tasks` 数组中包含的任务对象数量。 |
| `resp.200.version.tasks_error` | *the number of tasks in the **`tasks`** array returned with an error* | Number of task objects in the `tasks` array that finished with an error; if greater than zero, inspect each task's own status code. | `tasks` 数组中以错误结束的任务数量；若大于 0，应逐个检查任务自身的状态码。 |
| `resp.200.tasks` | *array of tasks* | Array of task objects; each entry carries the per-task status plus its `result` payload. | 任务对象数组，每个元素携带单个任务的状态及其 `result` 结果数据。 |
| `resp.200.tasks.id` | *task identifier* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | Unique identifier of this task in UUID format. Reuse it to fetch results from the matching `task_get` endpoint within the task's retention window. | 本任务在系统中的唯一标识，UUID 格式。可在任务保留期内用它向对应的 `task_get` 端点拉取结果。 |
| `resp.200.tasks.status_code` | *status code of the task* generated by DataForSEO; can be within the following range: 10000-60000 you can find the full list of the response codes [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Status code of this individual task (range 10000-60000). A value in the 20000 range means success; other ranges signal queued, in-progress or error states. | 单个任务的状态码（范围 10000-60000）。20000 区间表示成功，其它区间表示排队、处理中或错误等状态。 |
| `resp.200.tasks.status_message` | *informational message of the task* you can find the full list of general informational messages [here](https://docs.dataforseo.com/v3/appendix/errors.md) | Human-readable message describing the outcome of this task, paired with `tasks.status_code`. | 描述该任务处理结果的可读信息，与 `tasks.status_code` 配套。 |
| `resp.200.tasks.time` | *execution time, seconds* | Wall-clock time spent processing this task, in seconds. | 处理该任务所花费的时间，单位为秒。 |
| `resp.200.tasks.cost` | *cost of the task, USD* | Amount charged for this individual task, in USD. | 该单个任务的计费金额，单位为美元（USD）。 |
| `resp.200.tasks.result_count` | *number of elements in the `result` array* | Number of elements in this task's `result` array. | 该任务 `result` 数组中的元素数量。 |
| `resp.200.tasks.path` | *URL path* | URL path segments of the endpoint that produced this task, useful for tracing which call the result belongs to. | 生成该任务的端点 URL 路径片段，便于追溯结果对应的调用。 |
| `resp.200.tasks.data` | *contains the parameters passed in the request’s URL* | Echo of the parameters you submitted for this task, returned so you can correlate the result with the original request. | 回显你为该任务提交的参数，便于将结果与原始请求对应起来。 |
| `resp.200.result` | *array of results* | Array holding the actual payload of this task. | 承载该任务实际结果数据的数组。 |
| `resp.200.result.id` | *task identifier of the completed task* **unique task identifier in our system in the [UUID](https://en.wikipedia.org/wiki/Universally_unique_identifier) format** | UUID of a completed task that is now ready to be collected; pass it to the matching `task_get` endpoint to download the results. | 已完成、可供拉取的任务的 UUID；将其传给对应的 `task_get` 端点即可下载结果。 |
| `resp.200.result.se` | *search engine specified when setting the task* can take the following values: `trustpilot` | Search engine the completed task was set for. | 该已完成任务所针对的搜索引擎。 |
| `resp.200.result.se_type` | *search engine type* | Search engine type the completed task was set for. | 该已完成任务所针对的搜索引擎类型。 |
| `resp.200.result.date_posted` | *date when the task was posted (in the UTC format)* | Timestamp (UTC) when the task was originally submitted. | 任务最初提交的时间（UTC）。 |
| `resp.200.result.tag` | *user-defined task identifier* | User-defined label that was attached to the task when it was created, echoed back for matching. | 创建任务时附加的用户自定义标签，原样回显以便匹配。 |
| `resp.200.result.endpoint` | *URL for collecting the results of the task* | Relative endpoint URL to call for collecting this completed task's results. | 用于拉取该已完成任务结果的相对端点 URL。 |

