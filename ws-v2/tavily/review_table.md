# 增强对照表（源英文 → 加强英文 → 中文本地化）

> spec 共 4 个接口，95 个对外字段/参数。逐字段对照「原文 → 加强 → 本地化」三态，判断改得更好还是更坏。

## POST /tavily/search

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | Execute a search query using Tavily Search. | Run a web search through Tavily and return ranked results, with optional LLM-generated answer, images, raw page content and credit usage. Tune relevance and latency via search_depth, and narrow scope with topic, time window, domain and country filters. | 通过 Tavily 执行联网搜索，返回按相关度排序的结果，并可选附带 LLM 生成的答案、图片、原始网页内容与积分用量。可用 search_depth 在相关度与延迟间取舍，并以主题、时间窗、域名和国家等筛选缩小范围。 |
| `req.query` | The search query to execute with Tavily. | Natural-language search query to run against the web. | 要在网络上检索的自然语言查询。 |
| `req.search_depth` | Controls the latency vs. relevance tradeoff. | Latency vs. relevance tradeoff: lighter modes return faster but shallower results, heavier modes search more thoroughly at higher latency and cost.<br>[⚠️Note:各枚举值与延迟/成本的具体对应关系源码未说明，待研发确认。] | 延迟与相关度的取舍：较轻量的模式返回更快但更浅，较重的模式检索更彻底，但延迟和成本更高。<br>[⚠️批注:各枚举值与延迟/成本的具体对应关系源码未说明，待研发确认。] |
| `req.chunks_per_source` | Maximum number of relevant chunks returned per source. | How many of the most relevant text chunks to keep from each matched source. | 每个命中来源保留的最相关文本片段数量。 |
| `req.max_results` | Maximum number of search results to return. | Upper bound on the number of search results returned. | 返回搜索结果数量的上限。 |
| `req.topic` | Category of the search. | Search category that biases ranking and freshness: `general` for broad web, `news` for recent journalism, `finance` for financial sources. | 搜索类别，会影响排序与时效偏好：`general` 面向通用网页，`news` 面向时事新闻，`finance` 面向金融来源。 |
| `req.time_range` | Time range to filter results based on publish date. | Restrict results to content published within a recent window relative to now; both full words and single-letter aliases are accepted. | 将结果限制在相对当前的近期时间窗内发布的内容；支持完整单词与单字母简写两种写法。 |
| `req.start_date` | Return results after the specified start date. | Keep only results published on or after this date. | 仅保留在该日期当天或之后发布的结果。 |
| `req.end_date` | Return results before the specified end date. | Keep only results published on or before this date. | 仅保留在该日期当天或之前发布的结果。 |
| `req.include_answer` | Include an LLM-generated answer to the query. | When enabled, attach a concise LLM-generated answer to the query in the response. | 开启后，响应中附带一段由 LLM 生成的简洁问题答案。 |
| `req.include_raw_content` | Include cleaned and parsed HTML content of each search result. | When enabled, attach the cleaned, parsed page content of each result rather than just a snippet. | 开启后，为每条结果附带清洗解析后的完整页面内容，而不仅是摘要片段。 |
| `req.include_images` | Perform an image search and include results. | When enabled, also run an image search and return matching images. | 开启后，额外执行图片搜索并返回匹配的图片。 |
| `req.include_image_descriptions` | Add descriptive text for each image when include_images is true. | When enabled, add a descriptive caption to each returned image; only takes effect when image results are requested. | 开启后，为每张返回的图片附加描述文字；仅在请求图片结果时生效。 |
| `req.include_favicon` | Include the favicon URL for each result. | When enabled, include the favicon URL of each result's source site. | 开启后，为每条结果附带来源站点的 favicon URL。 |
| `req.include_domains` | List of domains to specifically include in the search results. | Restrict the search to only these domains; results from other sites are dropped. | 将搜索限定在这些域名内，其他站点的结果会被剔除。 |
| `req.exclude_domains` | List of domains to specifically exclude from the search results. | Drop any results coming from these domains. | 剔除来自这些域名的结果。 |
| `req.country` | Boost search results from a specific country. | Boost results originating from the given country; most effective for region-specific queries. | 提升来自指定国家的结果权重，对地域性查询效果最明显。 |
| `req.auto_parameters` | Automatically configure search parameters based on query content. | When enabled, let Tavily infer and set search parameters automatically from the query content. | 开启后，由 Tavily 依据查询内容自动推断并设置搜索参数。 |
| `req.include_usage` | Include credit usage information in the response. | When enabled, include credit usage details for this request in the response. | 开启后，在响应中包含本次请求的积分用量明细。 |
| `resp.200.query` | The search query that was executed. | Echo of the search query that was executed. | 本次实际执行的搜索查询回显。 |
| `resp.200.answer` | A short answer to the user's query, generated by an LLM. | Short LLM-generated answer to the query; present only when an answer was requested. | 针对查询由 LLM 生成的简短答案，仅在请求答案时出现。 |
| `resp.200.images` |  | Images matched by the query; present only when image search was requested. | 与查询匹配的图片，仅在请求图片搜索时出现。 |
| `resp.200.images.url` | Image URL. | Direct URL of the matched image. | 匹配图片的直接 URL。 |
| `resp.200.images.description` | Image description. | Caption describing the image; present only when image descriptions were requested. | 描述该图片的文字说明，仅在请求图片描述时出现。 |
| `resp.200.results` |  | Ranked list of web search results. | 按相关度排序的网页搜索结果列表。 |
| `resp.200.results.title` | Title of the search result. | Title of the result page. | 结果页面的标题。 |
| `resp.200.results.url` | URL of the search result. | URL of the result page. | 结果页面的 URL。 |
| `resp.200.results.content` | Content snippet of the search result. | Most relevant text snippet extracted from the result page. | 从结果页面中提取的最相关文本片段。 |
| `resp.200.results.score` | Relevance score of the search result. | Relevance score of the result to the query; higher means more relevant. | 该结果与查询的相关度评分，越高越相关。 |
| `resp.200.results.favicon` | Favicon URL of the search result. | Favicon URL of the result's source site; present only when favicons were requested. | 结果来源站点的 favicon URL，仅在请求 favicon 时出现。 |
| `resp.200.response_time` | Time in seconds it took to complete the request. | Server-side time taken to complete the request, in seconds. | 服务端完成本次请求所用的时间，单位为秒。 |
| `resp.200.usage` |  | Credit usage for this request; present only when usage reporting was requested. | 本次请求的积分用量，仅在请求用量信息时出现。 |
| `resp.200.usage.credits` | Credit usage details for the request. | Number of credits consumed by this request. | 本次请求消耗的积分数。 |
| `resp.200.request_id` | Unique request identifier. | Unique identifier of this request, useful for tracing and support. | 本次请求的唯一标识，便于追踪与排障。 |

## POST /tavily/extract

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | Extract web page content from specified URLs using Tavily Extract. | Fetch and clean the readable content of one or more URLs through Tavily Extract, returning parsed page text plus optional images and credit usage. Use query to rerank the extracted chunks by relevance to your intent. | 通过 Tavily Extract 抓取并清洗一个或多个 URL 的可读正文，返回解析后的页面文本，并可选附带图片与积分用量。可用 query 按你的意图对提取出的片段重排序。 |
| `req.urls` | The URLs to extract content from. | One or more web page URLs to extract content from. | 要提取内容的一个或多个网页 URL。 |
| `req.query` | User intent for reranking extracted content chunks. | Statement of intent used to rerank the extracted content chunks so the most relevant ones surface first. | 用于对提取出的内容片段重排序的意图说明，使最相关的片段优先呈现。 |
| `req.chunks_per_source` | Maximum number of relevant chunks returned per source. | How many of the most relevant text chunks to keep from each URL. | 每个 URL 保留的最相关文本片段数量。 |
| `req.extract_depth` | Depth of the extraction process. | Extraction thoroughness: `basic` is faster and lighter, `advanced` parses more thoroughly at higher latency and cost. | 提取的彻底程度：`basic` 更快更轻量，`advanced` 解析更彻底，但延迟和成本更高。 |
| `req.include_images` | Include a list of images extracted from the URLs. | When enabled, also return images found on the extracted pages. | 开启后，额外返回从被提取页面中找到的图片。 |
| `req.include_favicon` | Include the favicon URL for each result. | When enabled, include the favicon URL of each extracted page. | 开启后，为每个被提取页面附带其 favicon URL。 |
| `req.format` | Format of the extracted web page content. | Output format of the extracted content: `markdown` preserves structure, `text` returns plain text. | 提取内容的输出格式：`markdown` 保留结构，`text` 返回纯文本。 |
| `req.timeout` | Maximum time in seconds to wait for the URL extraction. | Maximum time to wait for a URL to be fetched and extracted, in seconds. | 等待单个 URL 抓取与提取完成的最长时间，单位为秒。 |
| `req.include_usage` | Include credit usage information in the response. | When enabled, include credit usage details for this request in the response. | 开启后，在响应中包含本次请求的积分用量明细。 |
| `resp.200.results` |  | Successfully extracted pages, one entry per URL. | 成功提取的页面，每个 URL 对应一项。 |
| `resp.200.results.url` | URL of the extracted content. | URL whose content was extracted. | 被提取内容的 URL。 |
| `resp.200.results.raw_content` | Extracted raw content from the URL. | Cleaned, parsed content of the page in the requested format. | 按所请求格式清洗解析后的页面内容。 |
| `resp.200.results.favicon` | Favicon URL of the extracted content. | Favicon URL of the extracted page; present only when favicons were requested. | 被提取页面的 favicon URL，仅在请求 favicon 时出现。 |
| `resp.200.failed_results` | List of URLs that could not be processed. | URLs that could not be fetched or extracted. | 无法抓取或提取的 URL 列表。 |
| `resp.200.response_time` | Time in seconds it took to complete the request. | Server-side time taken to complete the request, in seconds. | 服务端完成本次请求所用的时间，单位为秒。 |
| `resp.200.usage` |  | Credit usage for this request; present only when usage reporting was requested. | 本次请求的积分用量，仅在请求用量信息时出现。 |
| `resp.200.usage.credits` | Credit usage details for the request. | Number of credits consumed by this request. | 本次请求消耗的积分数。 |
| `resp.200.request_id` | Unique request identifier. | Unique identifier of this request, useful for tracing and support. | 本次请求的唯一标识，便于追踪与排障。 |

## POST /tavily/crawl

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | Graph-based website traversal tool using Tavily Crawl. | Traverse a website starting from a root URL, following links breadth- and depth-first within configurable limits, and return the extracted content of each visited page. Steer which links are followed with natural-language instructions and path/domain include or exclude filters. | 从根 URL 出发遍历整站，在可配置的深度与广度上限内沿链接抓取，返回每个访问页面的提取内容。可用自然语言指令以及路径/域名的包含或排除筛选，控制跟进哪些链接。 |
| `req.url` | The root URL to begin the crawl. | Root URL where the crawl begins. | 爬取的起始根 URL。 |
| `req.instructions` | Natural language instructions for the crawler. | Natural-language guidance telling the crawler what content to prioritize while traversing links. | 用自然语言告诉爬虫在遍历链接时优先关注哪些内容。 |
| `req.chunks_per_source` | Maximum number of relevant chunks returned per source. | How many of the most relevant text chunks to keep from each crawled page. | 每个被爬页面保留的最相关文本片段数量。 |
| `req.max_depth` | Max depth of the crawl. | How many link hops away from the root URL the crawler may go. | 爬虫从根 URL 出发可跟进的链接跳数（层级深度）。 |
| `req.max_breadth` | Max number of links to follow per level of the tree. | How many links the crawler may follow at each level of the link tree. | 在链接树每一层级上爬虫可跟进的链接数量。 |
| `req.limit` | Total number of links the crawler will process before stopping. | Total number of links the crawler will process before it stops. | 爬虫停止前总共处理的链接数量上限。 |
| `req.select_paths` | Regex patterns to select only URLs with specific path patterns. | Regex patterns; only URLs whose path matches are crawled. | 正则表达式模式；仅爬取路径匹配的 URL。 |
| `req.select_domains` | Regex patterns to select crawling to specific domains or subdomains. | Regex patterns; crawling is restricted to matching domains or subdomains. | 正则表达式模式；将爬取范围限定在匹配的域名或子域名。 |
| `req.exclude_paths` | Regex patterns to exclude URLs with specific path patterns. | Regex patterns; URLs whose path matches are skipped. | 正则表达式模式；路径匹配的 URL 会被跳过。 |
| `req.exclude_domains` | Regex patterns to exclude specific domains or subdomains from crawling. | Regex patterns; matching domains or subdomains are skipped. | 正则表达式模式；匹配的域名或子域名会被跳过。 |
| `req.allow_external` | Include external domain links in the final results list. | When enabled, links pointing to external domains are also kept in the results. | 开启后，指向外部域名的链接也会保留在结果中。 |
| `req.include_images` | Include images in the crawl results. | When enabled, also return images found while crawling. | 开启后，额外返回爬取过程中找到的图片。 |
| `req.extract_depth` | Depth of the extraction process. | Extraction thoroughness for each crawled page: `basic` is faster and lighter, `advanced` parses more thoroughly at higher latency and cost. | 对每个被爬页面的提取彻底程度：`basic` 更快更轻量，`advanced` 解析更彻底，但延迟和成本更高。 |
| `req.format` | Format of the extracted web page content. | Output format of the extracted page content: `markdown` preserves structure, `text` returns plain text. | 提取页面内容的输出格式：`markdown` 保留结构，`text` 返回纯文本。 |
| `req.include_favicon` | Include the favicon URL for each result. | When enabled, include the favicon URL of each crawled page. | 开启后，为每个被爬页面附带其 favicon URL。 |
| `req.timeout` | Maximum time in seconds to wait for the crawl operation. | Maximum time to wait for the crawl operation to finish, in seconds. | 等待整个爬取操作完成的最长时间，单位为秒。 |
| `req.include_usage` | Include credit usage information in the response. | When enabled, include credit usage details for this request in the response. | 开启后，在响应中包含本次请求的积分用量明细。 |
| `resp.200.base_url` | The base URL that was crawled. | Root URL that was crawled. | 实际被爬取的根 URL。 |
| `resp.200.results` |  | Pages visited during the crawl, with their extracted content. | 爬取过程中访问的页面及其提取内容。 |
| `resp.200.results.url` | URL of the crawled page. | URL of the crawled page. | 被爬页面的 URL。 |
| `resp.200.results.raw_content` | Extracted raw content from the page. | Cleaned, parsed content of the crawled page in the requested format. | 按所请求格式清洗解析后的被爬页面内容。 |
| `resp.200.results.favicon` | Favicon URL of the crawled page. | Favicon URL of the crawled page; present only when favicons were requested. | 被爬页面的 favicon URL，仅在请求 favicon 时出现。 |
| `resp.200.response_time` | Time in seconds it took to complete the request. | Server-side time taken to complete the request, in seconds. | 服务端完成本次请求所用的时间，单位为秒。 |
| `resp.200.usage` |  | Credit usage for this request; present only when usage reporting was requested. | 本次请求的积分用量，仅在请求用量信息时出现。 |
| `resp.200.usage.credits` | Credit usage details for the request. | Number of credits consumed by this request. | 本次请求消耗的积分数。 |
| `resp.200.request_id` | Unique request identifier. | Unique identifier of this request, useful for tracing and support. | 本次请求的唯一标识，便于追踪与排障。 |

## POST /tavily/map

| 字段路径 | 源英文（原生，只读） | 加强英文 desc_en | 中文 title_zh |
| --- | --- | --- | --- |
| `(operation)` | Generate comprehensive site maps using Tavily Map. | Discover the link structure of a website starting from a root URL and return the list of reachable URLs, without extracting page content. Useful for mapping a site before deciding what to crawl or extract. | 从根 URL 出发探测整站的链接结构，返回可达 URL 列表，但不提取页面正文。适合在决定爬取或提取之前先摸清站点结构。 |
| `req.url` | The root URL to begin the mapping. | Root URL where the mapping begins. | 站点映射的起始根 URL。 |
| `req.instructions` | Natural language instructions for the crawler. | Natural-language guidance telling the crawler which links to prioritize while mapping the site. | 用自然语言告诉爬虫在映射站点时优先跟进哪些链接。 |
| `req.max_depth` | Max depth of the mapping. | How many link hops away from the root URL the mapping may go. | 映射从根 URL 出发可跟进的链接跳数（层级深度）。 |
| `req.max_breadth` | Max number of links to follow per level of the tree. | How many links may be followed at each level of the link tree. | 在链接树每一层级上可跟进的链接数量。 |
| `req.limit` | Total number of links the crawler will process before stopping. | Total number of links the crawler will process before it stops. | 爬虫停止前总共处理的链接数量上限。 |
| `req.select_paths` | Regex patterns to select only URLs with specific path patterns. | Regex patterns; only URLs whose path matches are mapped. | 正则表达式模式；仅映射路径匹配的 URL。 |
| `req.select_domains` | Regex patterns to select crawling to specific domains or subdomains. | Regex patterns; mapping is restricted to matching domains or subdomains. | 正则表达式模式；将映射范围限定在匹配的域名或子域名。 |
| `req.exclude_paths` | Regex patterns to exclude URLs with specific path patterns. | Regex patterns; URLs whose path matches are skipped. | 正则表达式模式；路径匹配的 URL 会被跳过。 |
| `req.exclude_domains` | Regex patterns to exclude specific domains or subdomains from mapping. | Regex patterns; matching domains or subdomains are skipped. | 正则表达式模式；匹配的域名或子域名会被跳过。 |
| `req.allow_external` | Include external domain links in the final results list. | When enabled, links pointing to external domains are also kept in the results. | 开启后，指向外部域名的链接也会保留在结果中。 |
| `req.timeout` | Maximum time in seconds to wait for the map operation. | Maximum time to wait for the map operation to finish, in seconds. | 等待整个映射操作完成的最长时间，单位为秒。 |
| `req.include_usage` | Include credit usage information in the response. | When enabled, include credit usage details for this request in the response. | 开启后，在响应中包含本次请求的积分用量明细。 |
| `resp.200.base_url` | The base URL that was mapped. | Root URL that was mapped. | 实际被映射的根 URL。 |
| `resp.200.results` |  | Flat list of URLs discovered while mapping the site. | 映射站点过程中发现的 URL 平铺列表。 |
| `resp.200.response_time` | Time in seconds it took to complete the request. | Server-side time taken to complete the request, in seconds. | 服务端完成本次请求所用的时间，单位为秒。 |
| `resp.200.usage` |  | Credit usage for this request; present only when usage reporting was requested. | 本次请求的积分用量，仅在请求用量信息时出现。 |
| `resp.200.usage.credits` | Credit usage details for the request. | Number of credits consumed by this request. | 本次请求消耗的积分数。 |
| `resp.200.request_id` | Unique request identifier. | Unique identifier of this request, useful for tracing and support. | 本次请求的唯一标识，便于追踪与排障。 |

